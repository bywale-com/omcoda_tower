/**
 * Stable CT seed ids for stand-in wiring demos.
 * Firm = Cedar Pathways; contact/client ids match `src/app/data` (sarah, …).
 */
export const CT_DEMO = {
  consultantId: "consultant-cedar-sarah",
  firmId: "firm-cedar",
} as const;
