import type { SmeSeat } from "../../../types";

/** Auto-generated by scripts/sync-capability-sme-from-docs.py — do not edit by hand. */
export const seat: SmeSeat = {
  id: "eligibility-reference-pipeline",
  label: "C3 — Eligibility + reference-data pipeline",
  domain: "Ingestion, immutable publish/versioning, diffs, impact-scoped re-score, freshness/volatility, and reconciliation for immigration reference criteria.",
  whyExists: "Evaluation logic is specified; the pipeline that keeps criteria current and re-scores clients is the capability gap.",
  axis: "capability",
  items: [
    {
      id: "pipe-01",
      consideration: "What end-to-end stages must the reference-table ingestion pipeline expose so house upkeep can move IRCC/public criteria from detection to published data without a code deploy?",
      thesisGap: "Seat 2 named Import criteria + Publish version shells; without staged pipeline states, “ingestion” collapses into ad-hoc CSV paste with no audit spine.",
      solution: "Run a fixed pipeline — detect change → fetch canonical source → parse to schema → validate → hold as draft → dual-check → Publish version → enqueue consumers — so that every published row is traceable to a stage outcome and failed stages never become current.",
      references: [
        { title: "Express Entry: Rounds of invitations", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations.html" },
        { title: "How `operator-reference-data.md` (edit/import → Publish version)", url: "" },
        { title: "Seed §5.7 Directive 3 — versioned tables; no code deploy to update", url: "" },
      ],
      implementationProblem: "Reference data module exists as a shell; operators have no staged path from Canada.ca change to published-current, so currency work becomes engineering tickets or unlogged edits.",
      implementation: "On Reference data, open an ingestion-run row; the run detail shows a stage rail Detect → Fetch → Parse → Validate → Draft → Dual-check → Publish version.\nOn Import criteria, the stage rail has Advance stage / Fail stage controls and an immutable stage-log panel; failed stages stay Draft and never become published-current.",
      implementationAdds: ["ingestion-run", "detect", "fetch", "parse", "validate", "draft", "dual-check", "stage-log"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-02",
      consideration: "How should the pipeline bind canonical sources vs discovery alerts so scrapes never publish from secondary blogs?",
      thesisGap: "Domain seat forbade blog truth; pipeline must enforce source class at fetch time or operators will “just import the alert URL.”",
      solution: "Tag each table family’s source binding as canonical (Canada.ca / MI / ESDC NOC) or discovery-alert-only; allow fetch+parse only from canonical URLs, and route discovery hits into Detect without Publish rights — so that the pipeline cannot promote a secondary page to published-current.",
      references: [
        { title: "Ministerial instructions — Express Entry rounds", url: "https://www.canada.ca/en/immigration-refugees-citizenship/corporate/mandate/policies-operational-instructions-agreements/ministerial-instructions/express-entry-rounds.html" },
        { title: "Express Entry: Category-based selection", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations/category-based-selection.html" },
        { title: "Seat 2 ref-03 (canonical Canada.ca / MI; blogs = discovery only)", url: "" },
      ],
      implementationProblem: "Without source-class enforcement in the pipeline, a same-day draw alert from a blog can be imported as if it were ministerial truth.",
      implementation: "On Import criteria, each Reference table row has canonical-source URL fields and Discovery-alert-only feed chips.\nOn Reference data Detect, a discovery alert row can open an ingestion run, but the Fetch stage still uses the canonical Canada.ca / MI URL before Dual-check / Publish version.",
      implementationAdds: ["canonical-source", "discovery-alert-only", "source-class"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-03",
      consideration: "What change-detection pattern should the pipeline use against Canada.ca pages that do not offer a vendor CDC feed?",
      thesisGap: "“Poll daily” (seat 2 SLA) needs a concrete detect mechanism; without hash/diff detect, house either over-fetches into noise or misses mid-day multi-draws.",
      solution: "Detect via scheduled fetch of canonical pages plus content-hash (and ETag/Last-Modified when present); open an ingestion run only when the hash diverges or an on-alert path fires — so that Detect is change-driven, not a blind re-import every poll.",
      references: [
        { title: "Express Entry: Check your score", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/check-score.html" },
        { title: "CRS criteria", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/check-score/crs-criteria.html" },
        { title: "Industry practice: content-hash / ETag polling as a CDC substitute when sources are pull-only (document as ops pattern, not a statute)", url: "" },
      ],
      implementationProblem: "Scheduled polls without change-detect either spam Dual-check with identical drafts or miss same-business-day rounds between weekly reviews.",
      implementation: "On Reference data, the Detect schedule table shows one row per table family with poll cadence, last-seen content-hash, and ETag columns.\nOn Reference data, a diverged-hash row opens an ingestion run automatically; the on-alert Detect row also has Open ingestion run, while identical hashes do not create Draft versions.",
      implementationAdds: ["content-hash-detect", "ETag", "on-alert", "change-driven-run"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-04",
      consideration: "What schema-contract gate must Parse/Validate enforce before a draft may enter Dual-check?",
      thesisGap: "Seat 2 required validation; capability residual is a machine-enforceable contract per table family so HTML shifts cannot reach Dual-check.",
      solution: "Define a versioned schema contract per table family (required columns, types, cardinality bands, CRS-floor sanity, category NOC set non-empty); Validate fails closed into Draft-hold with house alert — so that freshness never outranks structural correctness.",
      references: [
        { title: "Rounds of invitations table shape", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations.html" },
        { title: "Seat 2 ref-16 (schema validation + dual-human/checksum; failed imports stay draft)", url: "" },
        { title: "Rules-engine practice: separate data facts from policy rules (Open Policy Agent / Drools data vs rule packs as pattern refs — not product choice)", url: "" },
      ],
      implementationProblem: "Broken scrapes can ship empty category sets or swapped CRS columns if humans are the only gate after a green “import succeeded” toast.",
      implementation: "On Import criteria, each Reference table family row has a Schema contract version dropdown plus required-column, type, and sanity-band fields.\nOn Import criteria Validate, the Validate status row shows Draft-hold and alerts house upkeep on contract failure; Dual-check / Publish version stay blocked until Validate passes.",
      implementationAdds: ["schema-contract", "draft-hold", "sanity-bands", "fail-closed-validate"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-05",
      consideration: "How must published versions stay immutable so audit and mid-flight pins remain meaningful?",
      thesisGap: "Editable “current” rows rewrite history under a consultant’s license; pins become lies.",
      solution: "Treat Publish version as content-addressed and immutable; corrections ship only as a new version (or explicit rollback publish) that supersedes by id — so that every evaluation pin resolves to bytes that cannot be silently rewritten.",
      references: [
        { title: "Seat 2 ref-10 / ref-11 (provenance; draft → published-current; evaluation pins)", url: "" },
        { title: "Audit trail practices: append-only version lineage (content-addressed publish) — industry pattern for regulated scoring systems", url: "" },
      ],
      implementationProblem: "If operators can edit published rows in place, Analysis that cited a version no longer matches what the store returns under that id.",
      implementation: "On Publish version, the publish modal shows the content-hash snapshot and freezes it when the primary Publish version button is clicked; in-place edit of published-current is refused.\nOn Reference tables, click Open correction draft on a published row to create Draft → Dual-check → Publish version with supersedes-version-id; prior versions remain readable for pins and Audit trail.",
      implementationAdds: ["immutable-publish", "content-addressed", "supersedes-version-id", "no-in-place-edit"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-06",
      consideration: "What provenance must each ingestion run and published version write into Audit trail for liability and replay?",
      thesisGap: "Domain listed provenance fields; pipeline must emit them as first-class Change events or house cannot prove what backed a score.",
      solution: "On every stage transition and Publish version, append a Change event with source URL(s), retrieved-at, importer, dual-check attestation, effective-from, supersedes-version-id, content-hash, and ingestion-run id — so that any Evaluation pack result can be replayed to a government page and an actor.",
      references: [
        { title: "Seat 2 ref-10 (source URL, retrieved-at, importer, dual-check, effective-from, supersedes, content hash)", url: "" },
        { title: "World §1.1 reference-data upkeep; §2.2 law/public-reference change", url: "" },
        { title: "Operator **Audit trail** / **Change event** surfaces", url: "" },
      ],
      implementationProblem: "Without pipeline-emitted Audit trail events, disputes collapse into “the system said so” with no retrieveable import lineage.",
      implementation: "On Publish version, the provenance checklist requires source URL, retrieved-at, importer, dual-check attestation, effective-from, supersedes-version-id, content-hash, and ingestion-run id before the Publish version button unlocks.\nOn Audit trail, filter Change event rows by Reference table, ingestion-run id, and actor; each row links the source URL and content-hash used.",
      implementationAdds: ["ingestion-run-id", "dual-check-attestation", "change-event-provenance"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-07",
      consideration: "Who may Dual-check and Publish, and how does the pipeline enforce separation of duties?",
      thesisGap: "Single-actor import+publish is the silent wrong-cutoff class failure; capability must encode the gate, not a wiki policy.",
      solution: "Require Dual-check attestation from an actor distinct from the importer (or importer + independent checksum job) before Publish version unlocks — so that one transcription error cannot become published-current alone.",
      references: [
        { title: "Seat 2 ref-16 / ref-25 (dual-human or human+checksum; house ops dual-check publish rights)", url: "" },
        { title: "Audit / maker-checker practice for production reference data (ops pattern)", url: "" },
      ],
      implementationProblem: "House SLA pressure on draw day encourages the same person to import and publish; policy without a hard gate fails.",
      implementation: "On Import criteria Dual-check, the Attest checkbox stays disabled when your actor id matches the importer unless a checksum job signature is present.\nOn Publish version, the Publish version button unlocks only after Dual-check passes; same-actor self-approve is refused except under a logged break-glass incident flag.",
      implementationAdds: ["dual-check-actor", "checksum-job", "break-glass-incident"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-08",
      consideration: "How should the pipeline quarantine partial or conflicting imports across table families that must publish together?",
      thesisGap: "Publishing NOC membership without updated predicates (or the reverse) creates a coherent-looking but false “current” category definition.",
      solution: "Support publish groups: when a table family declares a co-publish set (e.g. category membership + predicates), hold all members in Draft until each Validates; publish atomically under one publish-group id — so that packs never see a half-applied ministerial change.",
      references: [
        { title: "Category-based selection", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations/category-based-selection.html" },
        { title: "Seat 2 ref-05 / ref-24 (category membership + predicates versioned together)", url: "" },
      ],
      implementationProblem: "Independent per-table publishes can interleave mid-flight evaluations across inconsistent category halves.",
      implementation: "On Reference tables, each co-dependent family row can be assigned to a Publish group with a group-id chip.\nOn Publish version, the group publish modal either publishes all members atomically or keeps every member Draft-hold. On Configuration libraries → Evaluation packs catalog, open Evaluation pack editor; the reference pin dropdown omits partial Draft-hold groups, so packs never pin a partial group.",
      implementationAdds: ["publish-group", "atomic-publish", "co-dependent-families"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-09",
      consideration: "What must a rule/reference-version diff contain so operators see *what changed* between two published versions?",
      thesisGap: "“New version published” without a structured diff forces humans to eyeball CSV; who-to-rescore stays guesswork (**partial** today).",
      solution: "On Publish version success, compute a structured diff (added/removed/changed rows and fields, typed by table family: floor, membership, predicate, grid cell) stored beside the version — so that re-score planning reads machine diff, not tribal knowledge.",
      references: [
        { title: "CRS criteria", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/check-score/crs-criteria.html" },
        { title: "Seat 2 ref-11 / ref-12 (pins vs current; cutoff publish → re-score)", url: "" },
        { title: "OPA / Drools practice: decision diffs against data version changes (pattern ref)", url: "" },
      ],
      implementationProblem: "Publish bumps a version id with no field-level change set, so house cannot tell a new round row from a TEER remap that invalidates half the book.",
      implementation: "On Publish version, click Version diff for the supersedes pair; the diff panel shows added / removed / changed rows typed by change class.\nOn Reference data, attach that Version diff to the Change event row for Oversight and Analysis → Evaluation provenance consumers.",
      implementationAdds: ["version-diff", "change-class", "added", "removed", "changed"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-10",
      consideration: "How does the pipeline map a Version diff to *who must re-score* (cohort selection)?",
      thesisGap: "Book-wide rescore on every publish is wasteful and noisy; under-scoped rescore leaves stale Analysis under a new published-current.",
      solution: "Classify each diff change-class into impact scopes — round/cutoff → firms with draw/category outcome families bound; membership/predicate → firms asserting those category/NOC checks; grid/CLB/funds → firms whose packs read those table keys — and enqueue only contacts under bound Evaluation packs in those scopes — so that “what changed → who re-scores” is deterministic.",
      references: [
        { title: "Seed Core bet: re-evaluate as IRCC-shaped rules move", url: "" },
        { title: "Seat 2 ref-12 (book re-eval on cutoff publish; deltas; no auto-outreach)", url: "" },
        { title: "How Firm operations bind — packs bound per firm", url: "" },
      ],
      implementationProblem: "Without impact scoping, every Publish version either hammers the whole fleet or skips firms that actually consume the changed table.",
      implementation: "On Publish version, open the Impact scope panel; it derives cohort rows from Version diff change-class and bound Evaluation pack versions. On Firm operations bind, click firm row in firm-bind index. On firm detail, click Bind packs. On Bind packs modal, choose Evaluation pack version from the published-only dropdown; choose Automation pack version from the published-only dropdown; choose Engagement template version from the published-only dropdown; click Bind. Those bound version chips become the re-score cohort inputs when they reference the affected table keys / outcome families.",
      implementationAdds: ["impact-scope", "change-class→cohort", "bound-pack-consumers"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-11",
      consideration: "Should armed-only vs Active firms, and pinned vs follow-current packs, enter the re-score cohort the same way?",
      thesisGap: "Re-scoring Active follow-current books is the product bet; blindly rewriting pinned or armed-only books without policy creates distrust or silent freeze.",
      solution: "Always compute and store signals for Active firms on follow-current packs; for Armed-only, compute silently for readiness but do not enroll motion; for explicit pack pins inside expiry, compute a shadow adopt-current diff but do not flip the pin until expiry/incident policy says adopt — so that cohort rules respect bind state and pin law.",
      references: [
        { title: "Seat 2 ref-13 (follow published-current default; pins with expiry + reason)", url: "" },
        { title: "How Firm operations bind — Armed / Active", url: "" },
        { title: "Engine 2 give-back — signals recorded even when motion waits", url: "" },
      ],
      implementationProblem: "One fan-out policy for all tenants either wakes armed-only noise or leaves pinned packs looking “current” when they are not.",
      implementation: "On Configuration libraries → Evaluation packs catalog, open Evaluation pack editor; set re-score cohort mode with the selector (Active-follow-current | Armed-only | Pinned-shadow); click Publish version. On Oversight and the same Evaluation pack editor, Pin age and shadow-diff pending-adopt rows show which firms are active, armed-only, or pinned-shadow.",
      implementationAdds: ["active-follow-current", "armed-signal-only", "pinned-shadow", "shadow-diff"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-12",
      consideration: "What job semantics link Publish version → re-evaluation so mid-flight firms get explainable deltas rather than silent rewrite?",
      thesisGap: "Named in seat 2; not built as a durable job with cause codes (**partial**).",
      solution: "Enqueue a durable re-evaluation job per impact cohort with cause = reference-publish (or reference-correction on rollback), adopt-current per pack policy, and emit eligibility deltas against the prior pinned result — so that Board/Analysis updates are attributable to a publish id.",
      references: [
        { title: "Seat 2 ref-11 / ref-12 / ref-19 (pins retained on emitted Analysis; enqueue re-eval; rollback corrective deltas)", url: "" },
        { title: "Seed §5.7 / Engine 2 re-evaluation on fact and rule movement", url: "" },
      ],
      implementationProblem: "Publish updates “current” while already-shown Analysis rewrites in place with no cause, destroying consultant trust.",
      implementation: "On Publish version, click Enqueue Re-evaluation; the job table shows rows keyed by publish id and impact-scope.\nOn Analysis → Evaluation provenance, delta rows show was / now with cause reference-publish or reference-correction; prior Analysis snapshots keep their reference-version ids for Audit trail.",
      implementationAdds: ["re-evaluation-job", "cause:reference-publish", "cause:reference-correction", "was/now-delta"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-13",
      consideration: "What must the pipeline forbid after a reference-driven re-score so law-side detection cannot auto-fire outreach?",
      thesisGap: "Capability jobs that “helpfully” enroll sequences on cutoff move violate engagement law and desk license risk.",
      solution: "Constrain reference-driven re-score outputs to signals + Board Phase signal updates under Engine 2 precedence; enrollment/send remain owned by Firm operations bind runners and Send gates — so that criteria currency drives detection, not unbidden CEM motion.",
      references: [
        { title: "Engine 2 law — signal/action decoupling — [`immigration-rules-engine2-giveback.md`](../../product/immigration-rules-engine2-giveback.md)", url: "" },
        { title: "Seat 2 ref-12 (never auto-fire outreach solely because a number moved)", url: "" },
        { title: "Seat 1 elig-25 (stale reference suppresses draw/category reactivation)", url: "" },
      ],
      implementationProblem: "A naive “rescore then reactivate” hook on Publish version creates false reactivation storms on every draw.",
      implementation: "On Analysis → Evaluation provenance, reference-driven Re-evaluation job rows write signals and Board Phase signal only.\nOn Firm operations bind firm detail → Send gates panel, the enrollment readiness row shows reference-publish auto-enrollment refused as a deny chip; runners still apply Engine 2 precedence when a separate motion decision runs.",
      implementationAdds: ["signals-only-on-reference-publish", "no-auto-enrollment", "phase-signal-update"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-14",
      consideration: "How should Version diff interact with Evaluation pack editor authoring so pack structural rules and reference facts stay separated?",
      thesisGap: "Diffs that dump new numeric floors into pack editors invite authors to hardcode “temporary” literals again.",
      solution: "Surface Version diff as reference-input changelog only; Evaluation pack editor may link assertions to table keys/version pins but cannot paste diff constants into rule bodies — review rejects numeric public-criteria literals — so that Directive 3 stays enforceable when data moves.",
      references: [
        { title: "Seat 2 ref-14 / ref-15 / ref-26 (no evaluator literals; packs cite table keys)", url: "" },
        { title: "OPA/Drools pattern: policy compares inputs; data bundled/versioned separately", url: "" },
      ],
      implementationProblem: "Authors treat a cutoff diff as copy-paste fodder for pack conditions, re-breaking currency.",
      implementation: "On Configuration libraries → Evaluation packs catalog, open Evaluation pack editor; open Reference changelog (Version diff) as a read-only table-key impact panel. In publish review, the checklist rejects rule bodies that embed numeric public-criteria literals present in Reference tables; click Publish version only after review passes.",
      implementationAdds: ["reference-changelog", "table-key-link-only", "reject-numeric-literals"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-15",
      consideration: "What trigger must fire when Client Data write-back lands so re-score is immediate and Board-visible?",
      thesisGap: "Named in outcomes and Engine 2; **partial** — no durable trigger from Client Data → evaluator → Board.",
      solution: "On Client Data write-back commit, emit a fact-change event that enqueues an immediate per-contact Re-evaluation job against bound Evaluation packs and current (or pinned) reference versions, then refresh Board Phase signal and Live brief — so that new answers change detection before the next human glance.",
      references: [
        { title: "Engine 2 give-back — any write-back (form submit or consultant entry) → immediate re-evaluation", url: "" },
        { title: "How `contact-book.md` / `contact-refresh.md` — write-back re-evaluates (machinery)", url: "" },
        { title: "Seat 1 / desk handoff — brief recompute on write-back", url: "" },
      ],
      implementationProblem: "Forms and consultant edits save facts while Analysis and Board stay stale until a batch job or page reload folklore.",
      implementation: "On Client Data, click Commit write-back; the save action emits a fact-change event and automatically enqueues Re-evaluation for that contact.\nOn Board, the Client row Phase signal chip updates from cause fact-write-back; on Live brief, the same re-score feeds meeting-grade views.",
      implementationAdds: ["fact-change-event", "cause:fact-write-back", "immediate-re-eval", "phase-signal-refresh"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-16",
      consideration: "How should the pipeline order and idempotently merge concurrent write-back re-scores with reference-publish re-scores?",
      thesisGap: "Draw-day publish + form submit can race; last-writer-wins without keys duplicates signals or drops one cause.",
      solution: "Key Re-evaluation jobs by (contact id, trigger event id) with a merge window that runs once against the latest fact snapshot and the pack’s adopt-current/pin policy; persist both cause references on the result — so that concurrent law-side and fact-side triggers do not double-enroll or lose provenance.",
      references: [
        { title: "Engine 2 — one client, one motion; signals recorded as found", url: "" },
        { title: "Seat 2 ref-11 (evaluation records exact reference-version ids)", url: "" },
        { title: "CDC / event-sourcing practice: idempotent consumers keyed by event id", url: "" },
      ],
      implementationProblem: "Two jobs rewriting Analysis for one contact produce duplicate Phase signals or a result that cites neither publish id nor write-back id.",
      implementation: "On Evaluation packs re-score job detail, the job row shows coalesced Re-evaluation for the same contact within a short merge window and stored trigger event ids for both fact-write-back and reference-publish when both apply.\nOn Audit trail, a single result Change event row lists all trigger causes and the reference-version ids used.",
      implementationAdds: ["job-idempotency-key", "merge-window", "multi-cause-result"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-17",
      consideration: "What Board / Phase signal contract must write-back re-score satisfy so consultants see next motion class without opening every Client Data pane?",
      thesisGap: "Re-score that only updates hidden evaluator JSON fails the “consultant does nothing” inhabit loop.",
      solution: "Materialize re-score outcomes onto Board Phase signal with motion class (reactivation-worthy / nudge-only / none), confidence tier, and reference-version / pack-version pins — never IRCC-determination language — so that write-back → re-score → next motion is visible at list grade.",
      references: [
        { title: "Engine 2 precedence (reactivation vs nudge)", url: "" },
        { title: "Seat 1 elig-03 / elig-04 (service-eligible; reactivation-worthy vs nudge-only)", url: "" },
        { title: "Personas — Board as inhabit surface for phase / eligibility signals", url: "" },
      ],
      implementationProblem: "Signals die in Client Data detail; Board still shows pre-write-back posture and consultants miss meeting-grade changes.",
      implementation: "On Board Client row Phase signal, the chip shows motion class and service-eligible posture refreshed after write-back re-score.\nOn Board, open the Phase signal detail drawer to see pack + reference version pin ids for the signal without claiming an IRCC determination.",
      implementationAdds: ["motion-class-on-board", "pack-version-pin", "reference-version-pin", "not-IRCC-determination"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-18",
      consideration: "How must Book readiness constrain post-re-score motion so newly eligible-but-unreachable contacts do not enter sequences?",
      thesisGap: "Capability re-score can mark reactivation-worthy while Book readiness still fails channel/consent — runners must not treat re-score as enrollment authority.",
      solution: "Keep Re-evaluation outputs as eligibility/detection signals; require Book readiness Audit reachable (and Send gates / silence ledger) before any automatic enrollment — so that law-side or fact-side re-score never bypasses reachability.",
      references: [
        { title: "How `operator-book-readiness.md` — reachability verdicts gate sequence-ready", url: "" },
        { title: "HANDOFF crm-14 / casl-15 — enrollment inhibitors; Audit reachable", url: "" },
        { title: "Engine 2 — detection vs action separation", url: "" },
      ],
      implementationProblem: "A contact becomes draw-cleared on Publish version or write-back while email is invalid / silenced; naive hooks enroll anyway.",
      implementation: "On Firm operations bind firm detail → Send gates panel, enrollment readiness rows require Book readiness reachable verdict even when Phase signal is reactivation-worthy.\nOn Book readiness Audit run → Verdict list, click Re-batch after import refresh without clearing eligibility signals — detection and reachability stay separate axes.",
      implementationAdds: ["eligibility≠reachable", "audit-reachable-gate", "signal-retained"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-19",
      consideration: "What freshness / volatility classes should each reference table family carry so recheck cadence is not one global cron?",
      thesisGap: "Domain named SLAs; **new** capability is machine volatility flags that drive Detect schedules and past-freshness gates (roster: freshness/volatility + recheck; HANDOFF ref-23 is a consumer that must re-resolve funds URL each cycle when that table is asserted).",
      solution: "Assign each table family a volatility class — high (round/cutoff), medium (category membership/predicates, language charts when IRCC announces tests), low (CRS/FSW grids, ECA maps, funds-by-family-size when asserted) — stored as data on Reference tables — so that Detect cadence and freshness SLA clocks follow class, not a single fleet cron.",
      references: [
        { title: "Seat 2 ref-04 (draws: same business day), ref-05 (categories: on-change + annual), ref-07/ref-08 (grids/language: on-change), ref-22 (ECA: quarterly+on-change), ref-23 (funds: quarterly+on-change when asserted; URL NEEDS VERIFICATION each cycle)", url: "" },
        { title: "Rounds of invitations", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations.html" },
      ],
      implementationProblem: "One daily job either misses multi-draw days or burns Dual-check capacity re-verifying static grids every night.",
      implementation: "On Reference tables, each family row has a Volatility class selector (high / medium / low) with default SLA from the house matrix.\nOn Reference data Detect, schedule rows and on-alert rows follow Volatility class; funds tables (when asserted) show low + mandatory source-URL re-resolve each verify cycle.",
      implementationAdds: ["volatility-class", "high", "medium", "low", "source-URL-re-resolve"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-20",
      consideration: "How do freshness flags become pack-consumable gates so stale pins cannot silently assert draw/category outcomes?",
      thesisGap: "Freshness SLA exists as prose; packs need a boolean/time flag from the pipeline (**new**).",
      solution: "Maintain per-table last-verified-at, freshness-SLA deadline, and past-freshness flag updated by Detect/verify jobs; Evaluation packs read “published-current AND not past-freshness” before draw/category assertions — so that wrong-cutoff reactivation cannot fire on stale mirrors.",
      references: [
        { title: "Seat 1 elig-25 — published & not-past-freshness reference pin; else reference data unavailable", url: "" },
        { title: "Seat 2 ref-04 / CROSS-CUTTING freshness edge", url: "" },
        { title: "CRS tool + criteria pages as currency witnesses", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/check-score.html" },
      ],
      implementationProblem: "Packs pin a version id forever; without past-freshness, a week-old cutoff still clears reactivation-worthy.",
      implementation: "On Reference tables, each family row shows last-verified-at, freshness-SLA, and Past-freshness flag columns.\nOn Configuration libraries → Evaluation packs catalog, open Evaluation pack editor; set draw-cutoff and category-occupation assertions to require Published & not past freshness; click Publish version. Otherwise Analysis shows Reference data unavailable and those reactivation paths suppress.",
      implementationAdds: ["last-verified-at", "freshness-SLA", "past-freshness", "reference-data-unavailable"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-21",
      consideration: "What recheck cadence scheduler must the pipeline run so volatility classes and on-change Detect stay honest?",
      thesisGap: "Cadence without a scheduler object returns to engineering cron folklore; ops cannot see missed SLAs.",
      solution: "Run a house-visible cadence scheduler: high at least daily + on-alert same-business-day; medium on hash-change + scheduled verify (≥ weekly); low on hash-change + quarterly verify; miss → Oversight / Reference data SLA breach alert — so that recheck is an operable pipeline, not a hope.",
      references: [
        { title: "Seat 2 ref-25 (house ops SLA matrix; engineering builds pipeline)", url: "" },
        { title: "Ministerial instructions rounds ledger", url: "https://www.canada.ca/en/immigration-refugees-citizenship/corporate/mandate/policies-operational-instructions-agreements/ministerial-instructions/express-entry-rounds.html" },
        { title: "Category-based selection page", url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/express-entry/rounds-invitations/category-based-selection.html" },
      ],
      implementationProblem: "SLAs live in Pass2 prose; nothing pages house when a high-volatility table skips verify through a multi-draw week.",
      implementation: "On Reference data, open Recheck cadence; the cadence table shows next-due / last-verified per table family by Volatility class.\nOn Oversight, SLA breach alert rows appear when Detect/verify misses the class deadline; draw-day on-call can jump to Import criteria from the alert row.",
      implementationAdds: ["recheck-cadence", "next-due", "SLA-breach-alert", "same-business-day"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "pipe-22",
      consideration: "What continuous reconciliation and pipeline Audit trail prove the mirror still matches Canada.ca after publish?",
      thesisGap: "Publish-time Dual-check does not catch silent post-publish page edits or failed Detect; firms must not be unpaid QA.",
      solution: "Schedule reconciliation jobs that re-fetch canonical snapshots, diff against published-current hashes, alert on drift, sample contacts whose signals flipped on the last publish, and log all pipeline stages to Audit trail — so that wrong data is caught by upkeep ops before client complaint.",
      references: [
        { title: "Seat 2 ref-17 (house reconciliation vs live snapshot; drift alert; sample flipped evaluations)", url: "" },
        { title: "Rounds ledger", url: "https://www.canada.ca/en/immigration-refugees-citizenship/corporate/mandate/policies-operational-instructions-agreements/ministerial-instructions/express-entry-rounds.html" },
        { title: "Audit trail practices for data pipelines (drift detect + sampled replay)", url: "" },
      ],
      implementationProblem: "After a clean publish, IRCC HTML or operator error can drift while packs keep scoring; no house alarm fires.",
      implementation: "On Reference data, the Reconciliation job row runs on a fixed cadence and shows drift-alert status vs live Canada.ca snapshot.\nOn Reference data, open the flip-sample drawer to spot-check evaluations that flipped on the last Publish version.\nOn Audit trail, filter Change event rows by ingestion, publish, re-evaluation, and reconciliation to trace the pipeline end-to-end.",
      implementationAdds: ["reconciliation-job", "drift-alert", "flip-sample", "pipeline-audit-end-to-end"],
      implementationPlant: "not_done",
      status: "verified",
    },
  ],
};
