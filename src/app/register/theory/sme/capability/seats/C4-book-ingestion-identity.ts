import type { SmeSeat } from "../../../types";

/** Auto-generated by scripts/sync-capability-sme-from-docs.py — do not edit by hand. */
export const seat: SmeSeat = {
  id: "book-ingestion-identity",
  label: "C4 — Book ingestion & identity resolution",
  domain: "Messy book import → normalized, deduped, Q-ID-mapped, validation-ready contacts (field mapping, identity, reachability class).",
  whyExists: "Field mapping, dedup/identity, and validation-class are specialist data work beyond a shallow CSV import form.",
  axis: "capability",
  items: [
    {
      id: "ingest-01",
      consideration: "What must ingestion capture from a messy CSV/CRM export before any Q-ID assignment?",
      thesisGap: "Shallow CSV import assumes fixed column names; practice books use arbitrary headers and silent renames.",
      solution: "Capture a **source header inventory + sample values + provenance stamp** on every land so that mapping decisions are reproducible against the actual file, not an assumed schema.",
      references: [
        { title: "dbt tests / freshness & schema documentation practice", url: "" },
        { title: "SplitForge / HubSpot export column inventories", url: "" },
        { title: "Apollo import field-mapping guides", url: "" },
      ],
      implementationProblem: "Imports that jump straight to Contacts mutation without recording what columns arrived make Field→Q-ID mapping unverifiable and break re-import honesty.",
      implementation: "On Imports, after Authorize book hand-over lands a file/export, you can now open Source header inventory (headers, sample cells, provenance timestamp) before Field map.  \nOn Contacts, you can now refuse silent mutation until that inventory exists for the land batch.",
      implementationAdds: ["source-header-inventory", "provenance-stamp"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-02",
      consideration: "What intermediate vocabulary sits between arbitrary firm columns and Tower Q-IDs?",
      thesisGap: "Mapping firm labels directly onto eligibility Q-IDs conflates directory channels with evaluable facts and invents IDs for noise columns.",
      solution: "Normalize into a **canonical contact vocabulary** (identity, display name, channel candidates, opaque attributes, suppression flags) first so that Q-ID mapping only attaches where evaluable facts exist.",
      references: [
        { title: "CRM merge / field-standardization practice (HubSpot property mapping", url: "" },
        { title: "Salesforce data-import maps)", url: "" },
        { title: "entity-resolution pipelines that normalize to a canonical record model before match", url: "" },
      ],
      implementationProblem: "Product language jumps from CSV headers to Q-IDs; practice needs a stable contact layer so Book readiness and Evaluation packs do not share one brittle rename step.",
      implementation: "On Imports, you can now map source columns into Canonical contact fields (identity, display name, channel candidates, opaque attributes, suppression flags) before any Q-ID attach.  \nOn Book readiness, you can now audit against that canonical layer — not raw header strings.",
      implementationAdds: ["canonical-contact", "opaque-attributes", "channel-candidates"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-03",
      consideration: "How should firm columns that *do* carry evaluable facts attach to Q-IDs?",
      thesisGap: "Roster marks Field→Q-ID mapping **partial** — prototype CSV flow exists but does not produce evaluable attachments Audit/packs can trust.",
      solution: "Require an explicit **Field→Q-ID map version** (source column → Q-ID, transform, confidence) on each land batch so that evaluable facts are attributable and remappable without rewriting Contacts.",
      references: [
        { title: "Rules-engine / form-field catalogs in eligibility packs", url: "" },
        { title: "dbt column-level documentation", url: "" },
        { title: "CRM custom-property → standard-field maps", url: "" },
      ],
      implementationProblem: "Without versioned Field→Q-ID maps, shallow import leaves eligibility packs starved or silently guessing from free-text columns.",
      implementation: "On Imports, you can now edit and save a Field→Q-ID map version (column → Q-ID + transform + confidence) for the land batch.  \nOn Book readiness / Audit run, you can now read which Q-IDs the batch attached — unmapped evaluable candidates stay Unmapped, not invented.",
      implementationAdds: ["field-q-id-map", "map-version", "unmapped", "transform", "confidence"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-04",
      consideration: "What happens to columns that do not match a Q-ID or canonical channel field?",
      thesisGap: "Dropping unknown columns loses firm suppressions and future map targets; inventing Q-IDs for every header pollutes Evaluation packs.",
      solution: "Persist unmapped columns as **opaque attributes on the contact** (and surface Unmapped on the map) so that nothing is invented as evaluable and nothing firm-supplied is silently discarded.",
      references: [
        { title: "HubSpot “unmapped properties” import behavior", url: "" },
        { title: "data-quality practice of quarantine over silent drop", url: "" },
      ],
      implementationProblem: "Silent drop and over-eager Q-ID minting are both wrong; Imports must keep firm data without claiming evaluability.",
      implementation: "On Imports, you can now leave columns Unmapped and still land them as opaque attributes on Contacts.  \nOn Field→Q-ID map, you can now promote an opaque attribute to a Q-ID later without re-uploading the whole book.",
      implementationAdds: ["unmapped", "opaque-attributes", "promote-later"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-05",
      consideration: "Which normalization transforms must run before identity match and validation-class checks?",
      thesisGap: "Raw strings fail match keys and validators inconsistently (whitespace, case, punctuation, locale dates).",
      solution: "Apply a fixed **staging transform set** (trim, Unicode normalize, email lowercase domain/local policy, phone parse attempt, empty-sentinel → null) before match and validation so that identity and reachability share one cleaned layer.",
      references: [
        { title: "libphonenumber normalization practice", url: "" },
        { title: "Unicode NFKC / email local-part case conventions", url: "" },
        { title: "dbt staging models that cast/clean before marts", url: "" },
      ],
      implementationProblem: "Matching and validators fight dirty strings if normalization is optional or per-surface.",
      implementation: "On Imports, you can now preview Normalized values (trim, Unicode, email case policy, phone parse attempt, empty-sentinel→null) before land commits.  \nOn Book readiness, you can now consume only the normalized layer for Audit run — raw source remains in provenance.",
      implementationAdds: ["normalized-preview", "empty-sentinel-null", "staging-transforms"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-06",
      consideration: "What email hygiene belongs in ingestion vs deliverability infrastructure (C1)?",
      thesisGap: "Ingestion may treat any `@` string as a channel; C1 deliverability must not own basic syntax hygiene at land time.",
      solution: "Enforce **syntax-class email hygiene at land** (addr-spec shape, length, forbidden whitespace) and pass survivors as email channel candidates so that deliverability-class checks remain Book readiness / C1 consumers — not import blockers for every soft failure.",
      references: [
        { title: "RFC 5321 / 5322 addr-spec practice", url: "" },
        { title: "IETF email address internationalization notes", url: "" },
        { title: "mailbox validation tiers used by ESP import tools", url: "" },
      ],
      implementationProblem: "Garbage email strings create Audit theater; over-blocking at Imports hides salvageable rows behind hard fails.",
      implementation: "On Imports, you can now mark email Syntax-invalid vs Channel-candidate after hygiene.  \nOn Book readiness / Verdict list, you can now escalate survivors into deliverability-class checks — Syntax-invalid never counts as reachable.",
      implementationAdds: ["syntax-invalid", "channel-candidate", "email-hygiene"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-07",
      consideration: "How should phone candidates be normalized for match keys and reachability?",
      thesisGap: "National-format phones without region default split identity and fail SMS channel class checks.",
      solution: "Parse phones with **libphonenumber-class rules + firm/default region**, store E.164 when parse succeeds and keep raw otherwise, so that match keys and phone validation-class share one parse outcome.",
      references: [
        { title: "Google libphonenumber parsing/formatting", url: "" },
        { title: "CRM import guides requiring country context for national numbers", url: "" },
      ],
      implementationProblem: "Phone match and SMS readiness diverge when one path uses raw strings and another assumes E.164.",
      implementation: "On Imports, you can now set Default region for phone parse and see E.164 when parse succeeds (raw retained on fail).  \nOn Book readiness, you can now run phone validation-class only from parse outcomes — not from unparsed free text as if it were E.164.",
      implementationAdds: ["e164", "default-region", "phone-parse-outcome"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-08",
      consideration: "How should messy name columns become identity-stable display names?",
      thesisGap: "Splitting every “Full Name” into given/family invents false structure; omitting display name blocks Audit.",
      solution: "Prefer a **display-name required** rule with optional structured parts when source provides them so that identity and Audit name-present pass without false name parsing.",
      references: [
        { title: "CRM merge display-name conventions", url: "" },
        { title: "entity-resolution name tokenization", url: "" },
        { title: "Seed Audit “name present” data-validity check", url: "" },
      ],
      implementationProblem: "Aggressive name parsing creates fake given/family fields that later look like Client Data facts.",
      implementation: "On Imports, you can now require Display name (from Full Name or composed parts) and treat Given/Family as optional only when source columns exist.  \nOn Book readiness / Audit run, you can now score name-present from Display name — not from invented splits.",
      implementationAdds: ["display-name", "optional-name-parts"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-09",
      consideration: "When auto-suggest Field→Q-ID / canonical maps are ambiguous, what practice gate applies?",
      thesisGap: "Fully automatic mapping will mis-attach immigration-relevant columns; fully manual mapping blocks assisted land.",
      solution: "Allow auto-suggest only at **high confidence**; require **human confirm on medium/low** before map version saves so that wrong Q-ID attach cannot silently poison Evaluation packs.",
      references: [
        { title: "CRM import “review mapping” steps", url: "" },
        { title: "human-in-the-loop entity resolution review queues", url: "" },
      ],
      implementationProblem: "Partial mapping exists as a shallow flow; confidence gating is the missing practice control.",
      implementation: "On Imports, you can now see Map confidence per column (high / medium / low) and must Confirm map on any medium/low before save.  \nOn Authorize book completion path into Imports, you can now block Field→Q-ID map version publish until confirms land — high-confidence autos may prefill only.",
      implementationAdds: ["map-confidence", "confirm-map", "high", "medium", "low"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-10",
      consideration: "What deterministic identity key policy should ingestion own (deepening crm-13 without re-arguing auth)?",
      thesisGap: "Dedup/identity is **new**; seat 7 named keys for connect, but ingestion must implement upsert and cluster behavior.",
      solution: "Resolve V1 identity by **normalized email → E.164 phone → source-row id**, emitting a stable Tower contact id on first land so that re-import upserts and Audit dedupe share one spine.",
      references: [
        { title: "HubSpot email-as-unique-on-import", url: "" },
        { title: "Apollo matching-key guidance", url: "" },
        { title: "entity-resolution practice: deterministic keys before probabilistic scores", url: "" },
      ],
      implementationProblem: "Without an ingestion-owned key spine, Book readiness dedupe and Imports upsert invent competing identities.",
      implementation: "On Imports, you can now upsert by match-key policy (normalized email → E.164 phone → source-row id) into a stable Contact id.  \nOn Book readiness / Verdict list, you can now show Duplicate of {Contact} using that same spine — not a second key invent.",
      implementationAdds: ["match-email", "match-phone", "match-source-row-id", "contact-id"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-11",
      consideration: "When deterministic keys are absent or collide oddly, what secondary resolution is in scope for V1?",
      thesisGap: "Name+phone-last-four fuzzy merges can false-merge distinct people in immigration books (family sharing phones).",
      solution: "Treat name/phone/email **scored suggestions as review clusters only** in V1 — never auto-merge without deterministic key or explicit firm confirm — so that false merges cannot collapse distinct engagement histories.",
      references: [
        { title: "Fellegi–Sunter record linkage", url: "" },
        { title: "Open Source entity-resolution frameworks (e.g. Zingg, Splink) practice of scored match clusters", url: "" },
        { title: "CRM fuzzy-match merge suggestions", url: "" },
      ],
      implementationProblem: "Auto fuzzy-merge looks smart and destroys reachability history; identity **new** surface must default fail-closed.",
      implementation: "On Imports / Contacts, you can now open Possible same-person clusters (scored) for Confirm merge or Keep separate — never auto-merge on score alone.  \nOn Book readiness, you can now list Unresolved identity clusters as data-validity outcomes without blocking unrelated reachable rows.",
      implementationAdds: ["possible-same-person", "confirm-merge", "keep-separate", "unresolved-identity"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-12",
      consideration: "When two rows are the same person, which values survive?",
      thesisGap: "Last-write-wins on re-import can erase good channels or firm suppression flags.",
      solution: "Apply explicit **survivorship rules** (non-empty channel beats empty; suppression flags OR across sources; newest provenance wins ties on conflicting scalars; opaque attributes union) so that merge improves reachability without wiping firm “do not contact.”",
      references: [
        { title: "Salesforce duplicate / merge survivorship rules", url: "" },
        { title: "HubSpot merge record practice", url: "" },
        { title: "MDM survivorship policies", url: "" },
      ],
      implementationProblem: "Naive overwrite on dedupe silently drops phones/emails and suppressions that Book readiness needs.",
      implementation: "On Contacts, when Confirm merge runs, you can now preview Survivorship (channels kept if non-empty, suppressions OR-merged, conflicting scalars by newest provenance, opaque attributes unioned).  \nOn Imports re-land, you can now upsert under the same survivorship — last-write-wins is not the default for channels or suppressions.",
      implementationAdds: ["survivorship", "suppression-or-merge", "merge-preview"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-13",
      consideration: "How should identity resolution treat contacts already living in the tenancy from prior lands?",
      thesisGap: "Each CSV treated as greenfield creates duplicates and splits Engagement record history.",
      solution: "On every batch, **match against the living tenancy book first** and classify rows Insert / Upsert / Duplicate-review so that “already-in-Tower” is an ingestion outcome, not only an Audit surprise.",
      references: [
        { title: "Seed §5.2 “already-in-Tower” Audit check", url: "" },
        { title: "CRM upsert-vs-insert import modes", url: "" },
        { title: "dbt unique-key incremental models", url: "" },
      ],
      implementationProblem: "Book readiness discovers already-in-Tower too late if Imports never consulted the living directory.",
      implementation: "On Imports, you can now see row class Insert / Upsert / Duplicate-review against the living Contacts book before commit.  \nOn Book readiness / Verdict list, you can now confirm already-in-Tower only as a residual check — primary resolution happened at land.",
      implementationAdds: ["insert", "upsert", "duplicate-review", "already-in-tower"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-14",
      consideration: "What happens when upsert brings a different email/phone for an existing Contact id?",
      thesisGap: "Silent overwrite vs silent ignore both break sequence routing and identity trust.",
      solution: "Quarantine conflicting channel changes as **Channel conflict** on the contact pending firm/operator accept or reject so that engagement never silently switches destination mid-sequence without an intentional accept.",
      references: [
        { title: "Apollo field-ownership matrices", url: "" },
        { title: "MDM “trust source” tiers", url: "" },
      ],
      implementationProblem: "Identity resolution without conflict handling either forks people or hijacks the reachable channel.",
      implementation: "On Imports, when upsert channel differs from living Contact, you can now open Channel conflict (accept new / keep existing / keep both as candidates).  \nOn Book readiness, you can now hold sequence-ready until conflict is resolved if the armed channel is the disputed one.",
      implementationAdds: ["channel-conflict", "accept-new", "keep-existing", "keep-both-candidates"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-15",
      consideration: "What email validation *classes* must Book readiness consume after ingestion lands candidates?",
      thesisGap: "Roster marks email validation-class **new**; seat 7 named the need — this seat specifies the class ladder for Audit.",
      solution: "Run email checks as a **class ladder** (syntax → domain/MX-class → mailbox/deliverability-class when wired) mapping to reachable / partial / unreachable so that CTO can attach vendors without changing verdict semantics.",
      references: [
        { title: "Seed §5.2 external validation systems", url: "" },
        { title: "crm-20 practice-need naming", url: "" },
      ],
      implementationProblem: "Raw channel candidates cannot become sequence-ready; without named classes, Audit invents binary valid/invalid theater.",
      implementation: "On Book readiness / Audit run, you can now execute Email validation-class ladder (syntax → domain/MX-class → mailbox/deliverability-class when wired).  \nOn Verdict list, you can now map class outcomes to reachable / partial / unreachable — Imports only supplied candidates.",
      implementationAdds: ["email-validation-class", "syntax", "domain-mx-class", "mailbox-deliverability-class", "reachable", "partial", "unreachable"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-16",
      consideration: "What phone validation classes belong beside email before SMS/voice channel use?",
      thesisGap: "E.164 parse success ≠ SMS-reachable; class checks are **new** and must not be collapsed into “has digits.”",
      solution: "Classify phones as **parse-valid → line-type-class → reachability-class (when wired)** so that channel match to intended sequence can refuse fixed-line-only rows for SMS sequences without claiming eligibility knowledge.",
      references: [
        { title: "libphonenumber number types", url: "" },
        { title: "Twilio Lookup-class practice", url: "" },
        { title: "Seed §5.2 phone valid", url: "" },
      ],
      implementationProblem: "Book readiness channel-match needs phone classes; ingestion currently stops at string presence.",
      implementation: "On Book readiness / Audit run, you can now run Phone validation-class (parse-valid → line-type-class → reachability-class when wired).  \nOn Verdict list, you can now mark SMS-intended rows Partial/Unreachable when line-type-class is incompatible — Contacts still retain the number as a channel candidate.",
      implementationAdds: ["phone-validation-class", "parse-valid", "line-type-class", "reachability-class"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-17",
      consideration: "What invariants must ingestion guarantee so Book readiness is not theater?",
      thesisGap: "Authorization can succeed while normalized/deduped/mapped records are unfinished — Audit then grades garbage.",
      solution: "Gate Audit-eligible batches on **normalized canonical rows + match keys applied + map version saved + validation-class pending|runnable** so that Book readiness starts from ingestion-complete, not file-received.",
      references: [
        { title: "Seed §5.2 / operator-book-readiness", url: "" },
        { title: "dbt “data tests before expose”", url: "" },
        { title: "crm-19 connect→Audit handoff", url: "" },
      ],
      implementationProblem: "Authorize book land trigger without ingestion-complete lets Audits run on raw CSV.",
      implementation: "On Imports, you can now mark Ingestion-complete only when normalized canonical rows, match keys, and saved Field→Q-ID map version all hold.  \nOn Book readiness, you can now start Audit run only from Ingestion-complete batches — validation-class checks then run or stay explicitly pending.",
      implementationAdds: ["ingestion-complete", "validation-class-pending"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-18",
      consideration: "Should one bad row fail the whole land batch?",
      thesisGap: "All-or-nothing import either blocks activation on typos or silently lands unusable rows as Contacts.",
      solution: "**Quarantine failing rows** with reason codes while committing valid rows (batch succeeds if ≥1 Audit-eligible row lands) so that messy books remain workable without pretending quarantine rows are sequence-ready.",
      references: [
        { title: "Great Expectations / dbt warn-vs-error test configs", url: "" },
        { title: "CRM import error-row CSVs", url: "" },
        { title: "data-quality quarantine tables pattern", url: "" },
      ],
      implementationProblem: "Messy firm CSVs always contain bad rows; product needs quarantine, not false activation failure or silent poison.",
      implementation: "On Imports, you can now commit Valid rows and send failures to Quarantine with reason codes (syntax, missing channel, map reject).  \nOn Book readiness, you can now exclude Quarantine from reachable counts; Contacts directory does not treat quarantine as enrolled.",
      implementationAdds: ["quarantine", "reason-codes", "valid-rows"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-19",
      consideration: "How does ingestion preserve firm suppressions for Audit without judging CASL?",
      thesisGap: "Normalization that “cleans away” odd columns can strip the only firm halt signal before Book readiness.",
      solution: "Map known suppression-shaped columns into **opaque suppression flags** (and keep unknowns opaque) so that Audit data-validity can halt outreach without this seat asserting consent lawfulness.",
      references: [
        { title: "CRM marketing-status / do-not-contact fields", url: "" },
        { title: "Seed Assump. 18 / crm-21 adjacency", url: "" },
        { title: "suppression-list practice in messaging stacks", url: "" },
      ],
      implementationProblem: "Staging transforms that drop unknown booleans erase do-not-contact; over-interpreting them invents CASL.",
      implementation: "On Imports, you can now map suppression-shaped columns to Opaque suppression flags (pass-through) during Canonical contact normalize.  \nOn Book readiness / Audit run, you can now consume those flags as data-validity inputs — Authorize book / Imports never show CASL-lawful claims.",
      implementationAdds: ["opaque-suppression-flags", "pass-through"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-20",
      consideration: "What lineage must stick to a Q-ID value after land?",
      thesisGap: "Evaluation packs may treat imported Q-ID values as client-asserted truth without source attribution.",
      solution: "Attach **source system, batch id, map version, raw value, transform id, landed-at** to every mapped Q-ID fact so that desk and packs can distrust or refresh imported facts deliberately.",
      references: [
        { title: "MDM source-system attribution", url: "" },
      ],
      implementationProblem: "Mapped Q-IDs without lineage look like Client Data answers and poison eligibility confidence.",
      implementation: "On Imports, you can now store Fact provenance (source, batch id, map version, raw → transform → value, landed-at) per Q-ID attach.  \nOn Contacts / Client row, you can now read Imported fact vs Client-asserted fact — Book readiness does not upgrade provenance class.",
      implementationAdds: ["fact-provenance", "imported-fact", "client-asserted", "batch-id", "map-version"],
      implementationPlant: "not_done",
      status: "verified",
    },
    {
      id: "ingest-21",
      consideration: "After V1 one-way land, what would incremental sync require that must stay out of the activation finish line?",
      thesisGap: "Building incremental sync before ingestion-complete + identity spine exists repeats seat-7’s “sync perfection blocks running” failure mode.",
      solution: "**Defer incremental sync for V1.** Document the future need for change-detection (watermark / updated-at / hash of canonical row) that upserts by Contact id — and keep Activation state free of any sync-complete gate — so that campaign-real stays auth ∩ escrow, not CDC maturity.",
      references: [
        { title: "Seed KU #7", url: "" },
        { title: "SplitForge API-for-ongoing vs CSV migrate", url: "" },
        { title: "dbt incremental models / changed-data-capture practice", url: "" },
        { title: "Apollo sync cadence guidance", url: "" },
      ],
      implementationProblem: "If Incremental sync appears as a V1 Imports control, firms and operators will treat sync lag as activation failure.",
      implementation: "On Imports / Authorize book, you can now read Sync posture: one-way land only; Incremental sync deferred (KU #7) — not offered as a V1 control.  \nOn Activation state, you can now reach running without sync-complete; currency uses re-hand-over upsert (seat 7) until CDC ships.",
      implementationAdds: ["incremental-sync-deferred", "one-way-land", "sync-complete-not-required"],
      implementationPlant: "not_done",
      status: "partial",
    },
    {
      id: "ingest-22",
      consideration: "How should continuous pull from vertical RCIC/practice platforms be treated relative to ingestion capability?",
      thesisGap: "Capability residual names incremental sync + vertical OAuth together; shipping OAuth pull without mapping/identity/validation classes recreates thin import at higher privilege.",
      solution: "**Defer vertical CRM OAuth continuous pull** until Field→Q-ID maps, identity spine, and validation-class ladder exist for export-mediated land; when revisited, OAuth pull must reuse the same ingestion pipeline (inventory → normalize → map → match → quarantine → Audit) so that live pull never bypasses identity resolution.",
      references: [
        { title: "Seed KU #7", url: "" },
        { title: "crm-05 / crm-17 adjacency (vertical OAuth immature", url: "" },
        { title: "vendor integration maturity notes (", url: "" },
      ],
      implementationProblem: "A privileged OAuth pull that skips Imports normalization would mint a second, dirtier book path.",
      implementation: "On Authorize book / Imports, you can now treat any future live-crm pull as feeding the same Ingestion pipeline (header inventory → normalize → Field→Q-ID map → match → quarantine) — and in V1 you can now see Vertical continuous pull deferred (KU #7).  \nOn Book readiness, you can now keep one Audit contract for file and future pull — no pull-specific bypass.",
      implementationAdds: ["vertical-oauth-pull-deferred", "same-ingestion-pipeline"],
      implementationPlant: "not_done",
      status: "partial",
    },
  ],
};
