import type { CantItem, FurnishItem } from "./types";

/** Auto-generated by scripts/sync-wiring-paper-from-docs.py - do not edit by hand. */
export const CANT_ITEMS: CantItem[] = [
{
    id: "cant-01",
    title: "webhook replay double-counts outcomes",
    failureMode: "ESP, SMS, payment, or internal retry deliveries are processed twice, inflating rates, suppressions, held balances, or release completions.",
    whereItHangs: ["`provider_webhook_receipt` -> `messaging_event_stream`", "`payment_webhook_receipt` -> `escrow_ledger_entry`", "`release_attempt` -> `payment_webhook_receipt`", "`metrics_materialization`"],
    guardToAdd: "Stable receipt idempotency keys, deterministic fallback keys, duplicate-suppressed receipts, and replay audit entries before any stream append, ledger mutation, or rollup update.",
    seats: ["C1", "C2", "C6", "C7"],
  },
{
    id: "cant-02",
    title: "warmup stalls or premature volume opens",
    failureMode: "A domain/IP sits in stalled warmup without operator visibility, or Activation treats authenticated identity as permission for full-book send volume.",
    whereItHangs: ["`sending_domain` -> `warmup_schedule`", "`warmup_schedule` -> `send_gate_decision`", "`provider_webhook_receipt` -> `warmup_schedule`", "`firm_tenancy` -> `engagement_attempt`"],
    guardToAdd: "Warmup stage SLO, consumed-cap reconciliation, provider-signal advancement rules, and hard cap enforcement before sequence enrollment or provider accept.",
    seats: ["C1", "C2", "C7"],
  },
{
    id: "cant-03",
    title: "stale reference tables used mid-sequence",
    failureMode: "A sequence continues acting on a score snapshot whose reference version is past freshness or superseded by a current publish.",
    whereItHangs: ["`reference_freshness_state` -> `evaluation_pack`", "`reference_table_version` -> `re_score_job`", "`client_score_snapshot` -> `send_gate_decision`", "`sequence_instance`"],
    guardToAdd: "Freshness-gated evaluation read, version-pin display on score snapshots, and gate recheck when the pack requires published-current freshness.",
    seats: ["C2", "C3", "C7"],
  },
{
    id: "cant-04",
    title: "release on unverified booking",
    failureMode: "A booked signal opens payment release without attribution, measurement-window stability, or evidence-package validation.",
    whereItHangs: ["`outcome_verification_event` -> `attribution_path`", "`attribution_path` -> `measurement_window`", "`measurement_window` -> `release_evidence_package`", "`release_evidence_package` -> `release_attempt`"],
    guardToAdd: "Require a validated `release_evidence_package` containing outcome, attribution, window, frozen terms, and instrument refs before any provider transfer request.",
    seats: ["C2", "C6", "C7"],
  },
{
    id: "cant-05",
    title: "reputation bleed crosses tenants",
    failureMode: "Bounce, complaint, quarantine, or throttle state from one firm affects peer firms on a different reputation unit, or hides a global toxic-address block as a firm-local fact.",
    whereItHangs: ["`firm_tenancy` -> `reputation_unit`", "`outbound_message` -> `reputation_unit`", "`reputation_unit` -> `send_gate_decision`", "`suppression_list` -> `audit_trail`"],
    guardToAdd: "Reputation unit keys must include firm/domain/IP tier scope; peer preservation must be auditable; global suppression reasons must be privacy-preserving.",
    seats: ["C1", "C4", "C7"],
  },
{
    id: "cant-06",
    title: "provider outage creates unsafe motion",
    failureMode: "Email, SMS, enrichment, reference, analytics, or payment provider downtime is interpreted as success, leaving sends, hydrates, re-scores, or releases to proceed on stale or missing external state.",
    whereItHangs: ["`provider_throttle_state`", "`provider_webhook_receipt`", "`payment_webhook_receipt`", "`crawl_budget`", "`reference_detect_cursor`", "`pipeline_health`", "`release_attempt`"],
    guardToAdd: "Outage state closes dependent gates, records provider-class health, distinguishes unavailable from negative outcomes, and requires recovery replay before reopening motion.",
    seats: ["C1", "C3", "C5", "C6", "C7"],
  },
{
    id: "cant-07",
    title: "suppression race with in-flight send",
    failureMode: "Opt-out, STOP, hard bounce, complaint, or pass-through suppression lands while a scheduled attempt is already queued, and the provider-bound send still leaves.",
    whereItHangs: ["`consent_record` -> `suppression_list`", "`provider_webhook_receipt` -> `messaging_event_stream` -> `suppression_list`", "`suppression_list` -> `send_gate_decision`", "`attempt_job_queue` -> `outbound_message`"],
    guardToAdd: "Race-boundary cancellation on suppression writes, mandatory final suppression read immediately before provider accept, and nullified attempt chronology.",
    seats: ["C1", "C2", "C4", "C7"],
  },
{
    id: "cant-08",
    title: "identity false merge inherits wrong channels",
    failureMode: "Weak identity hints or concurrent imports merge two people, carrying suppressions, reachable channels, or Q-ID facts across contacts.",
    whereItHangs: ["`contact_identity` -> `identity_review_cluster`", "`identity_review_cluster` -> `merge_survivorship_policy`", "`merge_survivorship_policy` -> `suppression_list`", "`book_readiness`"],
    guardToAdd: "Probabilistic hints create review clusters only; deterministic conflicts hold readiness; confirmed merges use explicit survivorship with suppression OR and provenance.",
    seats: ["C1", "C4"],
  },
{
    id: "cant-09",
    title: "crawl robots or rate abuse",
    failureMode: "Forward-deploy enrichment fetches disallowed paths, ignores robots directives, or hammers firm origins and blocks the customer's public site.",
    whereItHangs: ["`enrichment_root` -> `crawl_permission`", "`crawl_budget` -> `public_fetch_cache`", "`crawl_budget` -> `enrichment_fact`", "`audit_trail`"],
    guardToAdd: "Fail-closed robots state, declared crawler identity, per-host rate and backoff, cache reuse inside TTL, and source-visible audit receipts.",
    seats: ["C5"],
  },
{
    id: "cant-10",
    title: "late or out-of-order delivery events corrupt timelines",
    failureMode: "Provider delivery, reply, or booking events arrive after later runtime actions and overwrite current state or produce impossible funnel/order metrics.",
    whereItHangs: ["`messaging_event_stream`", "`provider_webhook_receipt` -> `correlation_envelope`", "`metrics_materialization`", "`pipeline_health`", "`engagement_record`"],
    guardToAdd: "Store event time separately from processing time, apply watermarks, recompute within policy, audit late drops/corrections, and present chronology by occurrence time.",
    seats: ["C1", "C2", "C7"],
  },
{
    id: "cant-11",
    title: "escrow dispute freeze ignored",
    failureMode: "Buyer dispute, chargeback, or provider dispute arrives while a release job is pending, but the release attempt still executes money motion.",
    whereItHangs: ["`payment_webhook_receipt` -> `dispute_freeze`", "`dispute_freeze` -> `release_attempt`", "`release_attempt` -> `provider_payment_identity`", "`escrow_ledger_entry`"],
    guardToAdd: "Active freeze cancels pending release jobs, forces disputed ledger status, and requires evidence/window re-evaluation after clearance before any provider request.",
    seats: ["C6"],
  },
{
    id: "cant-12",
    title: "metric definition drifts from shell meaning",
    failureMode: "A shell tile keeps its label while the formula, taxonomy inputs, window, or primary/diagnostic role changes underneath it.",
    whereItHangs: ["`metric_definition` -> `metrics_materialization`", "`event_taxonomy` -> `metric_definition`", "`funnel_definition`", "`audit_trail`"],
    guardToAdd: "Version every metric and funnel definition, bind materialized values to definition versions, and keep historical shell values on the prior formula.",
    seats: ["C7"],
  },
{
    id: "cant-13",
    title: "intent misclassification acts without HITL",
    failureMode: "Low-confidence, policy-edge, hostile, or ambiguous replies are classified as safe next actions and continue automated sends.",
    whereItHangs: ["`conversation_thread` -> `intent_classification`", "`intent_classification` -> `engagement_attempt`", "`intent_classification` -> `escalation_ticket`", "`runtime_tool_invocation`"],
    guardToAdd: "Confidence floor with `unsure`, automatic pause, escalation ticket creation, and human disposition before resume, reclassify, close, or keep suppression.",
    seats: ["C1", "C2"],
  },
{
    id: "cant-14",
    title: "token redeem replay opens workspace repeatedly",
    failureMode: "A no-login workspace token is reused from browser history, referers, or forwarded links after the intended redeem.",
    whereItHangs: ["`access_token` -> `redeem_session`", "`token_policy`", "`prepared_workspace`", "`audit_trail`"],
    guardToAdd: "One-time redeem into short session, hash-only verifier storage, purpose and TTL enforcement, raw secret stripped from navigation, and replay-deny audit.",
    seats: ["C5"],
  },
{
    id: "cant-15",
    title: "counsel-blocked MT rail accidentally armed",
    failureMode: "Engineering rail configuration or provider account setup is mistaken for legal clearance, enabling production MT/MSB posture or client-fund intake.",
    whereItHangs: ["`counsel_gate` -> `firm_tenancy`", "`provider_payment_identity`", "`escrow_ledger_entry`", "`audit_trail`"],
    guardToAdd: "Production rail activation requires explicit `counsel_gate` clearance; pending counsel blocks MT/MSB posture and rejects client-fund intake paths.",
    seats: ["C6"],
  },
{
    id: "cant-16",
    title: "reference schema drift publishes bad current",
    failureMode: "A public reference source changes shape, parse still succeeds partially, and bad rows become published-current.",
    whereItHangs: ["`reference_source_binding` -> `ingestion_run`", "`ingestion_run` -> `schema_contract`", "`schema_contract` -> `reference_table_version`", "`audit_trail`"],
    guardToAdd: "Versioned shape and sanity contracts, draft-side failure on validation mismatch, required provenance, and no publish advancement until dual-check passes.",
    seats: ["C3"],
  },
{
    id: "cant-17",
    title: "dependent reference tables publish half a change",
    failureMode: "Co-dependent table families move at different times, leaving evaluation packs to see a mixed ministerial state.",
    whereItHangs: ["`publish_group` -> `reference_table_version`", "`reference_table_version` -> `evaluation_pack`", "`reference_diff`"],
    guardToAdd: "Atomic publish groups for dependent families, all-member readiness, group lineage in audit, and pins that never point to partial groups.",
    seats: ["C3"],
  },
{
    id: "cant-18",
    title: "re-score collision loses provenance",
    failureMode: "Fact-write-back and reference-publish triggers collide, creating duplicate signals or one result that hides one trigger cause.",
    whereItHangs: ["`client_data_record` -> `re_score_job`", "`reference_table_version` -> `re_score_job`", "`re_score_job` -> `client_score_snapshot`", "`audit_trail`"],
    guardToAdd: "Contact plus trigger-event idempotency keys, merge-window coalescing, multi-cause result write, and all-cause audit append.",
    seats: ["C3", "C4"],
  },
{
    id: "cant-19",
    title: "mapping confidence silently mutates facts",
    failureMode: "Medium/low-confidence field maps or changed Q-ID mappings publish into readiness without human confirmation or map version lineage.",
    whereItHangs: ["`source_header_inventory` -> `field_mapping`", "`field_mapping` -> `import_batch`", "`field_mapping` -> `fact_lineage`", "`book_readiness`"],
    guardToAdd: "Confirm-required state below confidence floor, batch-bound map versions, immutable lineage on promoted facts, and blocked readiness until mapping is published.",
    seats: ["C4"],
  },
{
    id: "cant-20",
    title: "channel conflict overwrites armed outreach channel",
    failureMode: "A new import changes the email or phone on an existing contact and silently replaces the active channel used by pending attempts.",
    whereItHangs: ["`contact_identity` -> `channel_conflict`", "`channel_conflict` -> `book_readiness`", "`merge_survivorship_policy`", "`engagement_attempt`"],
    guardToAdd: "Open channel conflicts hold sequence-ready for affected rows; accept/reject/keep-both decisions are explicit and audit-stamped before channel promotion.",
    seats: ["C2", "C4"],
  },
{
    id: "cant-21",
    title: "auto-reply finishes a human thread",
    failureMode: "OOO or machine replies get treated as human intent, closing sequences or triggering next-owner decisions incorrectly.",
    whereItHangs: ["`provider_webhook_receipt` -> `messaging_event_stream`", "`messaging_event_stream` -> `intent_classification`", "`auto_reply_pause`", "`sequence_finish_state`"],
    guardToAdd: "Machine-reply class writes `auto_reply_pause`, holds current owner, and requires resume-window gate recheck rather than finish predicates.",
    seats: ["C2", "C7"],
  },
{
    id: "cant-22",
    title: "live inbound fails to nullify scheduled motion",
    failureMode: "A live reply or form visit happens during the retry window, but an already armed send job still fires.",
    whereItHangs: ["`messaging_event_stream` -> `engagement_attempt`", "`form_activity_event` -> `engagement_attempt`", "`engagement_attempt` -> `attempt_job_queue`", "`engagement_record`"],
    guardToAdd: "Race-boundary nullification on live inbound/form evidence, scheduled-send cancellation before policy continuation, and idempotent nullify chronology.",
    seats: ["C2", "C7"],
  },
{
    id: "cant-23",
    title: "runtime tool bypasses send gates",
    failureMode: "Agentic decide-to-act selects a send-like or state-changing tool that creates an outbound message without consent, suppression, posture, or deliverability checks.",
    whereItHangs: ["`intent_classification` -> `runtime_tool_invocation`", "`runtime_tool_invocation` -> `send_gate_decision`", "`send_gate_decision` -> `outbound_message`", "`audit_trail`"],
    guardToAdd: "Closed tool set with preconditions, mandatory gate re-entry for send-like actions, no `outbound_message` creation on deny, and tool audit state.",
    seats: ["C1", "C2"],
  },
{
    id: "cant-24",
    title: "public enrichment stores private or sensitive content",
    failureMode: "Pre-connect crawl captures authenticated, person-sensitive, or non-public page content and hydrates it into Prepared Workspace.",
    whereItHangs: ["`enrichment_fact` -> `public_fact_policy`", "`public_fact_policy` -> `prepared_workspace`", "`source_provenance`", "`audit_trail`"],
    guardToAdd: "Versioned public-fact allowlist, reject-without-retain for private classes, provenance on accepted facts, and scope enforcement during hydrate.",
    seats: ["C5"],
  },
{
    id: "cant-25",
    title: "stale facts or brand package presented as ready",
    failureMode: "A Prepared Workspace is marked ready even though facts are stale, branding is unconfirmed, or hydration used outdated template/fact bindings.",
    whereItHangs: ["`fact_freshness` -> `hydrate_job`", "`brand_package` -> `prepared_workspace`", "`workspace_readiness`", "`engagement_template_version`"],
    guardToAdd: "Readiness gate must require current fact TTL, accepted or explicit fallback branding, pinned template/fact/brand identifiers, and regenerate lineage.",
    seats: ["C5", "C7"],
  },
{
    id: "cant-26",
    title: "required merge fields pass as blanks",
    failureMode: "Hydration silently treats missing required facts as empty copy, producing a workspace that looks complete but is factually hollow.",
    whereItHangs: ["`hydrate_job` -> `merge_field_contract`", "`merge_field_contract` -> `prepared_workspace`", "`workspace_readiness`", "`audit_trail`"],
    guardToAdd: "Merge-field contract fails closed, records missing key names, keeps ready closed, and prevents scoped blocks from rendering as successful content.",
    seats: ["C5"],
  },
{
    id: "cant-27",
    title: "high-cardinality metrics explode cost or expose identity",
    failureMode: "Contact ids, email addresses, message bodies, or raw attempt identifiers become metric labels, creating expensive series and privacy leakage.",
    whereItHangs: ["`metric_definition` -> `metrics_materialization`", "`messaging_event_stream`", "`pipeline_health`"],
    guardToAdd: "Bounded metric label contract, validation failure before materialization, and high-cardinality detail retained only as stream drill-through ids.",
    seats: ["C7"],
  },
{
    id: "cant-28",
    title: "missing producers leave empty shells",
    failureMode: "A required Sending, Conversations, Booking, Capture, or Prepared Workspace producer never emits, and empty metrics are read as healthy zeroes.",
    whereItHangs: ["`event_taxonomy` -> `producer_contract`", "`producer_contract` -> `messaging_event_stream`", "`pipeline_health`", "`metrics_materialization`"],
    guardToAdd: "Producer coverage by taxonomy owner, required emit-on-path contracts, missing/stale producer incidents, and shell fill marked telemetry-unhealthy.",
    seats: ["C1", "C2", "C5", "C7"],
  },
{
    id: "cant-29",
    title: "SMS eligibility inherits email readiness",
    failureMode: "Email warmup, DKIM, or domain readiness is treated as SMS permission, allowing sends without A2P, carrier throughput, STOP, or phone reachability clearance.",
    whereItHangs: ["`sms_sender_registration`", "`carrier_throughput_tier`", "`sms_reputation_unit`", "`warmup_schedule` -> `send_gate_decision`", "`reachability_class`"],
    guardToAdd: "Separate SMS-ready conjunction for registration, throughput, phone validation, STOP/suppression, and carrier reputation; email warmup is non-authorizing.",
    seats: ["C1", "C2", "C4"],
  },
{
    id: "cant-30",
    title: "payment webhook signature failure mutates money state",
    failureMode: "Spoofed or unverifiable payment callbacks write holds, disputes, refunds, or release completions.",
    whereItHangs: ["`payment_webhook_receipt` -> `provider_payment_identity`", "`payment_webhook_receipt` -> `held_balance`", "`payment_webhook_receipt` -> `dispute_freeze`", "`audit_trail`"],
    guardToAdd: "Signed ingress verification, tenant correlation, idempotency key match, and failure DLQ before provider refs, balances, or ledger status can change.",
    seats: ["C6"],
  },
{
    id: "cant-31",
    title: "cap or collar bypass on release/refund",
    failureMode: "Release, return, or forfeit action moves more money than held principal, remaining cap, or frozen terms allow.",
    whereItHangs: ["`held_balance` -> `release_attempt`", "`escrow_ledger_entry` -> `release_evidence_package`", "`release_attempt` -> `provider_payment_identity`"],
    guardToAdd: "Amount comparison against held principal and remaining releasable cap before provider request, cap-block state, and no provider ref creation on block.",
    seats: ["C6"],
  },
{
    id: "cant-32",
    title: "tenant isolation failure in analytics or money projections",
    failureMode: "Firm health, Fleet drill-down, escrow status, or stream ids expose another firm's events, balances, or reputation unit facts.",
    whereItHangs: ["`firm_tenancy` -> `messaging_event_stream`", "`firm_tenancy` -> `metrics_materialization`", "`firm_tenancy` -> `escrow_status_read_model`", "`audit_trail`"],
    guardToAdd: "Tenant key required on ingest and materialization, deliberate fleet-rollup scope only, peer-firm drill denial, and rejected access audit.",
    seats: ["C1", "C6", "C7"],
  },
{
    id: "cant-33",
    title: "provider throttle reopens too fast",
    failureMode: "Deferral pressure clears for one mailbox provider or reputation unit, and the system restores full capacity across unrelated units.",
    whereItHangs: ["`provider_webhook_receipt` -> `provider_throttle_state`", "`provider_throttle_state` -> `send_gate_decision`", "`warmup_schedule`", "`reputation_unit`"],
    guardToAdd: "Provider/unit-scoped backoff, gradual capacity reopen, independent warmup cap respect, and audit of throttle relaxation cause.",
    seats: ["C1", "C7"],
  },
{
    id: "cant-34",
    title: "cancel/reopen event arrives after release",
    failureMode: "Meeting canceled or reopened event arrives after measurement-window close or provider release attempt, leaving release state inconsistent.",
    whereItHangs: ["`outcome_verification_event` -> `measurement_window`", "`measurement_window` -> `release_evidence_package`", "`release_attempt`", "`escrow_status_read_model`"],
    guardToAdd: "Event-time window policy, late cancel/reopen audit, release-attempt hold until evidence finality, and explicit exception/dispute state after terminal money motion.",
    seats: ["C2", "C6", "C7"],
  },
{
    id: "cant-35",
    title: "proxy campaign metrics treated as truth",
    failureMode: "Ad-platform aggregates drive success/kill criteria without first-party capture, workspace, or booking path events.",
    whereItHangs: ["`campaign_proxy_aggregate` -> `metrics_materialization`", "`producer_contract` -> `messaging_event_stream`", "`funnel_definition`", "`pipeline_health`"],
    guardToAdd: "First-party join required for scoreable variants, proxy-only metrics diagnostic, missing campaign/creative keys marked unscoreable.",
    seats: ["C7"],
  },
{
    id: "cant-36",
    title: "workspace token authorizes later book motion",
    failureMode: "A Prepared Workspace preview token is reused to authorize touchpoint continuation, book connection, or sequence motion.",
    whereItHangs: ["`access_token` -> `token_policy`", "`prepared_workspace`", "`sequence_enrollment`", "`audit_trail`"],
    guardToAdd: "Purpose-scoped tokens, distinct touchpoint tokens, preview-only authority after workspace redeem, and deny events when token purpose exceeds scope.",
    seats: ["C2", "C5"],
  }
];

export const FURNISH_ITEMS: FurnishItem[] = [
{
    id: "furnish-01",
    title: "webhook dead-letter and replay desk",
    opsNode: "webhook_dead_letter_replay",
    purpose: "DLQ + replay for signed provider ingress, normalization failures, duplicate/replay review, and poison payload isolation.",
    attachesToFunctionNodes: ["provider_webhook_receipt", "payment_webhook_receipt", "messaging_event_stream", "pipeline_health", "audit_trail"],
    doesNotChangeFunction: "It observes and replays failed ingress through the same receipt gates; it adds no new domain transition.",
  },
{
    id: "furnish-02",
    title: "re-score job health",
    opsNode: "re_score_job_health",
    purpose: "Job health for queue depth, coalescing collisions, retry age, stuck contacts, and publish/fact-write-back cause coverage.",
    attachesToFunctionNodes: ["re_score_job", "reference_table_version", "client_data_record", "client_score_snapshot", "audit_trail"],
    doesNotChangeFunction: "It reports and re-drives existing re-score work without changing evaluation rules or score state.",
  },
{
    id: "furnish-03",
    title: "hydrate job health",
    opsNode: "hydrate_job_health",
    purpose: "Job health for hydrate failures, missing merge keys, stale fact inputs, brand-package waits, and regenerate backlog.",
    attachesToFunctionNodes: ["hydrate_job", "merge_field_contract", "fact_freshness", "brand_package", "prepared_workspace", "workspace_readiness"],
    doesNotChangeFunction: "It watches hydration readiness and retry posture without creating new workspace versions outside `hydrate_job`.",
  },
{
    id: "furnish-04",
    title: "warmup job health",
    opsNode: "warmup_health_glance",
    purpose: "Job health for stage stalls, consumed-vs-allowed cap drift, provider signal absence, and premature reopen attempts.",
    attachesToFunctionNodes: ["warmup_schedule", "provider_webhook_receipt", "provider_throttle_state", "send_gate_decision", "reputation_unit"],
    doesNotChangeFunction: "It surfaces warmup posture and anomalies without changing cap logic or send eligibility.",
  },
{
    id: "furnish-05",
    title: "release job health",
    opsNode: "release_job_health",
    purpose: "Job health for pending release, return, forfeit, dispute-canceled, idempotency-key reuse, and provider completion lag.",
    attachesToFunctionNodes: ["release_attempt", "release_evidence_package", "payment_webhook_receipt", "dispute_freeze", "escrow_ledger_entry", "held_balance"],
    doesNotChangeFunction: "It monitors existing release attempts and replay posture without adding release predicates or money states.",
  },
{
    id: "furnish-06",
    title: "reference reconciliation glance",
    opsNode: "reference_reconciliation_glance",
    purpose: "Reconciliation glance for canonical-source drift, sampled replay deltas, stale reference families, and missed verify cadence.",
    attachesToFunctionNodes: ["reconciliation_job", "reference_source_binding", "reference_table_version", "reference_freshness_state", "client_score_snapshot", "audit_trail"],
    doesNotChangeFunction: "It summarizes existing reconciliation and freshness state without publishing tables or changing scores.",
  },
{
    id: "furnish-07",
    title: "per-firm send cost glance",
    opsNode: "per_firm_send_cost_glance",
    purpose: "Per-firm cost / FinOps glance for email/SMS send volume, retries, provider throttle waste, dedicated IP posture, and warmup opportunity cost.",
    attachesToFunctionNodes: ["firm_tenancy", "outbound_message", "ip_pool_tier", "carrier_throughput_tier", "warmup_schedule", "metrics_materialization"],
    doesNotChangeFunction: "It reads existing send and provider-state facts for cost visibility; it does not alter gates, throttles, or sends.",
  },
{
    id: "furnish-08",
    title: "producer coverage glance",
    opsNode: "producer_coverage_glance",
    purpose: "Producer coverage for `messaging_event_stream` by taxonomy event, owner, emitting path, last seen time, and required shell readiness.",
    attachesToFunctionNodes: ["producer_contract", "event_taxonomy", "messaging_event_stream", "pipeline_health", "metrics_materialization"],
    doesNotChangeFunction: "It reports missing or stale emitters without inventing events or bypassing producer contracts.",
  },
{
    id: "furnish-09",
    title: "escrow reconciliation glance",
    opsNode: "escrow_reconciliation_glance",
    purpose: "Escrow reconciliation glance for ledger-vs-provider refs, held balance, release/refund/forfeit attempts, dispute freezes, and counsel gate visibility.",
    attachesToFunctionNodes: ["escrow_status_read_model", "escrow_ledger_entry", "held_balance", "provider_payment_identity", "payment_webhook_receipt", "counsel_gate"],
    doesNotChangeFunction: "It compares and displays existing escrow facts without changing ledger status or provider rails.",
  },
{
    id: "furnish-10",
    title: "crawl budget health",
    opsNode: "crawl_budget_health",
    purpose: "Crawl budget health for robots failures, host backoff, cache hit rate, origin errors, blocked paths, and enrichment freshness risk.",
    attachesToFunctionNodes: ["crawl_budget", "crawl_permission", "public_fetch_cache", "enrichment_root", "enrichment_fact", "fact_freshness"],
    doesNotChangeFunction: "It observes crawler posture and retry pressure without changing public-fact policy or hydrate readiness.",
  },
{
    id: "furnish-11",
    title: "pipeline freshness SLO glance",
    opsNode: "pipeline_freshness_slo_glance",
    purpose: "Pipeline freshness SLO glance for producer time, receipt time, stream append lag, materialization lag, late drops, DLQ depth, and stale shell scopes.",
    attachesToFunctionNodes: ["pipeline_health", "messaging_event_stream", "provider_webhook_receipt", "metrics_materialization", "metric_definition", "audit_trail"],
    doesNotChangeFunction: "It displays telemetry health from existing pipeline state without changing metric definitions or materialized values.",
  },
{
    id: "furnish-12",
    title: "identity review backlog glance",
    opsNode: "identity_review_backlog_glance",
    purpose: "Review health for ambiguous identity clusters, channel conflicts, false-merge risk, stuck readiness rows, and survivor-policy decisions.",
    attachesToFunctionNodes: ["identity_review_cluster", "channel_conflict", "contact_identity", "merge_survivorship_policy", "book_readiness", "audit_trail"],
    doesNotChangeFunction: "It surfaces review load and risk without merging, splitting, or promoting identity state.",
  }
];

export function getCantItem(cantId: string): CantItem | undefined {
  return CANT_ITEMS.find((item) => item.id === cantId);
}

export function getFurnishItem(furnishId: string): FurnishItem | undefined {
  return FURNISH_ITEMS.find((item) => item.id === furnishId);
}
