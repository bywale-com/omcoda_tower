import type { WiringSeatTraces } from "../types";

/** Auto-generated by scripts/sync-wiring-paper-from-docs.py - do not edit by hand. */
export const WIRING_FUNCTION_TRACES: WiringSeatTraces[] = [
{
    id: "c1-email-deliverability",
    label: "C1 Email / SMS deliverability",
    items: [
{
        id: "deliv-01",
        title: "SPF authorizes every ESP hop",
        implementationSource: "Sending infrastructure exposes SPF authorization for each domain and Send gates block Armed / Active volume until the ESP and return-path hop pass.",
        start: "Starting from sending_domain (new)",
        pathHops: ["When SPF authorization verification occurs at `sending_domain`, domain readiness predicate update occurs at `send_gate_decision`.", "When return-path SPF verification occurs at `sending_domain`, bounce-hop authorization update occurs at `return_path_domain`.", "When firm identity binding for volume occurs at `sending_identity_binding`, SPF-ready requirement attachment occurs at `send_gate_decision`.", "When CEM leave request occurs at `engagement_attempt`, provider-accept eligibility update occurs at `outbound_message`.", "When SPF missing or misaligned status occurs at `sending_domain`, domain-not-ready denial append occurs at `audit_trail`."],
        nodesTouched: ["sending_domain", "return_path_domain", "sending_identity_binding", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-02",
        title: "DKIM signs with aligned per-firm keys",
        implementationSource: "Sending infrastructure provisions per-firm branded DKIM keys and Send gates require aligned signing before Opt-in or Nudge messages leave.",
        start: "Starting from dkim_keyset (new)",
        pathHops: ["When per-firm DKIM key provisioning occurs at `dkim_keyset`, signing identity link occurs at `sending_domain`.", "When DKIM d= alignment verification occurs at `dkim_keyset`, authentication predicate update occurs at `send_gate_decision`.", "When firm sending identity selection occurs at `sending_identity_binding`, aligned-key requirement update occurs at `from_identity`.", "When Opt-in or Nudge leave request occurs at `engagement_attempt`, DKIM alignment stamp occurs at `outbound_message`.", "When aligned signing unavailable status occurs at `dkim_keyset`, CEM leave denial occurs at `send_gate_decision`.", "When CEM leave denial occurs at `send_gate_decision`, denial record append occurs at `audit_trail`."],
        nodesTouched: ["dkim_keyset", "sending_domain", "sending_identity_binding", "from_identity", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-03",
        title: "DMARC is mandatory on From domains",
        implementationSource: "Sending infrastructure shows DMARC record, policy, rua, and alignment status, and Activation stays closed until the bound identity is DMARC-ready.",
        start: "Starting from dmarc_policy (new)",
        pathHops: ["When DMARC discovery occurs at `dmarc_policy`, policy and rua state update occurs at `sending_domain`.", "When SPF or DKIM From-domain alignment evaluation occurs at `dmarc_policy`, aligned-domain predicate update occurs at `send_gate_decision`.", "When firm From identity binding occurs at `sending_identity_binding`, DMARC activation-gate attachment occurs at `firm_tenancy`.", "When Activation ready-to-send evaluation occurs at `firm_tenancy`, DMARC predicate read occurs at `send_gate_decision`.", "When DMARC missing or below-minimum status occurs at `dmarc_policy`, ready-to-send closure occurs at `send_gate_decision`.", "When ready-to-send closure occurs at `send_gate_decision`, denial record append occurs at `audit_trail`."],
        nodesTouched: ["dmarc_policy", "sending_domain", "sending_identity_binding", "firm_tenancy", "send_gate_decision", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-04",
        title: "Per-firm branded subdomains isolate tenants",
        implementationSource: "Sending infrastructure allocates a per-firm branded subdomain from the managed pool and Firm operations bind attaches it as that firm's sending identity.",
        start: "Starting from sending_domain (existing)",
        pathHops: ["When managed subdomain allocation occurs at `sending_domain`, tenant ownership update occurs at `firm_tenancy`.", "When allocated subdomain authentication occurs at `sending_domain`, auth-dependency update occurs at `send_gate_decision`.", "When firm subdomain choice occurs at `sending_identity_binding`, firm-scoped From update occurs at `from_identity`.", "When new-subdomain warmup initialization occurs at `warmup_schedule`, ramp and cap attachment occurs at `sending_domain`.", "When tenant-scoped send occurs at `outbound_message`, separate reputation accounting occurs at `reputation_unit`."],
        nodesTouched: ["sending_domain", "firm_tenancy", "send_gate_decision", "sending_identity_binding", "from_identity", "warmup_schedule", "outbound_message", "reputation_unit"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-05",
        title: "Reputation blast radius is isolated",
        implementationSource: "Sending infrastructure exposes reputation units by subdomain and IP tier, quarantines units on bounce or complaint threshold trips, and gates only the affected firm.",
        start: "Starting from reputation_unit (new)",
        pathHops: ["When per-firm subdomain and IP-tier pairing occurs at `reputation_unit`, domain and tier binding occurs at `sending_domain`.", "When delivery outcome receipt occurs at `provider_webhook_receipt`, hard-bounce and complaint counter update occurs at `reputation_unit`. idempotent", "When threshold evaluation occurs at `reputation_unit`, quarantine or throttle state change occurs at `send_gate_decision`.", "When one-unit quarantine application occurs at `reputation_unit`, peer gate preservation occurs at `send_gate_decision`.", "When blocked-firm enrollment attempt occurs at `engagement_attempt`, quarantine denial occurs at `outbound_message`.", "When quarantine denial occurs at `outbound_message`, cause append occurs at `audit_trail`."],
        nodesTouched: ["reputation_unit", "sending_domain", "ip_pool_tier", "provider_webhook_receipt", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-06",
        title: "New domains and IPs warm up before volume",
        implementationSource: "Warmup shows a daily recipient and volume ramp for each new domain/IP, and Activation blocks full-book blasts until the stage permits them.",
        start: "Starting from warmup_schedule (new)",
        pathHops: ["When new domain or IP tier assignment occurs at `sending_domain`, ramp-plan opening occurs at `warmup_schedule`.", "When daily unique-recipient and absolute-volume cap setting occurs at `warmup_schedule`, allowed-cap update occurs at `send_gate_decision`.", "When Activation planned-volume check occurs at `firm_tenancy`, warmup-stage comparison occurs at `engagement_attempt`.", "When planned sequence over-cap status occurs at `engagement_attempt`, full-book block or cap occurs at `send_gate_decision`.", "When provider acceptance and complaint signal receipt occurs at `provider_webhook_receipt`, warmup advancement or hold update occurs at `warmup_schedule`. idempotent"],
        nodesTouched: ["sending_domain", "ip_pool_tier", "warmup_schedule", "send_gate_decision", "firm_tenancy", "engagement_attempt", "provider_webhook_receipt"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-07",
        title: "Ready-to-send includes deliverability readiness",
        implementationSource: "Activation Progress shows deliverability readiness beside consent and commercial readiness, and Send gates require the full conjunction for Armed / Active state.",
        start: "Starting from send_gate_decision (existing)",
        pathHops: ["When SPF, DKIM, and DMARC predicate refresh occurs at `send_gate_decision`, authentication readiness combination occurs at `sending_identity_binding`.", "When warmup capacity refresh occurs at `warmup_schedule`, volume-readiness change occurs at `send_gate_decision`.", "When reputation quarantine status change occurs at `reputation_unit`, deliverability readiness change occurs at `send_gate_decision`.", "When consent or silence status change occurs at `consent_record`, legal send eligibility change occurs at `send_gate_decision`.", "When Activation ready-to-send evaluation occurs at `firm_tenancy`, Armed or Active eligibility update occurs at `engagement_attempt`.", "When readiness conjunction failure occurs at `send_gate_decision`, blocked-reason append occurs at `audit_trail`."],
        nodesTouched: ["send_gate_decision", "sending_identity_binding", "warmup_schedule", "reputation_unit", "consent_record", "firm_tenancy", "engagement_attempt", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-08",
        title: "Warmup caps bind the sequencer",
        implementationSource: "Send gates expose remaining warmup and throttle capacity, enrollment beyond the cap queues or blocks, and Warmup shows consumed versus allowed volume.",
        start: "Starting from warmup_schedule (existing)",
        pathHops: ["When daily cap window opening occurs at `warmup_schedule`, remaining-capacity update occurs at `send_gate_decision`.", "When sequence enrollment request occurs at `engagement_attempt`, capacity reservation occurs at `warmup_schedule`. within the daily throttle window", "When reservation-fit check occurs at `warmup_schedule`, allowed-recipient advancement occurs at `outbound_message`.", "When reservation-over-cap check occurs at `warmup_schedule`, overflow queue or denial occurs at `send_gate_decision`.", "When ESP accept event receipt occurs at `provider_webhook_receipt`, consumed-capacity update occurs at `warmup_schedule`. idempotent", "When cap state change occurs at `warmup_schedule`, shared-budget visibility update occurs at `send_gate_decision`."],
        nodesTouched: ["warmup_schedule", "send_gate_decision", "engagement_attempt", "outbound_message", "provider_webhook_receipt"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-09",
        title: "Identity changes trigger re-warmup",
        implementationSource: "Warmup reopens on domain change, IP tier change, ESP migration, or long idle, and Activation and Send gates hold tightened caps until re-warmup completes.",
        start: "Starting from sending_identity_binding (existing)",
        pathHops: ["When domain change occurs at `sending_identity_binding`, re-warmup marker update occurs at `sending_domain`.", "When IP tier change occurs at `ip_pool_tier`, reputation-reset marker update occurs at `reputation_unit`.", "When ESP migration or idle-window expiry occurs at `sending_domain`, re-warmup phase opening occurs at `warmup_schedule`.", "When re-warmup start occurs at `warmup_schedule`, tightened-cap update occurs at `send_gate_decision`.", "When prior-volume reactivation request occurs at `firm_tenancy`, old-volume denial occurs at `send_gate_decision`.", "When re-warmup completion occurs at `warmup_schedule`, tightened-cap lift occurs at `send_gate_decision`."],
        nodesTouched: ["sending_identity_binding", "sending_domain", "ip_pool_tier", "reputation_unit", "warmup_schedule", "send_gate_decision", "firm_tenancy"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-10",
        title: "Bounce classification drives eligibility",
        implementationSource: "Provider webhooks classify hard and soft bounces, hard bounces add suppressions, and soft bounces retry only within bounded attempts visible on firm health.",
        start: "Starting from provider_webhook_receipt (new)",
        pathHops: ["When provider bounce webhook receipt occurs at `provider_webhook_receipt`, duplicate-processing prevention occurs at `provider_webhook_receipt`. idempotent", "When bounce classification occurs at `messaging_event_stream`, hard-or-soft outcome normalization occurs at `outbound_message`.", "When hard-bounce classification occurs at `messaging_event_stream`, scoped address suppression occurs at `suppression_list`.", "When soft-bounce classification occurs at `messaging_event_stream`, bounded retry-attempt update occurs at `engagement_attempt`.", "When future send request for that address occurs at `engagement_attempt`, hard-suppression or soft-retry eligibility update occurs at `send_gate_decision`.", "When bounce eligibility change occurs at `send_gate_decision`, reason append occurs at `audit_trail`."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "outbound_message", "suppression_list", "engagement_attempt", "send_gate_decision", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-11",
        title: "Complaint rate is a kill switch",
        implementationSource: "Oversight and Firm health show complaint rate by reputation unit, and threshold approach or breach throttles or quarantines the affected unit.",
        start: "Starting from reputation_unit (existing)",
        pathHops: ["When spam-complaint event receipt occurs at `provider_webhook_receipt`, complaint-count update occurs at `reputation_unit`. idempotent", "When complaint-rate calculation occurs at `reputation_unit`, target and ceiling posture update occurs at `provider_reputation_feed`.", "When operating-target approach occurs at `reputation_unit`, throttle state change occurs at `send_gate_decision`.", "When complaint-ceiling breach occurs at `reputation_unit`, quarantine state change occurs at `send_gate_decision`.", "When new-volume attempt occurs at `engagement_attempt`, complaint kill-switch block or cap occurs at `outbound_message`.", "When throttle or quarantine change occurs at `send_gate_decision`, threshold-reason append occurs at `audit_trail`."],
        nodesTouched: ["provider_webhook_receipt", "reputation_unit", "provider_reputation_feed", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-12",
        title: "Postmaster and FBL feeds surface reputation",
        implementationSource: "Sending infrastructure shows Postmaster and feedback-loop registration, and Oversight / Firm health consume provider spam-rate and reputation signals alongside ESP webhooks.",
        start: "Starting from provider_reputation_feed (new)",
        pathHops: ["When Postmaster, FBL, or sender-intelligence registration recording occurs at `provider_reputation_feed`, coverage-state update occurs at `sending_domain`.", "When provider spam-rate or reputation signal ingest occurs at `provider_reputation_feed`, health-indicator update occurs at `reputation_unit`.", "When complaint feed event receipt occurs at `provider_webhook_receipt`, provider-event normalization occurs at `messaging_event_stream`. idempotent", "When degraded reputation signal occurs at `provider_reputation_feed`, gate-posture change occurs at `send_gate_decision`.", "When provider-grade reputation state change occurs at `reputation_unit`, fleet and firm health materialization occurs at `provider_reputation_feed`."],
        nodesTouched: ["provider_reputation_feed", "sending_domain", "reputation_unit", "provider_webhook_receipt", "messaging_event_stream", "send_gate_decision"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-13",
        title: "One-click List-Unsubscribe feeds suppression",
        implementationSource: "Opt-in and Nudge CEMs emit List-Unsubscribe and List-Unsubscribe-Post headers, and one-click POSTs honor the same withdrawal path as in-body unsubscribe.",
        start: "Starting from outbound_message (existing)",
        pathHops: ["When marketing CEM composition occurs at `outbound_message`, one-click header attachment occurs at `outbound_message`.", "When message From identity resolution occurs at `from_identity`, unsubscribe endpoint scoping occurs at `outbound_message`.", "When mailbox-provider one-click POST receipt occurs at `provider_webhook_receipt`, withdrawal-path acceptance occurs at `provider_webhook_receipt`. idempotent", "When one-click withdrawal normalization occurs at `messaging_event_stream`, silence state update occurs at `consent_record`.", "When silence state change occurs at `consent_record`, technical-deny mirror occurs at `suppression_list`.", "When future CEM enrollment occurs at `engagement_attempt`, suppression-deny gate closure occurs at `send_gate_decision`."],
        nodesTouched: ["outbound_message", "from_identity", "provider_webhook_receipt", "messaging_event_stream", "consent_record", "suppression_list", "engagement_attempt", "send_gate_decision"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-14",
        title: "Provider webhooks control reputation state",
        implementationSource: "Delivery-event ingest normalizes accepted, deferred, bounce, complaint, delivered, and rejected events into Warmup, Suppression list, reputation-unit health, and audit explanations.",
        start: "Starting from provider_webhook_receipt (existing)",
        pathHops: ["When ESP or SMS provider webhook receipt occurs at `provider_webhook_receipt`, duplicate-receipt ignore occurs at `provider_webhook_receipt`. idempotent", "When provider event normalization occurs at `messaging_event_stream`, delivery outcome attachment occurs at `outbound_message`.", "When accepted or delivered outcome occurs at `messaging_event_stream`, ramp-progress change occurs at `warmup_schedule`.", "When hard-bounce or complaint outcome occurs at `messaging_event_stream`, deny-entry change occurs at `suppression_list`.", "When deferred, rejected, bounce, or complaint outcome occurs at `messaging_event_stream`, health-counter change occurs at `reputation_unit`.", "When delivery-state eligibility change occurs at `messaging_event_stream`, synchronous gate update occurs at `send_gate_decision`.", "When delivery-event gate change occurs at `send_gate_decision`, explanation append occurs at `audit_trail`."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "outbound_message", "warmup_schedule", "suppression_list", "reputation_unit", "send_gate_decision", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-15",
        title: "Hard bounces and complaints suppress globally",
        implementationSource: "House-global Suppression list applies hard bounces and spam complaints across every tenancy, and Send gates block even when a firm's book marks the contact reachable.",
        start: "Starting from suppression_list (existing)",
        pathHops: ["When hard-bounce or spam-complaint normalization occurs at `messaging_event_stream`, address-level global deny write occurs at `suppression_list`.", "When same-person import or refresh occurs at `contact_identity`, suppression address match occurs at `suppression_list`.", "When reachable contact verdict occurs at `book_readiness`, global toxic-address override occurs at `send_gate_decision`.", "When cross-tenant enrollment attempt occurs at `engagement_attempt`, global suppression block occurs at `outbound_message`.", "When cross-tenant suppression block occurs at `send_gate_decision`, privacy-preserving deny reason append occurs at `audit_trail`."],
        nodesTouched: ["messaging_event_stream", "suppression_list", "contact_identity", "book_readiness", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-16",
        title: "Per-tenancy suppression mirrors legal silence",
        implementationSource: "Send gates show ordered denies from consent basis through per-tenancy and global suppressions, with opt-out, silence, complaints, and hard bounces represented as technical denies.",
        start: "Starting from consent_record (existing)",
        pathHops: ["When opt-out or silence change occurs at `consent_record`, per-tenancy withdrawal mirror occurs at `suppression_list`.", "When complaint or hard-bounce outcome occurs at `messaging_event_stream`, per-tenancy or global deny-scope change occurs at `suppression_list`.", "When permission-to-send evaluation occurs at `book_readiness`, reachability separation occurs at `send_gate_decision`.", "When CEM enrollment request occurs at `engagement_attempt`, ordered deny evaluation occurs at `send_gate_decision`.", "When ordered deny closure occurs at `send_gate_decision`, single visible reason append occurs at `audit_trail`."],
        nodesTouched: ["consent_record", "suppression_list", "messaging_event_stream", "book_readiness", "engagement_attempt", "send_gate_decision", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-17",
        title: "Suppression is mandatory before provider accept",
        implementationSource: "Every automated and manual firm-branded email/SMS path consults Suppression list before provider accept, with blocked-suppressed visible where enrollment would target denied addresses.",
        start: "Starting from send_gate_decision (existing)",
        pathHops: ["When automated sequence enrollment occurs at `engagement_attempt`, pre-send suppression check occurs at `suppression_list`.", "When manual retry or operator resend occurs at `engagement_attempt`, suppression-check update occurs at `send_gate_decision`.", "When activation first-message path occurs at `engagement_attempt`, provider-before-block check occurs at `suppression_list`.", "When suppressed address match occurs at `suppression_list`, provider-bound message denial occurs at `outbound_message`.", "When blocked-suppressed state change occurs at `send_gate_decision`, attempted-path deny append occurs at `audit_trail`."],
        nodesTouched: ["send_gate_decision", "engagement_attempt", "suppression_list", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-18",
        title: "Idle and non-engaging addresses age out",
        implementationSource: "Book readiness and Firm health show sunsetting or chronic non-engagement flags, and Send gates block reactivation bursts unless a new engagement path re-permissions the address.",
        start: "Starting from book_readiness (existing)",
        pathHops: ["When engagement-age evaluation occurs at `book_readiness`, stale or chronic non-engagement update occurs at `reachability_class`.", "When role, invalid, abandoned, or chronic non-engagement signal attachment occurs at `contact_identity`, hygiene-status change occurs at `suppression_list`.", "When reactivation burst proposal occurs at `engagement_attempt`, hygiene-deny read occurs at `suppression_list`.", "When re-permission requirement occurs at `send_gate_decision`, normal sequence volume block occurs at `engagement_attempt`.", "When permitted new engagement change occurs at `consent_record`, hygiene-state review occurs at `suppression_list`.", "When sunsetting volume prevention occurs at `send_gate_decision`, stale-list risk append occurs at `audit_trail`."],
        nodesTouched: ["book_readiness", "reachability_class", "contact_identity", "suppression_list", "engagement_attempt", "send_gate_decision", "consent_record", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-19",
        title: "Visible From is firm-branded and alignable",
        implementationSource: "Sending infrastructure sets the firm's From identity to the authenticated branded subdomain, and firm-branded CEMs no longer offer a shared platform From.",
        start: "Starting from from_identity (new)",
        pathHops: ["When firm-branded From identity creation occurs at `from_identity`, display-name and local-part binding occurs at `sending_domain`.", "When From-domain authentication occurs at `sending_domain`, alignment requirement update occurs at `send_gate_decision`.", "When CEM identity binding occurs at `sending_identity_binding`, shared-platform From removal occurs at `outbound_message`.", "When Opt-in or Nudge message composition occurs at `outbound_message`, firm-branded From application occurs at `from_identity`.", "When selected-From nonalignment occurs at `from_identity`, CEM leave denial occurs at `send_gate_decision`.", "When CEM leave denial occurs at `send_gate_decision`, denial append occurs at `audit_trail`."],
        nodesTouched: ["from_identity", "sending_domain", "send_gate_decision", "sending_identity_binding", "outbound_message", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-20",
        title: "Return-Path is platform-controlled",
        implementationSource: "Sending infrastructure separates platform-controlled custom MAIL FROM / Return-Path from firm header From, and audit events correlate bounces through that return path.",
        start: "Starting from return_path_domain (existing)",
        pathHops: ["When platform bounce subdomain configuration occurs at `return_path_domain`, SPF authorization link occurs at `sending_domain`.", "When firm From identity binding occurs at `from_identity`, envelope MAIL FROM separation occurs at `return_path_domain`.", "When provider email accept occurs at `outbound_message`, Return-Path token mapping occurs at `engagement_attempt`.", "When custom MAIL FROM bounce receipt occurs at `provider_webhook_receipt`, return-token correlation occurs at `outbound_message`. idempotent", "When bounce classification occurs at `messaging_event_stream`, suppression update occurs at `suppression_list`.", "When bounce classification occurs at `messaging_event_stream`, reputation update occurs at `reputation_unit`.", "When bounce-correlation eligibility change occurs at `send_gate_decision`, deliverability-event append occurs at `audit_trail`."],
        nodesTouched: ["return_path_domain", "sending_domain", "from_identity", "outbound_message", "engagement_attempt", "provider_webhook_receipt", "messaging_event_stream", "suppression_list", "reputation_unit", "send_gate_decision", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-21",
        title: "Reply-To lands on monitored capture",
        implementationSource: "Sending infrastructure sets Reply-To to a platform-captured or firm-monitored address per bind policy while From stays firm-branded and replies feed Conversations.",
        start: "Starting from reply_route (new)",
        pathHops: ["When Reply-To strategy selection occurs at `reply_route`, monitored-address binding occurs at `sending_identity_binding`.", "When platform capture policy occurs at `reply_route`, incoming reply routing occurs at `conversation_thread`.", "When firm-monitored policy selection occurs at `reply_route`, monitored-mailbox expectation update occurs at `conversation_thread`.", "When outbound CEM composition occurs at `outbound_message`, firm-branded From read occurs at `from_identity`.", "When outbound CEM composition occurs at `outbound_message`, Reply-To read occurs at `reply_route`.", "When reply arrival occurs at `conversation_thread`, reply intent classification update occurs at `intent_classification`.", "When STOP, booked, or question classification occurs at `intent_classification`, downstream conversation eligibility update occurs at `conversation_thread`."],
        nodesTouched: ["reply_route", "sending_identity_binding", "conversation_thread", "outbound_message", "from_identity", "intent_classification"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops",
      },
{
        id: "deliv-22",
        title: "IP tier follows reputation economics",
        implementationSource: "Sending infrastructure shows shared versus dedicated IP tier and promotion criteria, while Warmup requires a dedicated-IP ramp and PTR/rDNS hygiene before volume.",
        start: "Starting from ip_pool_tier (existing)",
        pathHops: ["When shared-pool or dedicated-IP assignment occurs at `ip_pool_tier`, tier binding occurs at `reputation_unit`.", "When sustained-volume and clean-metric evaluation occurs at `reputation_unit`, promotion eligibility change occurs at `ip_pool_tier`.", "When dedicated IP assignment occurs at `ip_pool_tier`, dedicated ramp opening occurs at `warmup_schedule`.", "When PTR or rDNS hygiene recording occurs at `ip_pool_tier`, infrastructure readiness update occurs at `send_gate_decision`.", "When premature dedicated-volume request occurs at `engagement_attempt`, volume denial or cap occurs at `send_gate_decision`.", "When IP strategy change occurs at `ip_pool_tier`, cost and warmup opportunity-cost visibility occurs at `ip_pool_tier`."],
        nodesTouched: ["ip_pool_tier", "reputation_unit", "warmup_schedule", "send_gate_decision", "engagement_attempt"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-23",
        title: "Provider deferrals drive adaptive throttle",
        implementationSource: "Sending infrastructure exposes per-provider throttle state from deferral or reject signals, and Send gates pause or back off enrollment when ISPs defer.",
        start: "Starting from provider_throttle_state (new)",
        pathHops: ["When 4xx deferral or rate-reject receipt occurs at `provider_webhook_receipt`, provider and unit outcome normalization occurs at `messaging_event_stream`. idempotent", "When deferral pressure recognition occurs at `messaging_event_stream`, adaptive backoff state change occurs at `provider_throttle_state`.", "When backoff state change occurs at `provider_throttle_state`, capacity and pause update occurs at `send_gate_decision`.", "When calendar-due enrollment occurs at `engagement_attempt`, provider-backoff consult occurs at `send_gate_decision`.", "When active throttle state occurs at `provider_throttle_state`, retry delay occurs at `engagement_attempt`. within provider backoff window", "When deferral clear and acceptance improvement occurs at `provider_webhook_receipt`, throttle relaxation occurs at `provider_throttle_state`.", "When throttle relaxation occurs at `provider_throttle_state`, gradual capacity reopen occurs at `send_gate_decision`."],
        nodesTouched: ["provider_throttle_state", "provider_webhook_receipt", "messaging_event_stream", "send_gate_decision", "engagement_attempt", "outbound_message"],
        facets: "Core App/Runtime, Data Storage, External Systems, Infra/Ops, Cost/FinOps",
      },
{
        id: "deliv-24",
        title: "SMS has separate carrier reputation",
        implementationSource: "Sending infrastructure shows SMS brand and campaign registration plus throughput tier, and Send gates require SMS registration, carrier capacity, and STOP/suppression clearance separately from email Warmup.",
        start: "Starting from sms_sender_registration (new)",
        pathHops: ["When A2P brand or campaign registration status change occurs at `sms_sender_registration`, SMS-ready state update occurs at `send_gate_decision`.", "When carrier throughput tier assignment occurs at `carrier_throughput_tier`, per-window SMS capacity change occurs at `send_gate_decision`. under carrier throughput window", "When SMS delivery, undelivered, or complaint event receipt occurs at `provider_webhook_receipt`, carrier reputation state update occurs at `sms_reputation_unit`. idempotent", "When STOP or opt-out event receipt occurs at `provider_webhook_receipt`, phone-number deny change occurs at `suppression_list`.", "When firm-branded SMS send request occurs at `engagement_attempt`, SMS eligibility conjunction update occurs at `send_gate_decision`.", "When email Warmup green status occurs at `warmup_schedule`, SMS eligibility non-authorizing record occurs at `send_gate_decision`.", "When SMS denial occurs at `send_gate_decision`, denial reason append occurs at `audit_trail`."],
        nodesTouched: ["sms_sender_registration", "carrier_throughput_tier", "send_gate_decision", "provider_webhook_receipt", "sms_reputation_unit", "suppression_list", "engagement_attempt", "warmup_schedule", "audit_trail"],
        facets: "Core App/Runtime, Data Storage, External Systems, Identity/Security/Compliance, Infra/Ops, Cost/FinOps",
      }
    ],
  },
{
    id: "c2-agentic-engagement-runtime",
    label: "C2 Agentic engagement runtime",
    items: [
{
        id: "agent-01",
        title: "Inbound replies are first-class conversation events, not optional log lines",
        implementationSource: "Conversations and Engagement record now show inbound replies as durable thread events linked to the prior outbound, and active firm runners consume the same stream before scheduling more motion.",
        start: "Starting from provider_webhook_receipt (existing)",
        pathHops: ["When an inbound participant reply webhook occurs at `provider_webhook_receipt`, an inbound reply event is appended at `messaging_event_stream`. idempotent", "When the inbound reply event occurs at `messaging_event_stream`, the channel thread and prior outbound linkage are updated at `conversation_thread`. encrypted", "When the linked reply appears at `conversation_thread`, a pending read request occurs at `intent_classification`.", "When reply capture completes at `conversation_thread`, inbound-received chronology occurs at `engagement_record`.", "When the inbound reply event occurs at `messaging_event_stream`, the active owner is marked reply-interrupted at `engagement_attempt`."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "conversation_thread", "intent_classification", "engagement_record", "engagement_attempt"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-02",
        title: "Closed intent set: booked / question / stop / hostile (plus unsure)",
        implementationSource: "Conversations, Engagement record, and sequence policy now use the same closed reply intent set rather than per-template free-text labels.",
        start: "Starting from conversation_thread (existing)",
        pathHops: ["When an unclassified inbound reply occurs at `conversation_thread`, a closed-set classification request occurs at `intent_classification`.", "When classification completion occurs at `intent_classification`, exactly one class and confidence occur at `intent_classification`.", "When the closed intent result occurs at `intent_classification`, the thread's current intent marker changes at `conversation_thread`.", "When the intent marker changes at `conversation_thread`, classified-reply chronology occurs at `engagement_record`.", "When the classified class occurs at `intent_classification`, next-action eligibility changes at `engagement_attempt`."],
        nodesTouched: ["conversation_thread", "intent_classification", "engagement_record", "engagement_attempt"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "agent-03",
        title: "Stop / opt-out language is a hard intent that freezes CEMs",
        implementationSource: "Stop-class language and provider STOP keywords now classify as terminal, suppress automatic CEMs, freeze attempts, and record the stop-to-suppression chain.",
        start: "Starting from provider_webhook_receipt (existing)",
        pathHops: ["When a STOP keyword or stop-language reply occurs at `provider_webhook_receipt`, an inbound reply event is appended at `messaging_event_stream`. idempotent", "When the stop reply event occurs at `messaging_event_stream`, terminal stop intent is written at `intent_classification`.", "When terminal stop intent occurs at `intent_classification`, a channel/contact suppression entry occurs at `suppression_list`. under jurisdiction", "When the suppression entry occurs at `suppression_list`, a deny result for automatic CEMs occurs at `send_gate_decision`.", "When the deny result occurs at `send_gate_decision`, the active owner and pending attempts are frozen at `engagement_attempt`.", "When the attempt freeze occurs at `engagement_attempt`, stop-to-suppression chronology occurs at `engagement_record`."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "intent_classification", "suppression_list", "send_gate_decision", "engagement_attempt", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-04",
        title: "Low-confidence / no-match must not invent an intent",
        implementationSource: "Low-confidence replies now become `unsure`, pause automatic owner motion, and open human review before the runtime acts.",
        start: "Starting from intent_classification (existing)",
        pathHops: ["When a reply cannot meet the confidence floor at `intent_classification`, an `unsure` result occurs at `intent_classification`.", "When `unsure` occurs at `intent_classification`, automatic next-attempt eligibility is paused at `engagement_attempt`.", "When the pause occurs at `engagement_attempt`, a review item with reply context occurs at `escalation_ticket`.", "When the review item occurs at `escalation_ticket`, escalated-thread state occurs at `conversation_thread`.", "When escalated-thread state occurs at `conversation_thread`, fail-closed chronology occurs at `engagement_record`."],
        nodesTouched: ["intent_classification", "engagement_attempt", "escalation_ticket", "conversation_thread", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-05",
        title: "Classification always re-enters Send gates before the next send",
        implementationSource: "Every classified inbound now forces a post-reply gate check before any next attempt can schedule or send.",
        start: "Starting from intent_classification (existing)",
        pathHops: ["When a non-terminal intent result occurs at `intent_classification`, a post-reply gate evaluation occurs at `send_gate_decision`.", "When the gate evaluation occurs at `send_gate_decision`, consent, suppression, and firm posture are read from `consent_record`, `suppression_list`, and `firm_operations_bind`.", "When gate deny occurs at `send_gate_decision`, blocked next-owner action occurs at `engagement_attempt`. idempotent", "When the gate allows at `send_gate_decision`, eligible next-attempt state occurs at `engagement_attempt`.", "When eligible next-attempt state occurs at `engagement_attempt`, any later send instance is created at `outbound_message`.", "When the gate result occurs at `send_gate_decision`, post-reply allow or deny chronology occurs at `engagement_record`."],
        nodesTouched: ["intent_classification", "send_gate_decision", "consent_record", "suppression_list", "firm_operations_bind", "engagement_attempt", "outbound_message", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; External Systems.",
      },
{
        id: "agent-06",
        title: "Auto-replies / OOO are not finishing \u201creplied\u201d intents",
        implementationSource: "Out-of-office and machine replies now pause under the owning channel, with a bounded resume policy, instead of finishing the engagement as human intent.",
        start: "Starting from provider_webhook_receipt (existing)",
        pathHops: ["When an auto-reply or OOO inbound occurs at `provider_webhook_receipt`, the inbound event is appended at `messaging_event_stream`. idempotent", "When the auto-reply signal occurs at `messaging_event_stream`, non-human pause classification occurs at `intent_classification`.", "When non-human pause classification occurs at `intent_classification`, an OOO pause window occurs at `auto_reply_pause`.", "When the OOO pause window occurs at `auto_reply_pause`, the current channel owner is held at `engagement_attempt`.", "When resume-window maturity occurs at `auto_reply_pause`, re-entry eligibility recheck occurs at `send_gate_decision`.", "When the pause or resume state changes at `auto_reply_pause`, pause chronology occurs at `engagement_record`."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "intent_classification", "auto_reply_pause", "engagement_attempt", "send_gate_decision", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-07",
        title: "Exactly one channel owns the flow at a time",
        implementationSource: "Active runners now expose and enforce a single owning channel, with attempts nested under that owner and dual-owner schedules refused.",
        start: "Starting from firm_operations_bind (new)",
        pathHops: ["When a sequence becomes Active for a firm at `firm_operations_bind`, an active sequence instance occurs at `sequence_instance`.", "When sequence-instance activation occurs at `sequence_instance`, single channel owner assignment occurs at `engagement_attempt`.", "When another owner schedule request occurs while one owner is active at `engagement_attempt`, request rejection occurs at `engagement_attempt`. idempotent", "When the active owner schedules an outbound attempt at `engagement_attempt`, gate evaluation occurs at `send_gate_decision`.", "When the active-owner state changes at `engagement_attempt`, owner chronology occurs at `engagement_record`."],
        nodesTouched: ["firm_operations_bind", "sequence_instance", "engagement_attempt", "send_gate_decision", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-08",
        title: "Happy-path handoff is forward-only when criteria are met",
        implementationSource: "Authored owner order now moves only forward when the current owner's criteria fire, with no backward re-entry outside a new local attempt.",
        start: "Starting from sequence_rule_set (new)",
        pathHops: ["When forward owner order authorship occurs at `sequence_rule_set`, ordered owner state occurs at `sequence_instance`.", "When a current-owner success or failure criterion occurs at `messaging_event_stream`, the matching window is satisfied at `criteria_window`.", "When the criteria window is satisfied at `criteria_window`, a forward-only owner transition occurs at `engagement_attempt`.", "When the forward owner transition occurs at `engagement_attempt`, channel-specific permission recheck occurs at `send_gate_decision`.", "When the transition is accepted at `engagement_attempt`, handoff chronology occurs at `engagement_record`."],
        nodesTouched: ["sequence_rule_set", "sequence_instance", "messaging_event_stream", "criteria_window", "engagement_attempt", "send_gate_decision", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-09",
        title: "Attempts are local retries under the failed channel, not a global Attempt N",
        implementationSource: "Attempt counters now live under the failed owning channel, and thread and record context show the same nested owner.",
        start: "Starting from engagement_attempt (existing)",
        pathHops: ["When an owner-level failure condition occurs at `engagement_attempt`, a local retry cycle is created under that same owner at `engagement_attempt`.", "When the local retry cycle occurs at `engagement_attempt`, a scheduled retry job occurs at `attempt_job_queue`.", "When the retry job is armed at `attempt_job_queue`, gate evaluation for the same owner occurs at `send_gate_decision`.", "When inbound reply arrival during the retry cycle occurs at `conversation_thread`, nested owner context update occurs at `conversation_thread`.", "When the local retry cycle changes at `engagement_attempt`, local-attempt chronology occurs at `engagement_record`."],
        nodesTouched: ["engagement_attempt", "attempt_job_queue", "send_gate_decision", "conversation_thread", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-10",
        title: "Criteria windows drive non-reply escalation between owners",
        implementationSource: "Delivery, open, click, form, and elapsed-window signals now drive owner escalation instead of blind timers.",
        start: "Starting from messaging_event_stream (existing)",
        pathHops: ["When delivered, opened, clicked, or elapsed message evidence occurs at `messaging_event_stream`, criteria evidence is evaluated at `criteria_window`.", "When form started or submitted evidence occurs at `form_activity_event`, criteria evidence is evaluated at `criteria_window`.", "When the authored criteria window is satisfied at `criteria_window`, non-reply transition eligibility occurs at `engagement_attempt`.", "When transition eligibility occurs at `engagement_attempt`, channel and posture recheck occurs at `send_gate_decision`.", "When the transition is applied at `engagement_attempt`, criteria-caused handoff chronology occurs at `engagement_record`."],
        nodesTouched: ["messaging_event_stream", "form_activity_event", "criteria_window", "engagement_attempt", "send_gate_decision", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-11",
        title: "Live inbound (and live form visit) nullifies in-flight attempts",
        implementationSource: "Live replies and form activity now hard-cancel pending scheduled attempts before intent policy or criteria policy can continue.",
        start: "Starting from messaging_event_stream (existing)",
        pathHops: ["When a live inbound reply occurs at `messaging_event_stream`, the active pending job is marked nullified at `engagement_attempt`. within race boundary \u00b7 idempotent", "When a live form visit occurs at `form_activity_event`, the active pending job is marked nullified at `engagement_attempt`. within race boundary \u00b7 idempotent", "When pending job nullification occurs at `engagement_attempt`, scheduled-send cancellation occurs at `attempt_job_queue`.", "When the job cancellation occurs at `attempt_job_queue`, classification or form policy evaluation continues at `intent_classification` or `criteria_window`.", "When nullification completes at `engagement_attempt`, attempt-nullified chronology occurs at `engagement_record`."],
        nodesTouched: ["messaging_event_stream", "form_activity_event", "engagement_attempt", "attempt_job_queue", "intent_classification", "criteria_window", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Infrastructure & Operations; Identity / Security / Compliance.",
      },
{
        id: "agent-12",
        title: "Finishing events close the sequence and may arm the next",
        implementationSource: "Booked, form-submitted, terminal no-reply, stop, and human close now finish the current sequence and can arm the next authored peer.",
        start: "Starting from sequence_finish_state (new)",
        pathHops: ["When booked or stop intent occurs at `intent_classification`, a finishing predicate is written at `sequence_finish_state`.", "When form submitted occurs at `form_activity_event`, a goal-met predicate is written at `sequence_finish_state`.", "When terminal no-reply occurs at `engagement_attempt`, a no-reply predicate is written at `sequence_finish_state`.", "When a finishing predicate occurs at `sequence_finish_state`, the active sequence closes at `sequence_instance`.", "When the closed sequence has an authored successor at `sequence_rule_set`, next-sequence armed state occurs at `sequence_enrollment`.", "When close or arm state occurs at `sequence_instance` or `sequence_enrollment`, finishing chronology occurs at `engagement_record`."],
        nodesTouched: ["sequence_finish_state", "intent_classification", "form_activity_event", "engagement_attempt", "sequence_instance", "sequence_rule_set", "sequence_enrollment", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-13",
        title: "Campaign calendar and attempt engine are two layers \u2014 do not conflate",
        implementationSource: "Day-ordered campaign content now enrolls a sequence instance, while owner and attempt law runs inside the attempt engine as separate state.",
        start: "Starting from campaign_calendar (new)",
        pathHops: ["When a campaign day becomes eligible at `campaign_calendar`, sequence enrollment state occurs at `sequence_enrollment`.", "When enrollment occurs at `sequence_enrollment`, a runtime sequence instance occurs at `sequence_instance`.", "When the runtime sequence instance occurs at `sequence_instance`, owner and attempt rules are loaded from `sequence_rule_set`.", "When owner rules are loaded from `sequence_rule_set`, active owner state occurs at `engagement_attempt`.", "When calendar phase or owner attempt state change occurs at `campaign_calendar` or `engagement_attempt`, distinct chronology entries occur at `engagement_record`."],
        nodesTouched: ["campaign_calendar", "sequence_enrollment", "sequence_instance", "sequence_rule_set", "engagement_attempt", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Cost / FinOps.",
      },
{
        id: "agent-14",
        title: "Decide\u2192act uses constrained tool-use, not free-form LLM sends",
        implementationSource: "Runtime decisions now execute through a closed tool set that re-enters Send gates and records tool name, intent, owner, and gate result.",
        start: "Starting from intent_classification (existing)",
        pathHops: ["When an actionable intent occurs at `intent_classification`, a closed tool choice occurs at `runtime_tool_invocation`.", "When a send-like tool choice occurs at `runtime_tool_invocation`, gate evaluation occurs at `send_gate_decision`.", "When the gate allows at `send_gate_decision`, owner action state changes at `engagement_attempt` and any send instance occurs at `outbound_message`.", "When the gate denies at `send_gate_decision`, tool failure state occurs at `runtime_tool_invocation` and no send occurs at `outbound_message`.", "When any tool act resolves at `runtime_tool_invocation`, tool audit state occurs at `audit_trail`.", "When the tool act changes runtime state at `runtime_tool_invocation`, tool-act chronology occurs at `engagement_record`."],
        nodesTouched: ["intent_classification", "runtime_tool_invocation", "send_gate_decision", "engagement_attempt", "outbound_message", "audit_trail", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; External Systems; Cost / FinOps.",
      },
{
        id: "agent-15",
        title: "Channel escalation still requires channel-scoped consent and suppression clearance",
        implementationSource: "Forward channel escalation now checks channel-scoped consent and suppression before the attempt engine can treat escalation as allowed.",
        start: "Starting from engagement_attempt (existing)",
        pathHops: ["When owner escalation eligibility occurs at `engagement_attempt`, channel permission evaluation occurs at `send_gate_decision`.", "When permission evaluation occurs at `send_gate_decision`, channel scope is read at `consent_record`.", "When permission evaluation occurs at `send_gate_decision`, address and channel suppression are read at `suppression_list`.", "When consent or suppression denies at `send_gate_decision`, blocked escalation state occurs at `engagement_attempt`. under jurisdiction", "When consent and suppression allow at `send_gate_decision`, next-owner scheduling occurs at `attempt_job_queue`.", "When escalation is blocked or allowed at `engagement_attempt`, channel-scope chronology occurs at `engagement_record`."],
        nodesTouched: ["engagement_attempt", "send_gate_decision", "consent_record", "suppression_list", "attempt_job_queue", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; External Systems.",
      },
{
        id: "agent-16",
        title: "Unsure and policy edge cases open Escalation queue, not silent continue",
        implementationSource: "Unsure replies, policy edges, and failed tool preconditions now pause automatic attempts and open an escalation item.",
        start: "Starting from intent_classification (existing)",
        pathHops: ["When `unsure` or policy-edge classification occurs at `intent_classification`, automatic action pause occurs at `engagement_attempt`.", "When a tool precondition fails at `runtime_tool_invocation`, automatic action pause occurs at `engagement_attempt`.", "When action pause occurs at `engagement_attempt`, an escalation item occurs at `escalation_ticket`.", "When the escalation item occurs at `escalation_ticket`, escalated status occurs at `conversation_thread`.", "When escalated status occurs at `conversation_thread`, escalation-opened chronology occurs at `engagement_record`."],
        nodesTouched: ["intent_classification", "runtime_tool_invocation", "engagement_attempt", "escalation_ticket", "conversation_thread", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-17",
        title: "Hostile intent halts automatic CEMs and escalates immediately",
        implementationSource: "Hostile replies now suppress automatic outreach for the contact sequence, halt attempts, and open a high-priority human escalation.",
        start: "Starting from intent_classification (existing)",
        pathHops: ["When hostile intent occurs at `intent_classification`, automatic CEM suppression occurs at `suppression_list`.", "When automatic CEM suppression occurs at `suppression_list`, deny state occurs at `send_gate_decision`.", "When deny state occurs at `send_gate_decision`, current and pending attempts halt at `engagement_attempt`.", "When hostile intent occurs at `intent_classification`, a high-priority escalation item occurs at `escalation_ticket`.", "When halt and escalation state occur at `engagement_attempt` and `escalation_ticket`, hostile-halt chronology occurs at `engagement_record`."],
        nodesTouched: ["intent_classification", "suppression_list", "send_gate_decision", "engagement_attempt", "escalation_ticket", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-18",
        title: "Escalation queue items land in Customer support Ticket queue with engagement context",
        implementationSource: "Runtime escalations now materialize as engagement-escalation tickets with thread, intent, owner, last-send, firm, and contact context.",
        start: "Starting from escalation_ticket (existing)",
        pathHops: ["When runtime escalation opening occurs at `escalation_ticket`, engagement-escalation typing occurs at `escalation_ticket`.", "When engagement-escalation typing occurs at `escalation_ticket`, support context is assembled at `support_context_bundle`.", "When support context assembly occurs at `support_context_bundle`, thread linkage occurs at `conversation_thread`.", "When the ticket and context are ready at `escalation_ticket`, ticket-opened chronology occurs at `engagement_record`.", "When escalation item work occurs at `escalation_ticket`, the same support queue item state occurs at `escalation_ticket`. idempotent"],
        nodesTouched: ["escalation_ticket", "support_context_bundle", "conversation_thread", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; Cost / FinOps.",
      },
{
        id: "agent-19",
        title: "Escalation policy carries priority, reason codes, and required context bundle",
        implementationSource: "Each escalation now carries reason, priority, verbatim reply, confidence, sequence, owner, pending attempt, and consent/silence snapshot.",
        start: "Starting from escalation_ticket (existing)",
        pathHops: ["When an escalation reason occurs at `escalation_ticket`, reason code and priority state occur at `escalation_ticket`.", "When reason and priority occur at `escalation_ticket`, reply and thread context gathering occurs at `support_context_bundle`.", "When context gathering occurs at `support_context_bundle`, confidence and class are gathered from `intent_classification`.", "When context gathering occurs at `support_context_bundle`, owner and pending attempt are gathered from `engagement_attempt`.", "When context gathering occurs at `support_context_bundle`, consent and gate posture are gathered from `consent_record` and `send_gate_decision`.", "When the bundle is complete at `support_context_bundle`, escalation audit state occurs at `audit_trail`."],
        nodesTouched: ["escalation_ticket", "support_context_bundle", "conversation_thread", "intent_classification", "engagement_attempt", "consent_record", "send_gate_decision", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-20",
        title: "Human resolution resumes, reclassifies, or closes \u2014 never silent drop",
        implementationSource: "Human handling now returns an explicit disposition that reclassifies, resumes, closes, or keeps suppression in durable runtime state.",
        start: "Starting from escalation_ticket (existing)",
        pathHops: ["When a human resolves an escalation at `escalation_ticket`, explicit disposition state occurs at `human_disposition`.", "When reclassify disposition occurs at `human_disposition`, corrected class state occurs at `intent_classification`.", "When resume disposition occurs at `human_disposition`, unpaused owner state occurs at `engagement_attempt`.", "When close disposition occurs at `human_disposition`, finishing predicate state occurs at `sequence_finish_state`.", "When keep-suppressed disposition occurs at `human_disposition`, continued deny state occurs at `suppression_list` and `send_gate_decision`.", "When any disposition changes runtime state at `human_disposition`, disposition chronology occurs at `engagement_record`."],
        nodesTouched: ["escalation_ticket", "human_disposition", "intent_classification", "engagement_attempt", "sequence_finish_state", "suppression_list", "send_gate_decision", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-21",
        title: "Conversations is the operator reply-triage surface; Engagement record stays chronology",
        implementationSource: "Conversations now owns live reply triage while Engagement record remains a read-only chronology of the same runtime events.",
        start: "Starting from conversation_thread (existing)",
        pathHops: ["When an inbound reply lands at `conversation_thread`, live triage state occurs at `conversation_thread`.", "When intent or escalation state change occurs at `intent_classification` or `escalation_ticket`, thread work state update occurs at `conversation_thread`.", "When an operator takes a triage action at `conversation_thread`, a closed runtime tool request occurs at `runtime_tool_invocation`.", "When a runtime tool request resolves at `runtime_tool_invocation`, immutable chronology occurs at `engagement_record`.", "When chronology occurs at `engagement_record`, no triage ownership changes occur at `engagement_record`. idempotent"],
        nodesTouched: ["conversation_thread", "intent_classification", "escalation_ticket", "runtime_tool_invocation", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-22",
        title: "Conversations triage shows intent, owner, gates, and pending attempt",
        implementationSource: "A Conversations thread now shows intent, owner, gate posture, pending or nullified attempt, and escalation status from the runtime's actual states.",
        start: "Starting from conversation_thread (existing)",
        pathHops: ["When a thread is opened for triage at `conversation_thread`, a context projection request occurs at `triage_context_snapshot`.", "When the projection request occurs at `triage_context_snapshot`, current class is read from `intent_classification`.", "When the projection request occurs at `triage_context_snapshot`, active owner and pending/nullified attempt are read from `engagement_attempt`.", "When the projection request occurs at `triage_context_snapshot`, gate posture is read from `send_gate_decision`.", "When the projection request occurs at `triage_context_snapshot`, escalation status is read from `escalation_ticket`.", "When context projection completion occurs at `triage_context_snapshot`, visible triage context change occurs at `conversation_thread`."],
        nodesTouched: ["conversation_thread", "triage_context_snapshot", "intent_classification", "engagement_attempt", "send_gate_decision", "escalation_ticket"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-23",
        title: "Engagement record must chronicle classifications, overrides, and tool acts",
        implementationSource: "Engagement record now appends inbound, classification, nullify, handoff, gate deny, escalation, disposition, and finish events without becoming the decision engine.",
        start: "Starting from messaging_event_stream (existing)",
        pathHops: ["When inbound or delivery evidence occurs at `messaging_event_stream`, event chronology occurs at `engagement_record`.", "When classification completes at `intent_classification`, classified-intent chronology occurs at `engagement_record`.", "When nullify or owner handoff occurs at `engagement_attempt`, runtime-transition chronology occurs at `engagement_record`.", "When gate deny occurs at `send_gate_decision`, gate-denied chronology occurs at `engagement_record`.", "When escalation opens or resolves at `escalation_ticket` or `human_disposition`, HITL chronology occurs at `engagement_record`.", "When a tool act resolves at `runtime_tool_invocation`, tool-act chronology occurs at `engagement_record` and audit state occurs at `audit_trail`.", "When sequence close or arm occurs at `sequence_finish_state` or `sequence_enrollment`, finish/armed chronology occurs at `engagement_record`."],
        nodesTouched: ["messaging_event_stream", "engagement_record", "intent_classification", "engagement_attempt", "send_gate_decision", "escalation_ticket", "human_disposition", "runtime_tool_invocation", "audit_trail", "sequence_finish_state", "sequence_enrollment"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "agent-24",
        title: "Runtime only runs while Firm operations bind is Armed/Active and enrolled",
        implementationSource: "The runtime now obeys firm bind posture: Armed can prepare, Active can speak, and unbound or posture-denied sends are blocked and recorded.",
        start: "Starting from firm_operations_bind (new)",
        pathHops: ["When a firm becomes bound at `firm_operations_bind`, eligible tenancy posture occurs at `firm_tenancy`.", "When a sequence is enrolled under the bound firm at `sequence_enrollment`, posture-scoped sequence state occurs at `sequence_instance`.", "When the posture is Armed at `firm_operations_bind`, prepare-only scheduling state can occur at `attempt_job_queue` but no contact-facing send occurs at `outbound_message`.", "When Active posture occurs at `firm_operations_bind`, contact-facing send eligibility occurs at `send_gate_decision`.", "When posture denies at `send_gate_decision`, blocked attempt state occurs at `engagement_attempt` and no send occurs at `outbound_message`.", "When inbound human-review need occurs at `conversation_thread`, escalation opening still occurs at `escalation_ticket` while not Active.", "When posture allows or blocks motion at `firm_operations_bind` or `send_gate_decision`, posture chronology occurs at `engagement_record`."],
        nodesTouched: ["firm_operations_bind", "firm_tenancy", "sequence_enrollment", "sequence_instance", "attempt_job_queue", "outbound_message", "send_gate_decision", "engagement_attempt", "conversation_thread", "escalation_ticket", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; External Systems.",
      }
    ],
  },
{
    id: "c3-eligibility-reference-pipeline",
    label: "C3 Eligibility evaluation + reference-data currency pipeline \u2014 paper Function pass",
    items: [
{
        id: "pipe-01",
        title: "Staged ingestion pipeline",
        implementationSource: "Reference data moves through fixed detect, fetch, parse, validate, draft, dual-check, publish, and consumer enqueue stages without a code deploy.",
        start: "Starting from `ingestion_run` (new)",
        pathHops: ["When a change candidate occurs at `ingestion_run`, staged run state occurs at `ingestion_run`. [stage order is fixed; failed stages remain draft-side]", "When fetch and parse completion occurs at `ingestion_run`, draft criteria snapshot creation occurs at `reference_table_version`. [draft is not published-current]", "When dual-check and publish handoff completion occurs at `ingestion_run`, immutable publish-state advancement occurs at `reference_table_version`. [only validated drafts can publish]", "When publish success occurs at `reference_table_version`, consumer work enqueue occurs at `re_score_job`. [enqueue is scoped by the later diff/impact path]", "When stage-state change occurs at `ingestion_run`, stage outcome append occurs at `audit_trail`. [append-only stage chronology]"],
        nodesTouched: ["ingestion_run", "reference_table_version", "re_score_job", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance",
      },
{
        id: "pipe-02",
        title: "Canonical source binding",
        implementationSource: "Each table family separates canonical source URLs from discovery alerts so only authoritative bindings can feed publishable data.",
        start: "Starting from `reference_source_binding` (new)",
        pathHops: ["When source binding registration occurs at `reference_source_binding`, canonical and discovery-only class storage occurs at `reference_source_binding`. [Canada.ca, ministerial instruction, or named domain source only for canonical]", "When discovery-only alert arrival occurs at `reference_source_binding`, detect-state opening occurs at `ingestion_run`. [alert cannot become publish input]", "When fetch start occurs at `ingestion_run`, source-class check occurs at `reference_source_binding`. [fetch-and-parse proceeds only from canonical bindings]", "When blocked source attempt occurs at `ingestion_run`, refusal provenance append occurs at `audit_trail`. [secondary source cannot promote to published-current]"],
        nodesTouched: ["reference_source_binding", "ingestion_run", "audit_trail"],
        facets: "External Systems; Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime",
      },
{
        id: "pipe-03",
        title: "Change-driven detect",
        implementationSource: "Scheduled canonical fetches compare content hashes and available HTTP validators so identical polls do not create drafts.",
        start: "Starting from `reference_detect_cursor` (new)",
        pathHops: ["When scheduled or alert-driven check occurs at `reference_detect_cursor`, canonical page-marker refresh occurs at `reference_detect_cursor`. [hash, ETag, and Last-Modified are stored when available]", "When marker divergence occurs at `reference_detect_cursor`, change-driven run opening occurs at `ingestion_run`. [run opens only for divergence or explicit alert path]", "When unchanged-marker verification occurs at `reference_detect_cursor`, verification-time update occurs at `reference_freshness_state`. [no duplicate draft version is created]", "When divergence-run opening occurs at `ingestion_run`, detect provenance append occurs at `audit_trail`. [source marker and retrieved time are recorded]"],
        nodesTouched: ["reference_detect_cursor", "ingestion_run", "reference_freshness_state", "audit_trail"],
        facets: "External Systems; Data Storage & Management; Infrastructure & Operations; Cost / FinOps",
      },
{
        id: "pipe-04",
        title: "Schema-contract validation",
        implementationSource: "Table-family contracts enforce required columns, types, cardinality, and sanity bands before a draft can reach dual-check.",
        start: "Starting from `schema_contract` (new)",
        pathHops: ["When contract-version attachment occurs at `schema_contract`, validation-state update occurs at `schema_contract`. [contract is versioned]", "When parsed-data landing occurs at `ingestion_run`, current-contract read occurs at `schema_contract`. [HTML shifts fail closed]", "When validation pass occurs at `ingestion_run`, draft snapshot advancement occurs at `reference_table_version`. [draft can enter dual-check]", "When validation failure occurs at `ingestion_run`, draft-hold state occurs at `reference_table_version` and failure-detail append occurs at `audit_trail`. [publish path stays blocked]"],
        nodesTouched: ["schema_contract", "ingestion_run", "reference_table_version", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations",
      },
{
        id: "pipe-05",
        title: "Immutable publish",
        implementationSource: "Published criteria are content-addressed and immutable; corrections produce a new version or explicit rollback publish.",
        start: "Starting from `reference_table_version` (existing)",
        pathHops: ["When publish request occurs at `reference_table_version`, draft content-hash binding occurs at `reference_table_version`. [published rows cannot be edited in place]", "When correction need occurs at `reference_table_version`, new draft lineage creation occurs at `reference_table_version`. [prior version remains readable]", "When correction publish occurs at `reference_table_version`, supersedes linkage record occurs at `reference_table_version`. [rollback is explicit publish, not mutation]", "When publish or correction completion occurs at `reference_table_version`, version-lineage append occurs at `audit_trail`. [pins resolve to the original bytes]"],
        nodesTouched: ["reference_table_version", "audit_trail"],
        facets: "Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime",
      },
{
        id: "pipe-06",
        title: "Provenance audit events",
        implementationSource: "Stage transitions and publish events write source, actor, attestation, effective date, supersedes id, content hash, and run id into audit state.",
        start: "Starting from `audit_trail` (existing)",
        pathHops: ["When ingestion-stage transition occurs at `ingestion_run`, provenance append occurs at `audit_trail`. [source URL, retrieved-at, actor, and run id are recorded]", "When publish request occurs at `reference_table_version`, required-provenance check occurs at `audit_trail`. [missing provenance blocks publish]", "When publish success occurs at `reference_table_version`, publish-provenance append occurs at `audit_trail`. [content hash, effective-from, supersedes id, and dual-check attestation are recorded]", "When version citation occurs at `client_score_snapshot`, cited-version resolution occurs at `audit_trail`. [score replay reaches source and actor]"],
        nodesTouched: ["ingestion_run", "reference_table_version", "client_score_snapshot", "audit_trail"],
        facets: "Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime",
      },
{
        id: "pipe-07",
        title: "Separation of duties",
        implementationSource: "Dual-check requires a distinct checker or independent checksum signature before publish can unlock.",
        start: "Starting from `ingestion_run` (new)",
        pathHops: ["When importer draft advancement occurs at `ingestion_run`, importer actor-state write occurs at `ingestion_run`. [actor identity is preserved]", "When dual-check attempt occurs at `ingestion_run`, checker-to-importer comparison occurs at `ingestion_run`. [same-actor approval is refused unless an incident flag is present]", "When independent checksum attestation occurs at `ingestion_run`, dual-check state advancement occurs at `ingestion_run`. [checksum signature substitutes for second human only where allowed]", "When dual-check pass occurs at `ingestion_run`, publish eligibility unlock occurs at `reference_table_version` and attestation append occurs at `audit_trail`. [publish remains gated by attestation]"],
        nodesTouched: ["ingestion_run", "reference_table_version", "audit_trail"],
        facets: "Identity / Security / Compliance; Core Application & Runtime; Data Storage & Management",
      },
{
        id: "pipe-08",
        title: "Atomic publish groups",
        implementationSource: "Co-dependent table families publish together under a group id so packs never see half of a ministerial change.",
        start: "Starting from `publish_group` (new)",
        pathHops: ["When co-dependent family declaration occurs at `publish_group`, member table-family binding occurs at `publish_group`. [category predicates and membership can share a group]", "When member draft validation occurs at `reference_table_version`, readiness reflection occurs at `publish_group`. [all members must be ready]", "When member validation failure occurs at `reference_table_version`, group draft-hold state occurs at `publish_group`. [no partial current state]", "When all-member pass occurs at `publish_group`, atomic member-version advancement occurs at `reference_table_version`. [one publish-group id covers the set]", "When group publish completion occurs at `reference_table_version`, group-lineage append occurs at `audit_trail`. [evaluation pins never point at a partial group]"],
        nodesTouched: ["publish_group", "reference_table_version", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Infrastructure & Operations",
      },
{
        id: "pipe-09",
        title: "Structured version diff",
        implementationSource: "Publish success computes added, removed, and changed rows or fields by table-family change class.",
        start: "Starting from `reference_diff` (existing)",
        pathHops: ["When superseding-version publish occurs at `reference_table_version`, version-pair diff creation occurs at `reference_diff`. [diff is tied to both version ids]", "When row or field difference occurs at `reference_diff`, change-class typing occurs at `reference_diff`. [floor, membership, predicate, grid cell, or equivalent class]", "When diff computation completion occurs at `reference_diff`, diff-id attachment occurs at `reference_table_version`. [diff sits beside the immutable version]", "When publish-event logging occurs at `audit_trail`, diff-id append occurs at `audit_trail`. [operators read machine diff, not raw eyeballing]"],
        nodesTouched: ["reference_table_version", "reference_diff", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Identity / Security / Compliance",
      },
{
        id: "pipe-10",
        title: "Diff-to-cohort scope",
        implementationSource: "Each diff change class maps deterministically to the bound firms and packs that consume affected table keys.",
        start: "Starting from `reference_diff` (existing)",
        pathHops: ["When typed diff availability occurs at `reference_diff`, affected-key resolution occurs at `reference_diff`. [scope comes from structured change class]", "When affected-key resolution occurs at `reference_diff`, consuming pack-reference read occurs at `evaluation_pack`. [packs declare table-key usage]", "When consuming-pack match occurs at `evaluation_pack`, tenant membership resolution occurs at `firm_tenancy`. [only bound firms enter scope]", "When tenant-scoped consumer resolution occurs at `firm_tenancy`, impact-scoped job enqueue occurs at `re_score_job`. [book-wide work is avoided unless the diff truly requires it]"],
        nodesTouched: ["reference_diff", "evaluation_pack", "firm_tenancy", "re_score_job"],
        facets: "Core Application & Runtime; Data Storage & Management; Cost / FinOps; Infrastructure & Operations",
      },
{
        id: "pipe-11",
        title: "Bind and pin cohort modes",
        implementationSource: "Active follow-current, armed-only, and pinned packs all compute signals with different adoption behavior.",
        start: "Starting from `firm_tenancy` (existing)",
        pathHops: ["When impact-scope arrival occurs at `firm_tenancy`, firm bind-state read occurs at `firm_tenancy`. [Active and armed-only are separate modes]", "When pack-policy read occurs at `evaluation_pack`, follow-current or pin-with-expiry state application occurs at `evaluation_pack`. [pins are not silently flipped]", "When Active follow-current application occurs at `evaluation_pack`, apply-mode work enqueue occurs at `re_score_job`. [current version can be adopted]", "When armed-only or pinned-shadow application occurs at `evaluation_pack`, signal-store or shadow-diff work enqueue occurs at `re_score_job`. [motion remains separate from detection]", "When mode choice occurs at `re_score_job`, mode-provenance append occurs at `audit_trail`. [operators can explain why a result did or did not adopt current]"],
        nodesTouched: ["firm_tenancy", "evaluation_pack", "re_score_job", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance",
      },
{
        id: "pipe-12",
        title: "Publish-triggered re-score jobs",
        implementationSource: "Publish creates durable re-evaluation jobs with cause codes and emits was/now deltas against prior pinned results.",
        start: "Starting from `re_score_job` (existing)",
        pathHops: ["When publish completion occurs at `reference_table_version`, impact cohort work creation occurs at `re_score_job`. [cause is reference-publish or reference-correction]", "When job run occurs at `re_score_job`, pack-policy and adoption read occurs at `evaluation_pack`. [pin and follow-current policy govern the version used]", "When evaluation completion occurs at `re_score_job`, prior/current result write occurs at `client_score_snapshot`. [was/now delta preserves previous pins]", "When score snapshot change occurs at `client_score_snapshot`, cause and version append occurs at `audit_trail`. [Board and Analysis updates are publish-attributable]"],
        nodesTouched: ["reference_table_version", "re_score_job", "evaluation_pack", "client_score_snapshot", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations",
      },
{
        id: "pipe-13",
        title: "Signals only on reference publish",
        implementationSource: "Reference-driven re-scores update detection signals and Board phase state but cannot auto-enroll outreach.",
        start: "Starting from `client_score_snapshot` (existing)",
        pathHops: ["When reference-publish result write occurs at `client_score_snapshot`, eligibility and delta signal storage occurs at `client_score_snapshot`. [cause remains reference-publish]", "When signal materialization occurs at `client_score_snapshot`, phase signal-state update occurs at `board_phase_signal`. [detection becomes visible]", "When reference-publish cause arrival occurs at `firm_tenancy`, auto-enrollment refusal occurs at `firm_tenancy`. [motion requires a separate runner decision]", "When outbound motion consideration occurs at `send_gate_decision`, send-authority denial occurs at `send_gate_decision`. [signal does not become send authority]", "When refusal or signal write occurs at `client_score_snapshot`, provenance append occurs at `audit_trail`. [decoupling is reviewable]"],
        nodesTouched: ["client_score_snapshot", "board_phase_signal", "firm_tenancy", "send_gate_decision", "audit_trail"],
        facets: "Core Application & Runtime; Identity / Security / Compliance; Data Storage & Management",
      },
{
        id: "pipe-14",
        title: "Diff stays outside rule bodies",
        implementationSource: "Version diffs appear as read-only reference changelogs while pack rules link to table keys and reject embedded public-criteria literals.",
        start: "Starting from `evaluation_pack` (new)",
        pathHops: ["When reference-impact opening occurs at `evaluation_pack`, changelog read occurs at `reference_diff`. [diff is read-only input context]", "When pack assertion edit occurs at `evaluation_pack`, table-key and version-pin reference storage occurs at `evaluation_pack`. [public criteria values live in reference data]", "When pack review run occurs at `evaluation_pack`, public-criteria literal comparison occurs at `reference_table_version`. [matching literals are rejected from rule bodies]", "When review outcome occurs at `evaluation_pack`, review-outcome append occurs at `audit_trail`. [Directive 3 enforcement is traceable]"],
        nodesTouched: ["evaluation_pack", "reference_diff", "reference_table_version", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance",
      },
{
        id: "pipe-15",
        title: "Fact write-back re-score trigger",
        implementationSource: "Client Data commits emit fact-change events that immediately re-score the contact against bound packs and current or pinned reference versions.",
        start: "Starting from `client_data_record` (new)",
        pathHops: ["When form or consultant write-back commit occurs at `client_data_record`, fact-change event record occurs at `client_data_record`. [commit is the trigger, not page view]", "When fact-change event occurs at `client_data_record`, bound pack and tenant-context read occurs at `firm_tenancy`. [only the contact's firm scope is touched]", "When bound pack resolution occurs at `firm_tenancy`, immediate per-contact work enqueue occurs at `re_score_job`. [cause is fact-write-back]", "When job completion occurs at `re_score_job`, refreshed result write occurs at `client_score_snapshot`. [current or pinned reference policy is preserved]", "When score snapshot update occurs at `client_score_snapshot`, phase signal-state update occurs at `board_phase_signal` and cause append occurs at `audit_trail`. [Board-visible without batch folklore]"],
        nodesTouched: ["client_data_record", "firm_tenancy", "re_score_job", "client_score_snapshot", "board_phase_signal", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations",
      },
{
        id: "pipe-16",
        title: "Concurrent re-score coalescing",
        implementationSource: "Concurrent fact and reference triggers merge by contact and event id so one result can carry both causes and exact reference versions.",
        start: "Starting from `re_score_job` (existing)",
        pathHops: ["When same-contact trigger collision occurs at `re_score_job`, idempotency-key comparison occurs at `re_score_job`. [key includes contact id and trigger event id]", "When merge-window match occurs at `re_score_job`, latest fact and version-policy read occurs at `client_data_record` and `evaluation_pack`. [latest fact snapshot, declared reference policy]", "When merged run completion occurs at `re_score_job`, multi-cause result write occurs at `client_score_snapshot`. [both trigger ids and reference versions are preserved]", "When result write occurs at `client_score_snapshot`, all-cause append occurs at `audit_trail`. [no duplicate signals and no lost provenance]"],
        nodesTouched: ["re_score_job", "client_data_record", "evaluation_pack", "client_score_snapshot", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations",
      },
{
        id: "pipe-17",
        title: "Board phase signal contract",
        implementationSource: "Re-score outcomes materialize motion class, confidence tier, and version pins onto Board-grade phase signal state without determination language.",
        start: "Starting from `board_phase_signal` (new)",
        pathHops: ["When score result write occurs at `client_score_snapshot`, motion class and confidence-tier derivation occurs at `board_phase_signal`. [reactivation-worthy, nudge-only, or none]", "When phase signal update occurs at `board_phase_signal`, pack and reference version-pin copy occurs at `board_phase_signal`. [signal is explainable at list grade]", "When signal wording storage occurs at `board_phase_signal`, determination-language exclusion occurs at `board_phase_signal`. [service posture only]", "When signal change occurs at `board_phase_signal`, change-provenance append occurs at `audit_trail`. [consultants can open the version trail]"],
        nodesTouched: ["client_score_snapshot", "board_phase_signal", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance",
        missingSeatFlag: "PM owns Board presentation; this trace wires the signal state and pins.",
      },
{
        id: "pipe-18",
        title: "Reachability before motion",
        implementationSource: "Eligibility re-score signals stay separate from Book readiness reachability so unreachable contacts do not enter sequences.",
        start: "Starting from `book_readiness` (existing)",
        pathHops: ["When reactivation-worthy score occurs at `client_score_snapshot`, detection signal-state update occurs at `board_phase_signal`. [eligibility is detection]", "When enrollment consideration occurs at `firm_tenancy`, reachable-verdict read occurs at `book_readiness`. [reachability is a separate axis]", "When unreachable verdict occurs at `book_readiness`, enrollment-authority denial occurs at `send_gate_decision`. [invalid, silenced, or unconsented contact cannot enter motion]", "When reachability change occurs at `book_readiness`, eligibility signal retention occurs at `board_phase_signal`. [readiness can recover without recomputing eligibility]", "When gate decision occurs at `send_gate_decision`, decision-provenance append occurs at `audit_trail`. [signal and reachability are both reviewable]"],
        nodesTouched: ["client_score_snapshot", "board_phase_signal", "firm_tenancy", "book_readiness", "send_gate_decision", "audit_trail"],
        facets: "Core Application & Runtime; Identity / Security / Compliance; Data Storage & Management",
      },
{
        id: "pipe-19",
        title: "Volatility classes",
        implementationSource: "Reference table families carry high, medium, or low volatility classes that drive detect cadence and source re-resolve behavior.",
        start: "Starting from `reference_freshness_state` (new)",
        pathHops: ["When table-family configuration occurs at `reference_freshness_state`, volatility-class storage occurs at `reference_freshness_state`. [high, medium, or low from house matrix]", "When volatility-class change occurs at `reference_freshness_state`, cadence input update occurs at `recheck_cadence`. [cadence follows class, not one fleet cron]", "When low-volatility funds assertion occurs at `reference_freshness_state`, source URL re-resolve requirement update occurs at `reference_source_binding`. [URL is verified each cycle when asserted]", "When class or re-resolve state change occurs at `reference_freshness_state`, configuration-change append occurs at `audit_trail`. [freshness policy is reviewable]"],
        nodesTouched: ["reference_freshness_state", "recheck_cadence", "reference_source_binding", "audit_trail"],
        facets: "Data Storage & Management; External Systems; Infrastructure & Operations; Cost / FinOps",
      },
{
        id: "pipe-20",
        title: "Freshness gates for packs",
        implementationSource: "Packs can require published-current and not-past-freshness before draw or category assertions evaluate.",
        start: "Starting from `reference_freshness_state` (new)",
        pathHops: ["When detect or verify success occurs at `reference_detect_cursor`, last-verified and freshness deadline update occurs at `reference_freshness_state`. [deadline follows volatility SLA]", "When deadline miss occurs at `reference_freshness_state`, past-freshness flag update occurs at `reference_freshness_state`. [staleness is data, not prose]", "When public-criteria evaluation read occurs at `evaluation_pack`, published-current read occurs at `reference_table_version` and freshness read occurs at `reference_freshness_state`. [both must pass for freshness-gated assertions]", "When freshness failure occurs at `evaluation_pack`, unavailable-reference result write occurs at `client_score_snapshot`. [draw/category reactivation path suppresses]", "When freshness-gated evaluation write occurs at `client_score_snapshot`, gate-outcome append occurs at `audit_trail`. [stale mirror decisions are explainable]"],
        nodesTouched: ["reference_detect_cursor", "reference_freshness_state", "evaluation_pack", "reference_table_version", "client_score_snapshot", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance",
      },
{
        id: "pipe-21",
        title: "Recheck cadence scheduler",
        implementationSource: "House-visible cadence state tracks next-due, last-verified, and SLA breach alerts by volatility class.",
        start: "Starting from `recheck_cadence` (new)",
        pathHops: ["When volatility-class set occurs at `reference_freshness_state`, next-due schedule computation occurs at `recheck_cadence`. [high at least daily plus alert path; medium scheduled verify plus hash-change; low quarterly plus hash-change]", "When next-due arrival occurs at `recheck_cadence`, detect or verify work update occurs at `reference_detect_cursor`. [cadence drives source checks]", "When verify success occurs at `reference_detect_cursor`, last-verified update occurs at `reference_freshness_state` and next-due advancement occurs at `recheck_cadence`. [class deadline is refreshed]", "When due-window miss occurs at `recheck_cadence`, breach-state append occurs at `audit_trail`. [house can see missed SLA]"],
        nodesTouched: ["reference_freshness_state", "recheck_cadence", "reference_detect_cursor", "audit_trail"],
        facets: "Infrastructure & Operations; External Systems; Data Storage & Management; Core Application & Runtime",
      },
{
        id: "pipe-22",
        title: "Continuous reconciliation",
        implementationSource: "Reconciliation refetches canonical snapshots, compares them with published hashes, samples flipped evaluations, and logs the pipeline end to end.",
        start: "Starting from `reconciliation_job` (new)",
        pathHops: ["When reconciliation due event occurs at `reconciliation_job`, canonical source-binding read occurs at `reference_source_binding`. [only canonical snapshots are reconciled]", "When canonical snapshot fetch occurs at `reconciliation_job`, live-to-published hash comparison occurs at `reference_table_version`. [drift is measured against immutable current]", "When drift detection occurs at `reconciliation_job`, alert-state opening occurs at `reconciliation_job` and provenance append occurs at `audit_trail`. [post-publish drift becomes house-visible]", "When last-publish impact sample occurs at `reconciliation_job`, flipped-result read occurs at `client_score_snapshot`. [sample focuses on contacts whose signals changed]", "When sampled replay completion occurs at `reconciliation_job`, reconciliation-outcome append occurs at `audit_trail`. [ingestion, publish, re-score, and reconciliation share one traceable spine]"],
        nodesTouched: ["reconciliation_job", "reference_source_binding", "reference_table_version", "client_score_snapshot", "audit_trail"],
        facets: "External Systems; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance",
      }
    ],
  },
{
    id: "c4-book-ingestion-identity",
    label: "C4 \u2014 Book ingestion, normalization & identity resolution",
    items: [
{
        id: "ingest-01",
        title: "Source header inventory before mapping",
        implementationSource: "Imports records the actual arrived headers, sample cells, and provenance before any mapping or contact mutation can proceed.",
        start: "Starting from import_batch (existing)",
        pathHops: ["When a file or CRM export lands at `import_batch`, `source_header_inventory` records header names, sample values, source label, file digest, and landed-at timestamp. [idempotent by batch plus file digest]", "When `source_header_inventory` is committed, `field_mapping` initializes candidate columns from the captured inventory rather than from assumed schema names.", "When `field_mapping` requests contact mutation, `contact_identity` accepts only rows carrying the inventory reference for that `import_batch`.", "When the inventory gate passes or blocks mutation, `audit_trail` appends the provenance stamp and gate result."],
        nodesTouched: ["import_batch", "source_header_inventory", "field_mapping", "contact_identity", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-02",
        title: "Canonical contact vocabulary vs firm labels",
        implementationSource: "Source columns first map into a canonical contact layer so Q-ID facts do not absorb directory channels or opaque firm labels.",
        start: "Starting from field_mapping (existing)",
        pathHops: ["When mapped column decisions are saved at `field_mapping`, `canonical_contact_record` receives identity fields, display labels, channel candidates, suppression-shaped fields, and opaque attributes as separate contact-layer slots.", "When `canonical_contact_record` changes, `opaque_attribute_store` retains fields that are not canonical slots and not evaluable Q-ID candidates.", "When canonical rows are staged, `contact_identity` resolves people from the canonical layer rather than raw header strings.", "When `book_readiness` evaluates a batch, it reads canonical contact completeness and channel candidates instead of source column names.", "When canonicalization completes, `audit_trail` appends the contact vocabulary version used for the batch."],
        nodesTouched: ["field_mapping", "canonical_contact_record", "opaque_attribute_store", "contact_identity", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-03",
        title: "Field to Q-ID mapping, partial to complete",
        implementationSource: "Each land batch carries an explicit source-column to Q-ID map version with transform and confidence before evaluable facts are attached.",
        start: "Starting from field_mapping (existing)",
        pathHops: ["When a Q-ID mapping decision is saved at `field_mapping`, the map version records source column, Q-ID target, transform choice, confidence, and unmapped state.", "When `field_mapping` publishes the map version, `import_batch` binds that version to the batch before Q-ID facts can land.", "When mapped values are produced, `fact_lineage` records batch id, map version, raw value, transform id, and normalized value.", "When `book_readiness` starts an audit read, it consumes only Q-ID facts with a batch-bound `field_mapping` version and leaves unmapped evaluable candidates outside the fact set.", "When the map version is used, `audit_trail` appends the version id and publish event."],
        nodesTouched: ["field_mapping", "import_batch", "fact_lineage", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-04",
        title: "Unmapped columns preserve, do not invent",
        implementationSource: "Unmapped columns persist as opaque contact attributes without minting Q-IDs or discarding firm-supplied data.",
        start: "Starting from field_mapping (existing)",
        pathHops: ["When a column is left unmapped at `field_mapping`, `opaque_attribute_store` receives the raw header, raw value, batch id, and column provenance.", "When `opaque_attribute_store` updates, `canonical_contact_record` exposes the attribute as retained non-evaluable firm data for the contact.", "When a later mapping decision promotes an opaque attribute, `field_mapping` creates a new map version instead of rewriting the old batch decision.", "When promoted values become Q-ID facts, `fact_lineage` links the new mapped value back to the preserved opaque source.", "When preserve or promote events occur, `audit_trail` appends the non-evaluable or promoted state transition."],
        nodesTouched: ["field_mapping", "opaque_attribute_store", "canonical_contact_record", "fact_lineage", "audit_trail"],
        facets: "Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime.",
      },
{
        id: "ingest-05",
        title: "Value normalization transforms",
        implementationSource: "A fixed staging transform set cleans values before identity matching and validation classes run.",
        start: "Starting from import_batch (existing)",
        pathHops: ["When source rows are accepted at `import_batch`, `normalized_contact_staging` records trim, Unicode normalization, empty-sentinel-to-null, email case policy, phone parse attempt, and date or scalar casts.", "When `normalized_contact_staging` finishes a row, `canonical_contact_record` receives cleaned display, channel, suppression, and opaque slots while raw source remains linked to the batch.", "When canonical rows carry normalized channel candidates, `contact_identity` computes match keys from the staged values only.", "When validation is requested, `reachability_class` reads staged email and phone outcomes rather than raw free text.", "When staging completes, `audit_trail` appends the transform set id used by the batch."],
        nodesTouched: ["import_batch", "normalized_contact_staging", "canonical_contact_record", "contact_identity", "reachability_class", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-06",
        title: "Email string hygiene",
        implementationSource: "Imports classifies email strings as syntax-invalid or channel candidates before downstream deliverability checks consume them.",
        start: "Starting from normalized_contact_staging (new)",
        pathHops: ["When an email-like value is normalized at `normalized_contact_staging`, `reachability_class` receives syntax-shape, length, forbidden whitespace, and address-part hygiene outcomes.", "When syntax hygiene fails, `canonical_contact_record` marks the email slot syntax-invalid and keeps the raw value in provenance without treating it as a usable channel candidate.", "When syntax hygiene passes, `canonical_contact_record` marks the value as an email channel candidate for identity and later validation-class checks.", "When `book_readiness` counts reachable rows, it excludes syntax-invalid emails and sends survivors to the validation ladder.", "When hygiene outcomes are assigned, `audit_trail` appends the class decision for the batch row."],
        nodesTouched: ["normalized_contact_staging", "reachability_class", "canonical_contact_record", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-07",
        title: "Phone normalization",
        implementationSource: "Phone candidates parse with firm/default region, store E.164 on success, and retain raw values on parse failure.",
        start: "Starting from normalized_contact_staging (new)",
        pathHops: ["When a phone value is staged at `normalized_contact_staging`, `firm_tenancy` supplies the firm or batch default region for national-number parsing.", "When parsing succeeds, `canonical_contact_record` stores E.164 and the parse-valid outcome as the phone channel candidate.", "When parsing fails, `canonical_contact_record` keeps the raw value and records a parse-failed outcome without pretending the value is E.164.", "When phone match keys are computed, `contact_identity` uses E.164 successes and skips parse-failed raw strings as deterministic phone keys.", "When phone validation later runs, `reachability_class` starts from the parse outcome instead of re-reading unparsed text.", "When parse outcomes are assigned, `audit_trail` appends default region and parse result."],
        nodesTouched: ["normalized_contact_staging", "firm_tenancy", "canonical_contact_record", "contact_identity", "reachability_class", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-08",
        title: "Display name vs structured name parts",
        implementationSource: "Contacts require a display name while structured given/family parts remain optional unless the source actually supplies them.",
        start: "Starting from canonical_contact_record (new)",
        pathHops: ["When name columns normalize into `canonical_contact_record`, the record chooses a display name from full name or composed supplied parts.", "When only a display name exists, `contact_identity` keeps given and family names absent rather than inventing split structure.", "When supplied structured parts exist, `contact_identity` stores them as optional identity signals beside the display name.", "When `book_readiness` checks name-present, it reads display name presence and does not require invented structured parts.", "When name construction completes, `audit_trail` appends whether the display name was supplied directly or composed from source columns."],
        nodesTouched: ["canonical_contact_record", "contact_identity", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-09",
        title: "Mapping confidence and human confirm",
        implementationSource: "High-confidence suggestions may prefill maps, while medium or low confidence mappings require confirmation before publish.",
        start: "Starting from field_mapping (existing)",
        pathHops: ["When source headers and samples reach `field_mapping`, the mapping state records confidence per proposed canonical or Q-ID target.", "When all proposed targets are high confidence, `field_mapping` may prefill the map version but still records the auto-suggest basis.", "When any proposed target is medium or low confidence, `field_mapping` holds the map version in confirm-required state.", "When confirmation is missing, `import_batch` cannot publish an ingestion-complete state for that batch.", "When confirmation lands, `book_readiness` can read the published map version; `audit_trail` appends the confirming actor and changed columns."],
        nodesTouched: ["field_mapping", "import_batch", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-10",
        title: "Deterministic match-key identity",
        implementationSource: "V1 identity resolves by normalized email, then E.164 phone, then source-row id, producing a stable Tower contact id.",
        start: "Starting from contact_identity (existing)",
        pathHops: ["When normalized canonical rows arrive at `contact_identity`, the identity state searches deterministic keys in order: normalized email, E.164 phone, then source-row id scoped to batch provenance.", "When a deterministic key matches a living person in `firm_tenancy`, `contact_identity` assigns the existing stable contact id and records an upsert decision.", "When no deterministic key matches, `contact_identity` creates a stable contact id within the firm tenancy and records an insert decision.", "When duplicate keys collide inside the same batch, `identity_review_cluster` receives the affected rows for review instead of silently merging them.", "When `book_readiness` displays duplicate outcomes, it reads the same `contact_identity` spine rather than inventing a second dedupe key.", "When insert, upsert, or review decisions occur, `audit_trail` appends the match-key class used."],
        nodesTouched: ["contact_identity", "firm_tenancy", "identity_review_cluster", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-11",
        title: "Probabilistic and secondary identity signals",
        implementationSource: "Scored same-person hints become review clusters only and never auto-merge without deterministic evidence or firm confirmation.",
        start: "Starting from contact_identity (existing)",
        pathHops: ["When deterministic keys are absent, weak, or oddly colliding at `contact_identity`, `identity_review_cluster` receives scored secondary signals such as display name similarity, partial channel overlap, and provenance proximity.", "When an `identity_review_cluster` is created, `contact_identity` keeps candidate contacts separate until confirm-merge or keep-separate is recorded.", "When a firm or operator confirms merge, `merge_survivorship_policy` receives the member contacts for survivor calculation.", "When keep-separate is recorded, `contact_identity` preserves separate stable contact ids and marks the cluster resolved.", "When `book_readiness` reads unresolved clusters, it reports data-validity outcomes for those rows without blocking unrelated reachable rows.", "When cluster state changes, `audit_trail` appends score inputs and the review decision."],
        nodesTouched: ["contact_identity", "identity_review_cluster", "merge_survivorship_policy", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Cost / FinOps.",
      },
{
        id: "ingest-12",
        title: "Merge survivor and field-level survivorship",
        implementationSource: "Confirmed same-person merges use explicit field survivor rules so channels and firm suppressions are not erased by last-write-wins.",
        start: "Starting from merge_survivorship_policy (new)",
        pathHops: ["When confirm-merge lands on `identity_review_cluster`, `merge_survivorship_policy` calculates survivor values by non-empty channels, suppression OR, newest provenance for conflicting scalars, and opaque-attribute union.", "When survivor calculation completes, `contact_identity` updates the stable contact id with the selected survivor fields and retains merged provenance.", "When suppression flags are OR-merged, `suppression_list` receives the halt signal without downgrading it because one source was blank.", "When upsert re-land hits the same identity later, `contact_identity` invokes the same `merge_survivorship_policy` instead of overwriting channels by arrival order.", "When `book_readiness` evaluates the merged contact, it reads the survivor channel set and suppression state.", "When survivorship applies, `audit_trail` appends the losing values, survivor values, and rule version."],
        nodesTouched: ["identity_review_cluster", "merge_survivorship_policy", "contact_identity", "suppression_list", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-13",
        title: "Already in Tower across batches",
        implementationSource: "Each new batch matches against the living firm book before commit and classifies rows as insert, upsert, or duplicate-review.",
        start: "Starting from import_batch (existing)",
        pathHops: ["When a normalized batch reaches commit preparation at `import_batch`, `firm_tenancy` scopes the lookup to the living contacts owned by that firm.", "When the firm-scoped lookup runs, `contact_identity` compares deterministic keys against existing contact ids before inserting any new identity.", "When one existing identity matches cleanly, `contact_identity` marks the row upsert; when none matches, it marks insert.", "When multiple existing identities or conflicting keys match, `identity_review_cluster` receives duplicate-review state before commit.", "When `book_readiness` later checks already-in-Tower, it reads the import-time insert/upsert/review decision as the primary classification.", "When row classes are assigned, `audit_trail` appends the classification counts for the batch."],
        nodesTouched: ["import_batch", "firm_tenancy", "contact_identity", "identity_review_cluster", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-14",
        title: "Conflicting channel facts on same identity",
        implementationSource: "Different email or phone values for an existing contact become channel conflicts pending accept, reject, or keep-both.",
        start: "Starting from contact_identity (existing)",
        pathHops: ["When an upsert at `contact_identity` brings a channel value that differs from the living contact, `channel_conflict` records existing value, incoming value, source provenance, and affected channel type.", "When `channel_conflict` is open, `contact_identity` keeps the stable contact id but does not replace the armed channel silently.", "When accept-new is recorded, `merge_survivorship_policy` promotes the incoming channel and preserves the replaced value in provenance.", "When keep-existing or keep-both is recorded, `contact_identity` keeps the chosen candidate set for later reachability checks.", "When `book_readiness` sees an open conflict on the channel intended for outreach, it holds sequence-ready for that row until the conflict resolves.", "When conflict state changes, `audit_trail` appends the selected action and actor."],
        nodesTouched: ["contact_identity", "channel_conflict", "merge_survivorship_policy", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-15",
        title: "Email validation-class checks",
        implementationSource: "Email candidates run through a named class ladder that maps outcomes to reachable, partial, or unreachable without hard-coding a vendor.",
        start: "Starting from reachability_class (existing)",
        pathHops: ["When `book_readiness` requests email validation for syntax-surviving candidates, `reachability_class` creates an email class run for syntax, domain/MX-class, and mailbox/deliverability-class when that check is wired.", "When syntax class fails, `reachability_class` marks the email unreachable and returns the reason to `book_readiness`.", "When syntax passes but domain/MX or mailbox class is pending or partial, `reachability_class` marks the candidate partial rather than fully reachable.", "When the highest available class passes, `reachability_class` marks the candidate reachable for the intended email channel.", "When `book_readiness` builds the verdict list, it maps class outcomes to reachable, partial, and unreachable row states while preserving the candidate on the contact.", "When validation classes are assigned, `audit_trail` appends class name, outcome, and provider-agnostic evidence."],
        nodesTouched: ["book_readiness", "reachability_class", "canonical_contact_record", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "ingest-16",
        title: "Phone validation-class checks",
        implementationSource: "Phone candidates run parse-valid, line-type, and reachability classes before SMS or voice use.",
        start: "Starting from reachability_class (existing)",
        pathHops: ["When `book_readiness` requests phone validation for parsed candidates, `reachability_class` creates a phone class run for parse-valid, line-type-class, and reachability-class when wired.", "When parse-valid is false, `reachability_class` marks the phone unreachable for sequence use and keeps the raw candidate on the contact.", "When line-type-class is incompatible with the intended SMS or voice channel, `reachability_class` marks the row partial or unreachable for that channel.", "When reachability-class is pending, `book_readiness` keeps validation explicitly pending instead of counting the row as sequence-ready.", "When class outcomes are available, `book_readiness` maps them to the verdict list while `contact_identity` retains the phone candidate for identity history.", "When phone classes are assigned, `audit_trail` appends class name, outcome, and channel intent."],
        nodesTouched: ["book_readiness", "reachability_class", "contact_identity", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "ingest-17",
        title: "Land to Audit contract",
        implementationSource: "Audit-eligible batches require normalized canonical rows, match keys, a saved map version, and explicit validation-class status.",
        start: "Starting from import_batch (existing)",
        pathHops: ["When row processing finishes at `import_batch`, the batch asks `normalized_contact_staging` whether every committed row has a transform outcome.", "When staging is complete, `field_mapping` confirms a saved map version for canonical and Q-ID decisions.", "When mapping is saved, `contact_identity` confirms deterministic match keys or review-cluster outcomes for committed rows.", "When identity is resolved or explicitly under review, `reachability_class` records validation-class status as runnable, pending, partial, or failed.", "When all required states exist, `book_readiness` marks the batch ingestion-complete and eligible for Audit run.", "When any required state is missing, `book_readiness` refuses Audit start for that batch and `audit_trail` appends the missing contract state."],
        nodesTouched: ["import_batch", "normalized_contact_staging", "field_mapping", "contact_identity", "reachability_class", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "ingest-18",
        title: "Row quarantine vs batch fail-closed",
        implementationSource: "Valid rows can commit while failed rows move to quarantine with reason codes, and only committed rows count for readiness.",
        start: "Starting from import_batch (existing)",
        pathHops: ["When row checks finish at `import_batch`, rows with blocking failures create `quarantine_row` entries carrying reason codes such as syntax, missing channel, map reject, or identity collision.", "When at least one row satisfies the ingestion contract, `import_batch` commits valid rows to `canonical_contact_record` and `contact_identity`.", "When no row satisfies the ingestion contract, `book_readiness` receives a batch-not-eligible state rather than a misleading partial ready count.", "When `quarantine_row` entries exist, `book_readiness` excludes them from reachable counts and verdict-ready rows.", "When a quarantined row is repaired, it re-enters `import_batch` through the same mapping, staging, identity, and validation path.", "When quarantine or repair occurs, `audit_trail` appends reason code, row reference, and transition."],
        nodesTouched: ["import_batch", "quarantine_row", "canonical_contact_record", "contact_identity", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "ingest-19",
        title: "Suppression and do-not-contact pass-through",
        implementationSource: "Suppression-shaped columns pass through as opaque halt flags for readiness without making legal consent judgments.",
        start: "Starting from field_mapping (existing)",
        pathHops: ["When source columns are mapped at `field_mapping`, suppression-shaped fields are classified as opaque suppression flags rather than Q-ID facts or legal consent outcomes.", "When suppression flags land in `canonical_contact_record`, `suppression_list` receives pass-through halt entries scoped to the contact and firm provenance.", "When `merge_survivorship_policy` combines duplicate contacts, suppression flags OR together so one blank source cannot erase a halt signal.", "When `book_readiness` evaluates outreach eligibility, it treats pass-through suppression as a data-validity halt without asserting CASL lawfulness.", "When suppression pass-through lands, `audit_trail` appends the source column, batch, and contact affected."],
        nodesTouched: ["field_mapping", "canonical_contact_record", "suppression_list", "merge_survivorship_policy", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-20",
        title: "Provenance and map lineage for evaluable facts",
        implementationSource: "Every imported Q-ID value retains source system, batch id, map version, raw value, transform id, normalized value, and landed-at time.",
        start: "Starting from fact_lineage (new)",
        pathHops: ["When `field_mapping` produces a mapped Q-ID value, `fact_lineage` records source system, batch id, map version, raw value, transform id, normalized value, and landed-at timestamp.", "When the Q-ID value attaches to a contact, `contact_identity` links the fact lineage to the stable contact id without upgrading it to client-asserted truth.", "When `book_readiness` or an audit read consumes the fact, it reads imported-fact provenance class from `fact_lineage`.", "When a map is changed later, `field_mapping` creates a new version and `fact_lineage` preserves the old lineage rather than overwriting historical facts.", "When lineage is written or read for readiness, `audit_trail` appends source, batch, map version, and contact id."],
        nodesTouched: ["field_mapping", "fact_lineage", "contact_identity", "book_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "ingest-21",
        title: "DEFERRED KU #7: Incremental sync after first land",
        implementationSource: "Incremental sync is deferred for V1 so activation does not depend on change-data-capture maturity.",
        start: "Starting from no V1 node (deferred)",
        pathHops: ["Deferred KU #7: not in V1 topology."],
        nodesTouched: ["None in V1 topology."],
        facets: "None for V1 Function trace; future design would touch Core Application & Runtime, Data Storage & Management, External Systems, and Infrastructure & Operations.",
        deferredOrBlocked: "DEFERRED KU #7: Incremental sync after first land",
      },
{
        id: "ingest-22",
        title: "DEFERRED KU #7: Vertical CRM OAuth continuous pull",
        implementationSource: "Vertical CRM OAuth continuous pull is deferred for V1 and cannot bypass the export-mediated ingestion pipeline when revisited.",
        start: "Starting from no V1 node (deferred)",
        pathHops: ["Deferred KU #7: not in V1 topology."],
        nodesTouched: ["None in V1 topology."],
        facets: "None for V1 Function trace; future design would touch Core Application & Runtime, Data Storage & Management, External Systems, Identity / Security / Compliance, and Infrastructure & Operations.",
        deferredOrBlocked: "DEFERRED KU #7: Vertical CRM OAuth continuous pull",
      }
    ],
  },
{
    id: "c5-forward-deploy-generation",
    label: "C5 Forward-deploy generation \u2014 Function wiring traces",
    items: [
{
        id: "fwd-01",
        title: "Verified public URL starts enrichment",
        implementationSource: "Forward-deploy requires a verified, resolvable firm-controlled public URL before enrichment or workspace readiness can advance.",
        start: "Starting from firm_tenancy (existing)",
        pathHops: ["When a forward-deploy firm is created in `firm_tenancy`, an `enrichment_root` is required before any public-facts run may open [firm-bound root, optional listing IDs only secondary].", "When the root URL resolves and matches the firm identity, `enrichment_fact` may begin collecting public facts for that firm.", "When the root URL fails resolve or firm-match, `prepared_workspace` keeps prepared-workspace-ready closed.", "When either decision lands, `audit_trail` records the root, match result, and block or proceed event."],
        nodesTouched: ["firm_tenancy", "enrichment_root", "enrichment_fact", "prepared_workspace", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance.",
      },
{
        id: "fwd-02",
        title: "Robots decisions gate crawl paths",
        implementationSource: "Enrichment fetches and honors robots.txt and crawl directives before path fetch, failing closed for disallowed paths.",
        start: "Starting from enrichment_root (existing)",
        pathHops: ["When `enrichment_root` is verified, `crawl_permission` fetches robots and directive state for the enrichment user-agent [fail closed on unavailable or disallowed permission state].", "When `crawl_permission` marks a path allowed, `enrichment_fact` may receive facts from that path.", "When `crawl_permission` marks a path disallowed, `enrichment_fact` receives no content from that path.", "When a robots decision is made, `audit_trail` appends the user-agent, root, path set, and allow/deny result."],
        nodesTouched: ["enrichment_root", "crawl_permission", "enrichment_fact", "audit_trail"],
        facets: "Core Application & Runtime; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "fwd-03",
        title: "Public-facts allowlist filters enrichment",
        implementationSource: "Pre-connect enrichment stores only already-public firm facts and drops private, authenticated, or person-sensitive fields.",
        start: "Starting from enrichment_fact (existing)",
        pathHops: ["When page content is extracted into `enrichment_fact`, `public_fact_policy` evaluates each candidate field against the pre-connect allowlist [legal name, public contact, public site copy, hours, services, public logo assets].", "When `public_fact_policy` accepts a field, `enrichment_fact` stores the field with its public class.", "When `public_fact_policy` rejects a field, `prepared_workspace` cannot hydrate that value from enrichment output.", "When fields are dropped, `audit_trail` records the policy version and rejected class without preserving private content."],
        nodesTouched: ["enrichment_fact", "public_fact_policy", "prepared_workspace", "audit_trail"],
        facets: "Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime.",
      },
{
        id: "fwd-04",
        title: "Listing facts are secondary with provenance",
        implementationSource: "GBP and similar listing facts supplement the firm site with field-level provenance and firm-site preference on conflicts.",
        start: "Starting from enrichment_root (existing)",
        pathHops: ["When `enrichment_root` includes a listing identity, `enrichment_fact` may collect listing-native public fields after the firm site has been addressed.", "When site and listing values differ, `source_provenance` stores both values, their source, and the conflict winner [prefer firm-site except listing-native fields].", "When `prepared_workspace` hydrates a conflicted field, it binds the winning value and keeps source context reviewable.", "When conflict resolution occurs, `audit_trail` records the field, sources, and selected provenance."],
        nodesTouched: ["enrichment_root", "enrichment_fact", "source_provenance", "prepared_workspace", "audit_trail"],
        facets: "External Systems; Data Storage & Management; Core Application & Runtime.",
      },
{
        id: "fwd-05",
        title: "Polite crawler posture controls scale",
        implementationSource: "Per-host rate limits, crawler identity, backoff, and fetch caching keep enrichment from hammering firm origins.",
        start: "Starting from enrichment_root (existing)",
        pathHops: ["When a root host is queued from `enrichment_root`, `crawl_budget` assigns the user-agent identity, contact URI, host rate, and backoff state.", "When a fetch is requested, `public_fetch_cache` is checked before origin access [reuse fresh public fetches within TTL].", "When the cache misses and `crawl_budget` allows a request, `enrichment_fact` receives the public response.", "When the host blocks, throttles, or fails, `crawl_budget` moves into polite backoff and `audit_trail` records the capped retry posture."],
        nodesTouched: ["enrichment_root", "crawl_budget", "public_fetch_cache", "enrichment_fact", "audit_trail"],
        facets: "External Systems; Infrastructure & Operations; Cost / FinOps; Core Application & Runtime.",
      },
{
        id: "fwd-06",
        title: "Structured firm facts feed hydration",
        implementationSource: "Public pages are extracted into typed firm-fact keys with preferred structured data and confidence on fallback heuristics.",
        start: "Starting from enrichment_fact (existing)",
        pathHops: ["When allowed content reaches `enrichment_fact`, `fact_schema` maps it into typed keys such as name, phone, address, service blurb, hours, and logo URL [JSON-LD preferred, OG fallback, heuristic lower confidence].", "When a key is populated, `source_provenance` stores extract method and confidence beside the value.", "When `prepared_workspace` hydrates, it binds to `fact_schema` keys rather than raw page markup.", "When a low-confidence heuristic key is used, `workspace_readiness` marks that key for confirm before ready."],
        nodesTouched: ["enrichment_fact", "fact_schema", "source_provenance", "prepared_workspace", "workspace_readiness"],
        facets: "Data Storage & Management; Core Application & Runtime; External Systems.",
      },
{
        id: "fwd-07",
        title: "Freshness clock blocks stale packs",
        implementationSource: "Firm-fact snapshots carry fetched-at and TTL state, and stale facts block hydrate or ready until refresh.",
        start: "Starting from enrichment_fact (existing)",
        pathHops: ["When `enrichment_fact` publishes a fact snapshot, `fact_freshness` stamps fetched-at, TTL, and source validators.", "When `fact_freshness` is current, `hydrate_job` may use the snapshot for `prepared_workspace` generation.", "When `fact_freshness` is stale, `workspace_readiness` closes the freshness gate and requires refresh before ship or open-as-ready.", "When refresh completes, `audit_trail` links the old snapshot, new snapshot, and freshness decision."],
        nodesTouched: ["enrichment_fact", "fact_freshness", "hydrate_job", "prepared_workspace", "workspace_readiness", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Infrastructure & Operations.",
      },
{
        id: "fwd-08",
        title: "Logo capture uses public brand surfaces",
        implementationSource: "Logo resolution follows a public-source priority chain and falls back to a neutral unconfirmed placeholder.",
        start: "Starting from enrichment_fact (existing)",
        pathHops: ["When public assets are present in `enrichment_fact`, `brand_extract` resolves logo candidates from schema logo, touch icon, guarded OG image, and listing profile image [do not treat generic photos as marks].", "When a candidate is accepted, `brand_package` receives the logo asset reference and source.", "When no candidate is accepted, `brand_package` receives a neutral placeholder and logo-unconfirmed state.", "When `prepared_workspace` renders the firm face, it uses the accepted logo or placeholder, never a platform mark as the firm mark."],
        nodesTouched: ["enrichment_fact", "brand_extract", "brand_package", "prepared_workspace"],
        facets: "External Systems; Core Application & Runtime; Data Storage & Management.",
      },
{
        id: "fwd-09",
        title: "Palette extraction keeps colors attributable and readable",
        implementationSource: "Brand colors come from attributable public surfaces and must pass contrast checks before use.",
        start: "Starting from brand_extract (existing)",
        pathHops: ["When `brand_extract` has theme-color, nav style, or logo-derived candidates, `brand_contrast_result` evaluates text-on-brand pairs [reject photo-biased samples].", "When a color pair passes, `brand_package` stores the palette with source and contrast result.", "When no pair passes, `brand_package` stores the neutral fallback palette.", "When `prepared_workspace` hydrates chrome, it applies only the stored contrast-safe or neutral palette."],
        nodesTouched: ["brand_extract", "brand_contrast_result", "brand_package", "prepared_workspace"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "fwd-10",
        title: "Voice snippets stay sourced",
        implementationSource: "Brand voice fields are sourced public excerpts, not model-invented copy.",
        start: "Starting from enrichment_fact (existing)",
        pathHops: ["When public site copy is accepted into `enrichment_fact`, `voice_excerpt` stores quoted or lightly truncated snippets with source URL and excerpt hash.", "When `brand_extract` assembles voice fields, it may use only `voice_excerpt` entries.", "When `prepared_workspace` hydrates brand copy, it renders attributable excerpts and excludes invented brand voice.", "When an excerpt is bound, `audit_trail` records the URL, hash, and target field."],
        nodesTouched: ["enrichment_fact", "voice_excerpt", "brand_extract", "prepared_workspace", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Identity / Security / Compliance.",
      },
{
        id: "fwd-11",
        title: "Brand package versions separate from facts",
        implementationSource: "Logo, palette, and voice artifacts are versioned independently from fact snapshots with explicit re-extract or keep-brand behavior.",
        start: "Starting from brand_extract (existing)",
        pathHops: ["When `brand_extract` publishes logo, palette, and voice outputs, `brand_package` creates a version independent of `enrichment_fact`.", "When `enrichment_fact` refreshes, `hydrate_job` receives an explicit choice to keep the current `brand_package` or bind a re-extracted version.", "When `prepared_workspace` is generated or regenerated, it records both the fact snapshot version and brand package version.", "When the brand binding changes, `audit_trail` records keep-brand or re-extract."],
        nodesTouched: ["brand_extract", "brand_package", "enrichment_fact", "hydrate_job", "prepared_workspace", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Infrastructure & Operations.",
      },
{
        id: "fwd-12",
        title: "Incomplete branding remains firm-faced",
        implementationSource: "Incomplete branding shows the firm public name and neutral chrome, never Om Coda marks as the firm's identity.",
        start: "Starting from brand_package (existing)",
        pathHops: ["When `brand_package` is incomplete, `prepared_workspace` reads the firm public name from `firm_tenancy` and sets branding-unconfirmed.", "When branding-unconfirmed is active, `prepared_workspace` renders neutral chrome and keeps platform marks only in platform identity positions.", "When the consultant confirms or replaces branding, `brand_package` updates its confirmation state.", "When confirmation state changes, `audit_trail` records the firm-faced fallback or confirmed package."],
        nodesTouched: ["brand_package", "prepared_workspace", "firm_tenancy", "audit_trail"],
        facets: "Core Application & Runtime; Identity / Security / Compliance; Data Storage & Management.",
      },
{
        id: "fwd-13",
        title: "Hydrate job binds templates to facts and brand",
        implementationSource: "A hydrate job pins a template version to a fact snapshot and brand package, then emits a Prepared Workspace instance.",
        start: "Starting from engagement_template_version (new)",
        pathHops: ["When a forward-deploy template is selected, `engagement_template_version` is pinned for the activation.", "When pinned template, current `enrichment_fact`, and selected `brand_package` are all available, `hydrate_job` creates a binding run.", "When `hydrate_job` succeeds, `prepared_workspace` receives a workspace instance with template, fact, and brand identifiers.", "When the instance is created, `workspace_readiness` marks hydrated as a separate gate and `audit_trail` records the binding inputs."],
        nodesTouched: ["engagement_template_version", "enrichment_fact", "brand_package", "hydrate_job", "prepared_workspace", "workspace_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations.",
      },
{
        id: "fwd-14",
        title: "Required merge fields fail closed",
        implementationSource: "Hydration validates required template fields against the fact record and leaves missing keys in needs-facts state.",
        start: "Starting from hydrate_job (existing)",
        pathHops: ["When `hydrate_job` starts, `merge_field_contract` loads required keys from `engagement_template_version`.", "When `merge_field_contract` finds all required keys in `enrichment_fact`, `prepared_workspace` may receive bound blocks.", "When keys are missing, `prepared_workspace` records needs-facts and `workspace_readiness` keeps ready closed.", "When missing keys are reported, `audit_trail` records the contract version and key names without treating blanks as success."],
        nodesTouched: ["hydrate_job", "merge_field_contract", "engagement_template_version", "enrichment_fact", "prepared_workspace", "workspace_readiness", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "fwd-15",
        title: "Workspace instances are immutable snapshots",
        implementationSource: "Prepared Workspace instances do not silently change; template or fact changes require explicit regenerate into a new version.",
        start: "Starting from prepared_workspace (existing)",
        pathHops: ["When `prepared_workspace` receives a successful hydrate result, it stores an immutable instance version with bound template, fact, and brand identifiers.", "When `enrichment_fact`, `brand_package`, or `engagement_template_version` changes later, the existing `prepared_workspace` version remains unchanged.", "When Regenerate is requested, `hydrate_job` emits a new `prepared_workspace` version and the old access policy remains explicitly governed.", "When hydrate or regenerate occurs, `audit_trail` records the issued snapshot and replacement lineage."],
        nodesTouched: ["prepared_workspace", "hydrate_job", "enrichment_fact", "brand_package", "engagement_template_version", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "fwd-16",
        title: "Readiness depends on hydrate quality",
        implementationSource: "Prepared-workspace-ready requires hydration, freshness, and accepted branding rather than mere shell existence.",
        start: "Starting from prepared_workspace (existing)",
        pathHops: ["When a `prepared_workspace` instance exists, `workspace_readiness` evaluates hydrate status, `fact_freshness`, merge validation, and brand acceptance.", "When all gates pass, `workspace_readiness` opens prepared-workspace-ready for walkthrough.", "When any gate is closed, `prepared_workspace` remains shell-only or needs-review and cannot present as ready.", "When gate state changes, `audit_trail` records the readiness transition and contributing gate states."],
        nodesTouched: ["prepared_workspace", "workspace_readiness", "fact_freshness", "merge_field_contract", "brand_package", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations.",
      },
{
        id: "fwd-17",
        title: "Hydration honors the public-facts deliverable",
        implementationSource: "Forward-deploy hydration stays inside a pre-connect public-facts pack and excludes connected-book or live-send claims.",
        start: "Starting from engagement_template_version (existing)",
        pathHops: ["When a template version is selected for forward-deploy, `deliverable_scope` marks it as pre-connect public-facts only.", "When `hydrate_job` binds the template, `deliverable_scope` excludes blocks that assert enrolled clients, connected books, or live sending.", "When `prepared_workspace` renders, it shows public facts, brand, template preview, and next-step motions without claiming authority not yet granted.", "When a scoped block is removed or allowed, `audit_trail` records the deliverable scope decision."],
        nodesTouched: ["engagement_template_version", "deliverable_scope", "hydrate_job", "prepared_workspace", "audit_trail"],
        facets: "Core Application & Runtime; Identity / Security / Compliance; Data Storage & Management.",
      },
{
        id: "fwd-18",
        title: "Opaque tokens open Prepared Workspace",
        implementationSource: "No-login access uses high-entropy opaque capability tokens bound to one Prepared Workspace instance.",
        start: "Starting from prepared_workspace (existing)",
        pathHops: ["When a `prepared_workspace` instance is issued, `access_token` may be created for that single instance [opaque bearer secret, not a sequential workspace id].", "When a no-login open request presents a valid token, `prepared_workspace` grants only the instance-bound view.", "When a request presents only an id, slug, or invalid token, `prepared_workspace` denies no-login access.", "When a token is issued or used, `audit_trail` records the event without storing the raw secret."],
        nodesTouched: ["prepared_workspace", "access_token", "audit_trail"],
        facets: "Identity / Security / Compliance; Core Application & Runtime; Data Storage & Management.",
      },
{
        id: "fwd-19",
        title: "Token lifetime and purpose are mandatory",
        implementationSource: "No-login tokens carry purpose scope, expiry, rotation after redeem, and operator revoke state.",
        start: "Starting from access_token (existing)",
        pathHops: ["When `access_token` is issued, `token_policy` binds purpose, TTL, redeem count, rotation behavior, and revoke state.", "When a token is redeemed within purpose and TTL, `prepared_workspace` opens or a scoped `redeem_session` is created.", "When a token is expired, revoked, or used outside purpose, `prepared_workspace` refuses access and requires a fresh issue path.", "When expiry, redeem, rotate, or revoke occurs, `audit_trail` records the token id, purpose, and state transition."],
        nodesTouched: ["access_token", "token_policy", "prepared_workspace", "redeem_session", "audit_trail"],
        facets: "Identity / Security / Compliance; Core Application & Runtime; Data Storage & Management.",
      },
{
        id: "fwd-20",
        title: "Token verifiers are hash-only at rest",
        implementationSource: "Raw capability secrets are shown only at issue time while stored token state keeps a cryptographic verifier and metadata.",
        start: "Starting from access_token (existing)",
        pathHops: ["When `access_token` is issued, the raw secret is emitted once to the intended delivery channel and never written to persistent state.", "When token state is stored, `access_token` keeps only the hash verifier plus instance, purpose, expiry, and redeem metadata.", "When a no-login request arrives, `access_token` compares the presented secret to the verifier before `prepared_workspace` can open.", "When token events are appended, `audit_trail` stores issue, redeem, expire, and revoke metadata without the raw secret."],
        nodesTouched: ["access_token", "prepared_workspace", "audit_trail"],
        facets: "Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime.",
      },
{
        id: "fwd-21",
        title: "Redeem flow removes secrets from navigation",
        implementationSource: "Delivered links use one-time exchange into a short session and strip token secrets from later URLs and referers.",
        start: "Starting from access_token (existing)",
        pathHops: ["When a token link is prepared for SMS or email, `access_token` is wrapped as a one-time redeem URL [no reusable full-secret navigation].", "When the link is opened, `access_token` is exchanged into `redeem_session` and the token is removed from the addressable workspace URL.", "When `prepared_workspace` loads third-party or downstream links, `redeem_session` carries access without forwarding the capability secret.", "When redeem succeeds or fails, `audit_trail` records the token id, session id, and delivery-safe transition."],
        nodesTouched: ["access_token", "redeem_session", "prepared_workspace", "audit_trail"],
        facets: "Identity / Security / Compliance; Core Application & Runtime; External Systems; Infrastructure & Operations.",
      },
{
        id: "fwd-22",
        title: "Touchpoint tokens are distinct from workspace tokens",
        implementationSource: "Prepared Workspace access and later contact touchpoints use separate purpose-scoped tokens or scoped sessions.",
        start: "Starting from access_token (existing)",
        pathHops: ["When `access_token` is issued for prepared-workspace-open, `token_policy` prevents that token from authorizing touchpoint continuation or book motions.", "When a later contact touchpoint needs no-login continuation, a separate `access_token` purpose is issued with its own TTL and revoke state.", "When `prepared_workspace` token redeem succeeds, it grants only workspace preview authority unless a new scoped token is present.", "When tokens are filtered or reviewed, `audit_trail` separates events by purpose scope."],
        nodesTouched: ["access_token", "token_policy", "prepared_workspace", "audit_trail"],
        facets: "Identity / Security / Compliance; Core Application & Runtime; Data Storage & Management.",
      }
    ],
  },
{
    id: "c6-escrow-payment-mechanics",
    label: "C6 Escrow / contingent-payment mechanics \u2014 Function paper traces",
    items: [
{
        id: "escmech-01",
        title: "Escrow ledger as per-firm instrument store",
        implementationSource: "Commercial and Release control read durable per-tenancy escrow instruments instead of reconstructing money posture from a processor console.",
        start: "Starting from `firm_tenancy` (existing)",
        pathHops: ["When a firm accepts contingent commercial terms at `firm_tenancy`, a scoped instrument is opened at `escrow_ledger_entry`.", "When the instrument is opened at `escrow_ledger_entry`, principal, currency, provider refs, terms version, cap, and current status are initialized at `held_balance`.", "When the ledger entry is initialized at `escrow_ledger_entry`, a creation receipt is appended at `audit_trail`.", "When Commercial requests firm money posture at `firm_tenancy`, current instruments and balances are projected from `escrow_ledger_entry` into `escrow_status_read_model`."],
        nodesTouched: ["firm_tenancy", "escrow_ledger_entry", "held_balance", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance.",
      },
{
        id: "escmech-02",
        title: "Closed ledger status machine",
        implementationSource: "Escrow instruments move only through the closed status path and rejected transitions are recorded.",
        start: "Starting from `escrow_ledger_entry` (existing)",
        pathHops: ["When a status transition is requested at `escrow_ledger_entry`, the requested edge is checked at `escrow_status_machine`.", "When the requested edge is legal at `escrow_status_machine`, the status change is applied at `escrow_ledger_entry`.", "When the requested edge is illegal at `escrow_status_machine`, a rejected transition receipt is appended at `audit_trail`.", "When status changes at `escrow_ledger_entry`, allowed next actions refresh at `escrow_status_read_model`."],
        nodesTouched: ["escrow_ledger_entry", "escrow_status_machine", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-03",
        title: "Immobilize principal while held",
        implementationSource: "A ledger reaches held only after the provider confirms funds are immobilized.",
        start: "Starting from `payment_webhook_receipt` (existing)",
        pathHops: ["When a signed hold-confirmation webhook arrives at `payment_webhook_receipt`, provider event identity is deduped before any balance changes occur.", "When the webhook is accepted at `payment_webhook_receipt`, immobilized provider refs are written to `provider_payment_identity`.", "When immobilized provider refs are present at `provider_payment_identity`, the corresponding funds become held at `held_balance`.", "When held funds are recorded at `held_balance`, the instrument moves from pending accept or failed hold to held at `escrow_ledger_entry`.", "When held status is applied at `escrow_ledger_entry`, Activation reads escrow satisfied from `escrow_status_read_model`."],
        nodesTouched: ["payment_webhook_receipt", "provider_payment_identity", "held_balance", "escrow_ledger_entry", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-04",
        title: "Stripe Connect / marketplace hold-release rail mechanics",
        implementationSource: "Firm-as-payer hold and later transfer are represented as provider rail refs on one ledger instrument, subject to counsel clearance.",
        start: "Starting from `provider_payment_identity` (new)",
        pathHops: ["When the selected Connect marketplace rail is configured at `provider_payment_identity`, charge, account, and eventual transfer reference slots are bound to `firm_tenancy`.", "When a firm hold succeeds at `provider_payment_identity`, the charge ref updates `held_balance`.", "When held funds settle at `held_balance`, the rail posture is written to `escrow_ledger_entry`.", "When release later becomes eligible at `release_evidence_package`, the transfer ref is added to the same provider identity rather than creating a detached payout.", "When provider refs change at `provider_payment_identity`, a rail receipt is appended at `audit_trail`."],
        nodesTouched: ["provider_payment_identity", "firm_tenancy", "held_balance", "escrow_ledger_entry", "release_evidence_package", "audit_trail", "counsel_gate"],
        facets: "External Systems; Data Storage & Management; Identity / Security / Compliance; Cost / FinOps.",
        missingSeatFlag: "MT/MSB legal posture remains gated by `counsel_gate`; mechanics do not create legal clearance.",
      },
{
        id: "escmech-05",
        title: "Authorize/capture and release transfer are distinct operations",
        implementationSource: "Accept-time custody and outcome-time payout are separate ledger and provider events.",
        start: "Starting from `escrow_ledger_entry` (existing)",
        pathHops: ["When Accept-time hold is initiated at `escrow_ledger_entry`, the hold operation creates or updates charge state at `provider_payment_identity`.", "When provider hold confirmation arrives at `payment_webhook_receipt`, captured or immobilized funds update `held_balance`.", "When outcome evidence validates at `release_evidence_package`, a separate transfer action is opened at `release_attempt`.", "When the transfer succeeds at `provider_payment_identity`, the ledger moves to released at `escrow_ledger_entry`.", "When the ledger moves to released at `escrow_ledger_entry`, both original charge ref and later transfer ref are retained in `audit_trail`."],
        nodesTouched: ["escrow_ledger_entry", "provider_payment_identity", "payment_webhook_receipt", "held_balance", "release_evidence_package", "release_attempt", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Cost / FinOps.",
      },
{
        id: "escmech-06",
        title: "Cap / collar enforcement on the ledger",
        implementationSource: "Release amounts are constrained by held principal, release schedule, and remaining cap on the instrument.",
        start: "Starting from `held_balance` (existing)",
        pathHops: ["When held principal is established at `held_balance`, hard cap and remaining releasable amount are recorded at `escrow_ledger_entry`.", "When a release action is requested at `release_attempt`, the requested amount is compared against remaining releasable at `held_balance`.", "When the requested amount exceeds the cap at `held_balance`, a cap-block decision is written to `release_attempt`.", "When a cap-block occurs at `release_attempt`, no provider transfer ref is created at `provider_payment_identity`.", "When cap state changes at `held_balance`, Commercial cap posture refreshes at `escrow_status_read_model`."],
        nodesTouched: ["held_balance", "escrow_ledger_entry", "release_attempt", "provider_payment_identity", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Cost / FinOps; Identity / Security / Compliance.",
      },
{
        id: "escmech-07",
        title: "Terms version bound to the instrument",
        implementationSource: "The accepted terms version and predicate hash are frozen on the instrument and used for all later decisions.",
        start: "Starting from `escrow_ledger_entry` (existing)",
        pathHops: ["When terms are accepted within `firm_tenancy`, the accepted terms version and predicate hash are stamped onto `escrow_ledger_entry`.", "When the instrument reaches held at `escrow_ledger_entry`, the same terms stamp is bound to `held_balance`.", "When release, return, or forfeit is evaluated at `release_evidence_package`, predicate checks read the frozen terms stamp from `escrow_ledger_entry`.", "When a terms amendment is recorded at `firm_tenancy`, open instruments keep their existing stamp and any new accepted instrument receives a new stamp.", "When any decision uses a terms stamp at `release_evidence_package`, the stamp is appended into `audit_trail`."],
        nodesTouched: ["firm_tenancy", "escrow_ledger_entry", "held_balance", "release_evidence_package", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance.",
      },
{
        id: "escmech-08",
        title: "Accept to hold confirmation via idempotent webhooks",
        implementationSource: "Verified provider webhooks drive pending accept to held, and duplicate event ids are no-ops.",
        start: "Starting from `payment_webhook_receipt` (existing)",
        pathHops: ["When a provider payment event arrives at `payment_webhook_receipt`, signature and tenant correlation are verified before applying the event.", "When the provider event id already exists at `payment_webhook_receipt`, the duplicate delivery is marked replayed and no ledger mutation occurs.", "When the provider event id is new at `payment_webhook_receipt`, the event links to a pending instrument at `escrow_ledger_entry`.", "When the linked event confirms successful hold at `payment_webhook_receipt`, `held_balance` records immobilized funds.", "When held funds are recorded at `held_balance`, the transition receipt is appended at `audit_trail`."],
        nodesTouched: ["payment_webhook_receipt", "escrow_ledger_entry", "held_balance", "audit_trail", "firm_tenancy"],
        facets: "External Systems; Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-09",
        title: "Webhook and job idempotency for release path",
        implementationSource: "Release, return, and forfeit attempts share stable idempotency keys across job retries and provider retries.",
        start: "Starting from `release_attempt` (new)",
        pathHops: ["When a release, return, or forfeit job is scheduled at `release_attempt`, an idempotency key is formed from instrument, evidence, and action.", "When the same action retries at `release_attempt`, the existing idempotency key and provider request state are reused.", "When provider completion arrives at `payment_webhook_receipt`, provider event id and action idempotency key are matched before status changes at `escrow_ledger_entry`.", "When a duplicate provider completion arrives at `payment_webhook_receipt`, the replay receipt updates `audit_trail` without moving funds again.", "When terminal money motion succeeds at `provider_payment_identity`, the corresponding balance reduction is applied at `held_balance`."],
        nodesTouched: ["release_attempt", "payment_webhook_receipt", "escrow_ledger_entry", "provider_payment_identity", "held_balance", "audit_trail"],
        facets: "Core Application & Runtime; External Systems; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance; Cost / FinOps.",
      },
{
        id: "escmech-10",
        title: "Outcome verification to release trigger",
        implementationSource: "A verified meeting booked event opens the release pending window for the matching held instrument.",
        start: "Starting from `outcome_verification_event` (new)",
        pathHops: ["When Engagement runtime emits meeting booked at `outcome_verification_event`, firm, campaign, enrollment, contact, meeting, and occurrence time are recorded.", "When the event is recorded at `outcome_verification_event`, the firm and campaign are matched to held instruments at `escrow_ledger_entry`.", "When a matching held instrument exists at `escrow_ledger_entry`, attribution proof is requested at `attribution_path`.", "When attribution proof passes at `attribution_path`, the instrument enters release pending window at `escrow_ledger_entry`.", "When the window opens at `escrow_ledger_entry`, the trigger evidence id appears in `escrow_status_read_model`."],
        nodesTouched: ["outcome_verification_event", "escrow_ledger_entry", "attribution_path", "measurement_window", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-11",
        title: "Attribution gate before window opens",
        implementationSource: "Release eligibility requires inspectable enrollment, campaign, contact, and meeting membership proof.",
        start: "Starting from `attribution_path` (new)",
        pathHops: ["When a meeting booked event is received at `outcome_verification_event`, membership data is assembled at `attribution_path`.", "When campaign, enrollment, contact, and meeting linkage is present at `attribution_path`, release eligibility advances at `escrow_ledger_entry`.", "When linkage is missing at `attribution_path`, attribution failed is written to `release_evidence_package`.", "When attribution failed is written at `release_evidence_package`, release pending window remains closed at `measurement_window`.", "When attribution decision is made at `attribution_path`, the decision and source ids are appended at `audit_trail`."],
        nodesTouched: ["outcome_verification_event", "attribution_path", "escrow_ledger_entry", "release_evidence_package", "measurement_window", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-12",
        title: "Measurement-window clock job",
        implementationSource: "Release waits for the frozen terms window to elapse without cancel or reopen events.",
        start: "Starting from `measurement_window` (new)",
        pathHops: ["When attribution passes at `attribution_path`, a window is opened at `measurement_window` using the frozen terms stamp from `escrow_ledger_entry`.", "When the window opens at `measurement_window`, the instrument changes to release pending window at `escrow_ledger_entry`.", "When meeting canceled or reopened occurs at `outcome_verification_event` before deadline, the window is aborted at `measurement_window`.", "When the deadline elapses without abort at `measurement_window`, releasable state is written to `release_evidence_package`.", "When window state changes at `measurement_window`, opened, ends, canceled, elapsed, and abort fields refresh at `escrow_status_read_model`."],
        nodesTouched: ["measurement_window", "attribution_path", "escrow_ledger_entry", "outcome_verification_event", "release_evidence_package", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance.",
      },
{
        id: "escmech-13",
        title: "Release evidence package assembly",
        implementationSource: "Provider release is gated on one validated evidence object containing outcome, attribution, window, and terms proof.",
        start: "Starting from `release_evidence_package` (existing)",
        pathHops: ["When window elapsed occurs at `measurement_window`, meeting, tenancy, instrument, attribution, and terms facts are assembled at `release_evidence_package`.", "When required fields are present at `release_evidence_package`, the package validates and becomes release eligible.", "When required fields are missing at `release_evidence_package`, release eligibility remains blocked at `release_attempt`.", "When the package validates at `release_evidence_package`, the package id is written to `escrow_ledger_entry`.", "When package state changes at `release_evidence_package`, evidence summary refreshes at `escrow_status_read_model`."],
        nodesTouched: ["release_evidence_package", "measurement_window", "release_attempt", "escrow_ledger_entry", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-14",
        title: "Release control to provider release or transfer",
        implementationSource: "A releasable instrument executes provider transfer first, then the ledger becomes released.",
        start: "Starting from `release_attempt` (new)",
        pathHops: ["When execute release is requested at `release_attempt`, releasable state and evidence validity are checked at `release_evidence_package`.", "When evidence is valid at `release_evidence_package`, remaining cap and held principal are checked at `held_balance`.", "When the amount passes cap at `held_balance`, provider transfer is requested through `provider_payment_identity` with the release idempotency key.", "When provider transfer succeeds at `provider_payment_identity`, transfer ref is written to `escrow_ledger_entry`.", "When transfer ref is present at `escrow_ledger_entry`, status moves to released and the receipt is appended at `audit_trail`."],
        nodesTouched: ["release_attempt", "release_evidence_package", "held_balance", "provider_payment_identity", "escrow_ledger_entry", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; External Systems; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations; Cost / FinOps.",
      },
{
        id: "escmech-15",
        title: "Dispute freeze halts release jobs",
        implementationSource: "Buyer or provider dispute state freezes the instrument and cancels pending release motion.",
        start: "Starting from `dispute_freeze` (new)",
        pathHops: ["When a buyer dispute is opened at `firm_tenancy`, freeze state is created at `dispute_freeze`.", "When a provider dispute or chargeback webhook arrives at `payment_webhook_receipt`, freeze state is created or updated at `dispute_freeze`.", "When freeze state becomes active at `dispute_freeze`, pending release jobs are canceled at `release_attempt`.", "When release jobs are canceled at `release_attempt`, the instrument moves to disputed at `escrow_ledger_entry`.", "When dispute clearance occurs at `dispute_freeze`, release controls re-evaluate evidence and window state before any money motion resumes."],
        nodesTouched: ["firm_tenancy", "payment_webhook_receipt", "dispute_freeze", "release_attempt", "escrow_ledger_entry", "release_evidence_package", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; External Systems; Data Storage & Management; Identity / Security / Compliance; Infrastructure & Operations.",
      },
{
        id: "escmech-16",
        title: "Return / refund rail mechanic",
        implementationSource: "Return predicates move held funds back to the firm through an idempotent provider refund path.",
        start: "Starting from `release_attempt` (new)",
        pathHops: ["When a return predicate fires at `release_evidence_package`, a return action is opened at `release_attempt`.", "When the return action opens at `release_attempt`, the provider refund or reversal target is resolved at `provider_payment_identity`.", "When provider refund succeeds at `provider_payment_identity`, refund ref and amount reduce `held_balance`.", "When the refund ref is present at `provider_payment_identity`, the instrument moves to returned at `escrow_ledger_entry`.", "When returned status is applied at `escrow_ledger_entry`, return reason and provider receipt are appended at `audit_trail`."],
        nodesTouched: ["release_evidence_package", "release_attempt", "provider_payment_identity", "held_balance", "escrow_ledger_entry", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; External Systems; Data Storage & Management; Identity / Security / Compliance; Cost / FinOps; Infrastructure & Operations.",
      },
{
        id: "escmech-17",
        title: "Forfeit terminal without calling it release",
        implementationSource: "Forfeit is a distinct terminal state with its own evidence type and money disposition.",
        start: "Starting from `release_evidence_package` (existing)",
        pathHops: ["When a forfeit predicate fires under frozen terms at `release_evidence_package`, a forfeit action is opened at `release_attempt`.", "When the forfeit action opens at `release_attempt`, forfeit reason and disposition are stored separately from meeting booked release evidence.", "When disposition requires money movement at `release_attempt`, provider transfer or fee refs are written at `provider_payment_identity` with a forfeit idempotency key.", "When forfeit disposition settles at `provider_payment_identity`, the instrument moves to forfeited at `escrow_ledger_entry`.", "When forfeited status is applied at `escrow_ledger_entry`, analytics and Commercial read forfeit rather than released from `escrow_status_read_model`."],
        nodesTouched: ["release_evidence_package", "release_attempt", "provider_payment_identity", "escrow_ledger_entry", "held_balance", "audit_trail", "escrow_status_read_model"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Cost / FinOps.",
      },
{
        id: "escmech-18",
        title: "Append-only commercial receipts",
        implementationSource: "Every ledger transition emits a tenant-scoped receipt with actor, inputs, decision, evidence, and provider refs.",
        start: "Starting from `audit_trail` (existing)",
        pathHops: ["When a ledger transition is attempted at `escrow_ledger_entry`, actor, source system, from status, to status, and predicate inputs are assembled for `audit_trail`.", "When evidence participates in the transition at `release_evidence_package`, evidence pointers are copied into the receipt at `audit_trail`.", "When provider refs participate in the transition at `provider_payment_identity`, provider pointers are copied into the receipt at `audit_trail`.", "When the receipt is appended at `audit_trail`, last receipt and transition history refresh at `escrow_status_read_model`.", "When Support requests Commercial facts at `firm_tenancy`, the last receipt is read from `escrow_status_read_model`."],
        nodesTouched: ["audit_trail", "escrow_ledger_entry", "release_evidence_package", "provider_payment_identity", "escrow_status_read_model", "firm_tenancy"],
        facets: "Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime; Infrastructure & Operations.",
      },
{
        id: "escmech-19",
        title: "PCI adjacency: tokenize and never store PAN",
        implementationSource: "Payment method capture stays provider-tokenized and Tower stores only provider identifiers.",
        start: "Starting from `provider_payment_identity` (new)",
        pathHops: ["When payment method entry begins at `firm_tenancy`, provider-hosted or tokenized collection state is created at `provider_payment_identity`.", "When tokenized collection succeeds at `provider_payment_identity`, customer id and payment method id are stored without raw card data.", "When a hold is initiated at `escrow_ledger_entry`, the ledger references tokenized provider identifiers from `provider_payment_identity`.", "When provider payment events arrive at `payment_webhook_receipt`, payload handling stores event ids, charge ids, and payment method refs rather than PAN or CVV.", "When provider identity changes at `provider_payment_identity`, a PCI boundary receipt is appended at `audit_trail`."],
        nodesTouched: ["firm_tenancy", "provider_payment_identity", "escrow_ledger_entry", "payment_webhook_receipt", "audit_trail"],
        facets: "External Systems; Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime; Infrastructure & Operations.",
      },
{
        id: "escmech-20",
        title: "BLOCKED counsel: money-transmitter boundary",
        implementationSource: "Production MT/MSB clearance is a counsel gate; engineering only records firm-payer scope and refuses client-fund intake.",
        start: "Starting from `counsel_gate` (new)",
        pathHops: ["When MT/MSB clearance is pending at `counsel_gate`, production release rail activation remains blocked at `firm_tenancy`; no production MT/MSB topology is invented.", "When firm-payer scope is recorded at `firm_tenancy`, the instrument remains limited to firm to Om Coda commercial consideration at `escrow_ledger_entry`.", "When client-fund intake is requested at `firm_tenancy`, the request is rejected into `audit_trail` because no client-fund intake node or production flow exists.", "When human counsel later clears a chosen rail at `counsel_gate`, production eligibility may be re-evaluated for the existing firm-payer nodes only."],
        nodesTouched: ["counsel_gate", "firm_tenancy", "escrow_ledger_entry", "audit_trail"],
        facets: "Identity / Security / Compliance; External Systems; Core Application & Runtime.",
        missingSeatFlag: "BLOCKED counsel \u2014 no production MT/MSB topology, client-fund intake rail, or legal clearance is represented here.",
        deferredOrBlocked: "BLOCKED counsel: money-transmitter boundary",
      },
{
        id: "escmech-21",
        title: "Engagement runtime to Release control event contract",
        implementationSource: "Engagement runtime publishes a versioned outcome contract that Release control consumes idempotently.",
        start: "Starting from `outcome_verification_event` (new)",
        pathHops: ["When meeting booked, canceled, or reopened occurs in Engagement runtime, a versioned event is written at `outcome_verification_event`.", "When the event is written at `outcome_verification_event`, event id, source, time, tenancy, campaign, contact, meeting, and schema version are recorded.", "When Release control consumes the event at `outcome_verification_event`, event id dedupe is checked before changes occur at `measurement_window`.", "When meeting booked passes dedupe at `outcome_verification_event`, attribution and window opening proceed through `attribution_path` and `measurement_window`.", "When meeting canceled or reopened passes dedupe at `outcome_verification_event`, any open window aborts at `measurement_window`.", "When event consumption changes payment state, Engagement and Escrow cross-links refresh at `escrow_status_read_model`."],
        nodesTouched: ["outcome_verification_event", "measurement_window", "attribution_path", "escrow_ledger_entry", "release_evidence_package", "escrow_status_read_model", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance.",
      },
{
        id: "escmech-22",
        title: "Escrow status read model for Commercial oversight",
        implementationSource: "Commercial, Release control, and Support share one per-tenancy escrow status projection.",
        start: "Starting from `escrow_status_read_model` (new)",
        pathHops: ["When instrument, status, or balance changes at `escrow_ledger_entry`, projection rows refresh at `escrow_status_read_model`.", "When evidence package state changes at `release_evidence_package`, evidence summary and trigger source refresh at `escrow_status_read_model`.", "When window or dispute state changes at `measurement_window` or `dispute_freeze`, clocks and freeze flags refresh at `escrow_status_read_model`.", "When provider refs change at `provider_payment_identity`, charge, transfer, refund, and rail refs refresh at `escrow_status_read_model`.", "When counsel gate state changes at `counsel_gate`, gate visibility refreshes at `escrow_status_read_model`.", "When Support or Commercial reads firm money posture at `firm_tenancy`, the shared projection supplies facts without processor-console access."],
        nodesTouched: ["escrow_status_read_model", "escrow_ledger_entry", "held_balance", "release_evidence_package", "measurement_window", "dispute_freeze", "provider_payment_identity", "counsel_gate", "firm_tenancy", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Identity / Security / Compliance; Infrastructure & Operations; Cost / FinOps.",
      }
    ],
  },
{
    id: "c7-instrumentation-analytics",
    label: "C7 Instrumentation / analytics pipeline \u2014 Function wiring traces",
    items: [
{
        id: "obs-01",
        title: "Messaging stream is the connective tissue",
        implementationSource: "Oversight, Firm health, and Audit trail read occurrence history only from the shared stream, not orphan ESP or runtime side logs.",
        start: "`messaging_event_stream` (new)",
        pathHops: ["When send, delivery, reply, classify, act, or booking-path events occur at `messaging_event_stream`, C1, C2, and C7 consume the same append-only occurrence state. [shared stream identity: C1/C2/C7]", "When those stream events include stable ids and event classes, `metrics_materialization` rolls shell rates and timelines from the stream rather than side-channel counters.", "When an operator drills from a shell value, `audit_trail` records or exposes the cited stream event ids behind the send / delivery / reply / act transition."],
        nodesTouched: ["messaging_event_stream", "metrics_materialization", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance",
      },
{
        id: "obs-02",
        title: "Versioned event taxonomy before shell KPI",
        implementationSource: "Shell metrics bind to a published taxonomy version, and unknown or breaking event names do not increment counters.",
        start: "`event_taxonomy` (new)",
        pathHops: ["When an event name or required-property contract is published at `event_taxonomy`, allowed producers and compatibility rules become named ingest state.", "When a producer emits into `messaging_event_stream`, ingest checks the event name and required dimensions against the active `event_taxonomy` version. [reject unknown names; reject breaking versions]", "When a shell metric is defined, `metric_definition` binds the tile to taxonomy names and versions before `metrics_materialization` can roll it.", "When a taxonomy version changes, `audit_trail` receives the definition-change row so later shell numbers cite the contract in force."],
        nodesTouched: ["event_taxonomy", "messaging_event_stream", "metric_definition", "metrics_materialization", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Identity / Security / Compliance; Infrastructure & Operations",
      },
{
        id: "obs-03",
        title: "Required correlation dimensions on every stream event",
        implementationSource: "Every messaging-path event carries tenant, subject, sequence/campaign, attempt, channel, and occurrence time so rollups and drills join deterministically.",
        start: "`firm_tenancy` (new)",
        pathHops: ["When a firm-bound or campaign-bound occurrence starts at `firm_tenancy`, tenant and subject scope become required event dimensions.", "When an event is appended to `messaging_event_stream`, the active `event_taxonomy` requires `firm_id`, subject identity, sequence or campaign identity, attempt identity where present, channel, and `occurred_at`. [tenant key required; event time required]", "When those dimensions are present, `correlation_envelope` can stitch sequence, attempt, and channel state without collapsing orphan events into unknown scope.", "When metrics roll, `metrics_materialization` scopes Firm row and Sequence detail by firm and attempt while preserving drill-through to the stream."],
        nodesTouched: ["firm_tenancy", "event_taxonomy", "messaging_event_stream", "correlation_envelope", "metrics_materialization"],
        facets: "Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime",
      },
{
        id: "obs-04",
        title: "Idempotent ingest with stable event keys",
        implementationSource: "Provider retries and internal replays are accepted once by stable event key so complaint, bounce, and reply rates are not double-counted.",
        start: "`provider_webhook_receipt` (new)",
        pathHops: ["When a provider webhook or internal retry lands at `provider_webhook_receipt`, the receipt records provider event id or deterministic fallback key.", "When the receipt key has already been accepted, `messaging_event_stream` suppresses a second append and marks the receipt duplicate-suppressed. [idempotent; at-least-once safe]", "When the receipt key is new, `messaging_event_stream` accepts or upserts the normalized occurrence under the stable stream id.", "When ingest accepts or suppresses a duplicate, `audit_trail` records the outcome for later operator review.", "When `metrics_materialization` consumes stream events, it sees one logical event per idempotency key and does not inflate shell rates."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "metrics_materialization", "audit_trail"],
        facets: "External Systems; Data Storage & Management; Infrastructure & Operations; Identity / Security / Compliance",
      },
{
        id: "obs-05",
        title: "Producer contracts emit on the path",
        implementationSource: "Each taxonomy event has an owning producer that emits during the domain action, and missing emitters surface as pipeline health.",
        start: "`producer_contract` (new)",
        pathHops: ["When a taxonomy name becomes required at `event_taxonomy`, `producer_contract` binds it to the Sending infrastructure, Conversations/runtime, Booking path, or Capture strip owner.", "When a domain action occurs at `outbound_message`, `conversation_thread`, `engagement_attempt`, or `prepared_workspace`, the owning producer emits the matching event to `messaging_event_stream` before the action is treated as observable. [emit on path; no scrape-later truth]", "When required producers are missing, stale, or not emitting, `pipeline_health` records producer coverage gaps by taxonomy name and owner.", "When producer coverage is available, `metrics_materialization` exposes readiness and shell-fill signals without inventing counts from ESP exports."],
        nodesTouched: ["event_taxonomy", "producer_contract", "outbound_message", "conversation_thread", "engagement_attempt", "prepared_workspace", "messaging_event_stream", "pipeline_health", "metrics_materialization"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; External Systems",
      },
{
        id: "obs-06",
        title: "Signed webhook ingress for deliverability telemetry",
        implementationSource: "Provider webhooks verify signatures, normalize delivery payloads, and append shared telemetry to the stream C1 and C7 both consume.",
        start: "`provider_webhook_receipt` (existing)",
        pathHops: ["When an ESP or SMS provider delivery callback lands at `provider_webhook_receipt`, the receipt stores provider, signature status, received time, and idempotency key. [signed ingress; reject unverifiable payloads]", "When the signature is valid, provider payload classes normalize into C1 delivery-event classes before append to `messaging_event_stream`.", "When normalized delivery events reach `messaging_event_stream`, C1 send gates and C7 rollups read the same delivery fact.", "When signature verify fails or normalization fails, `pipeline_health` records ingress failure and `audit_trail` preserves the receipt outcome."],
        nodesTouched: ["provider_webhook_receipt", "messaging_event_stream", "send_gate_decision", "metrics_materialization", "pipeline_health", "audit_trail"],
        facets: "External Systems; Identity / Security / Compliance; Data Storage & Management; Infrastructure & Operations",
      },
{
        id: "obs-07",
        title: "Delivery-event schema joins into the stream",
        implementationSource: "C1 delivery classes are first-class stream event types so gates and shells use one bounce, complaint, deferred, delivered, and rejected vocabulary.",
        start: "`event_taxonomy` (existing)",
        pathHops: ["When C1 delivery-event classes are registered at `event_taxonomy`, hard bounce, soft bounce, complaint, deferred, delivered, and rejected become first-class stream types.", "When provider delivery telemetry is accepted at `provider_webhook_receipt`, normalization maps the provider dialect to those event taxonomy classes.", "When normalized events append to `messaging_event_stream`, `suppression_list`, `reputation_unit`, and `send_gate_decision` read the same classes C7 materializes.", "When `metrics_materialization` rolls deliverability rates, it uses only those stream classes and preserves drill-through to the source events."],
        nodesTouched: ["event_taxonomy", "provider_webhook_receipt", "messaging_event_stream", "suppression_list", "reputation_unit", "send_gate_decision", "metrics_materialization"],
        facets: "External Systems; Data Storage & Management; Core Application & Runtime; Infrastructure & Operations",
      },
{
        id: "obs-08",
        title: "Funnel-step events beyond vanity opens",
        implementationSource: "Capture, opt-in, reply, invite, booking, and path-membership steps are stream facts, while opens remain diagnostic only.",
        start: "`event_taxonomy` (existing)",
        pathHops: ["When practice funnel steps are defined at `event_taxonomy`, capture submit, opt-in send/accept, nudge send, reply classified, meeting invited, and meeting booked become stream event classes.", "When those actions occur at `engagement_attempt`, `conversation_thread`, or `prepared_workspace`, the owning `producer_contract` emits the step to `messaging_event_stream`. [opens diagnostic-only]", "When steps arrive in `messaging_event_stream`, `funnel_definition` determines which ordered path and conversion window the step belongs to.", "When `metrics_materialization` computes funnel rates, it promotes practice outcomes and keeps vanity open/click events out of primary completion metrics."],
        nodesTouched: ["event_taxonomy", "producer_contract", "engagement_attempt", "conversation_thread", "prepared_workspace", "messaging_event_stream", "funnel_definition", "metrics_materialization"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations",
      },
{
        id: "obs-09",
        title: "End-to-end correlation from send to act",
        implementationSource: "The attempt envelope propagates across outbound send, provider delivery, inbound reply, classification, and act events to reconstruct one timeline.",
        start: "`correlation_envelope` (new)",
        pathHops: ["When an attempt is created at `engagement_attempt`, `correlation_envelope` records attempt id, outbound message id, channel, firm, sequence, and subject keys.", "When `outbound_message` emits send accepted to `messaging_event_stream`, the envelope travels with the stream event.", "When provider delivery events arrive at `provider_webhook_receipt`, provider message ids resolve back to `correlation_envelope` before append to `messaging_event_stream`.", "When `conversation_thread` emits reply classified or act events, the same envelope joins runtime decisions to the attempt.", "When Sequence detail or `engagement_record` reads chronology, it orders one send -> delivery -> reply -> act path from correlated stream events."],
        nodesTouched: ["engagement_attempt", "correlation_envelope", "outbound_message", "provider_webhook_receipt", "conversation_thread", "messaging_event_stream", "engagement_record"],
        facets: "Core Application & Runtime; Data Storage & Management; External Systems; Infrastructure & Operations",
      },
{
        id: "obs-10",
        title: "Metrics store distinct from raw event log",
        implementationSource: "Durable raw occurrences stay in the stream, while rollups and glance queries live in a separate materialization with drill-through to stream ids.",
        start: "`metrics_materialization` (new)",
        pathHops: ["When raw occurrence events accumulate at `messaging_event_stream`, consumers read them as the durable event log rather than querying shells directly against raw history.", "When rollup consumers apply `metric_definition`, counters, rates, funnel completions, and windows update in `metrics_materialization`. [separate retention; drill-through preserved]", "When Oversight, Firm health, Approach instrumentation, or Activation Progress reads a shell value, it reads `metrics_materialization` and cites underlying stream event ids.", "When a metric value is questioned, `audit_trail` and `messaging_event_stream` provide lineage back to source events and definitions."],
        nodesTouched: ["messaging_event_stream", "metric_definition", "metrics_materialization", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Infrastructure & Operations; Cost / FinOps",
      },
{
        id: "obs-11",
        title: "Cardinality discipline for metric labels",
        implementationSource: "Metric series use bounded labels, while contact and attempt detail remains in stream drill-down rather than exploding the aggregation store.",
        start: "`metric_definition` (existing)",
        pathHops: ["When a metric is registered at `metric_definition`, allowed dimensions are constrained to low-cardinality labels such as firm, sequence, channel, event class, outcome class, and reputation unit.", "When stream events carry contact or attempt detail at `messaging_event_stream`, those high-cardinality values remain drill-down state, not metric labels. [no contact_id, email, body hash, or raw message as metric label]", "When `metrics_materialization` rolls fleet and sequence rates, it creates bounded series and keeps contact-level evidence reachable through stream event ids.", "When a proposed metric violates label rules, `pipeline_health` records definition validation failure before the series is materialized."],
        nodesTouched: ["metric_definition", "messaging_event_stream", "metrics_materialization", "pipeline_health"],
        facets: "Data Storage & Management; Infrastructure & Operations; Cost / FinOps; Identity / Security / Compliance",
      },
{
        id: "obs-12",
        title: "Oversight Fleet health materializations",
        implementationSource: "Fleet health and Firm rows receive named stream-backed rollups for volume, delivery success, bounce, complaint, quarantine, and lag.",
        start: "`metrics_materialization` (existing)",
        pathHops: ["When delivery and send events arrive at `messaging_event_stream`, rollup consumers apply fleet SLI `metric_definition` contracts.", "When those consumers materialize send volume, delivery success, hard-bounce rate, complaint rate, and quarantine count, `metrics_materialization` stores fleet and firm-scoped series.", "When C1 updates `suppression_list` or `reputation_unit` state from the same delivery classes, Fleet health and Send gates remain aligned.", "When ingest lag affects freshness, `pipeline_health` adds lag state beside delivery SLIs so Oversight can distinguish reputation health from telemetry health."],
        nodesTouched: ["messaging_event_stream", "metric_definition", "metrics_materialization", "suppression_list", "reputation_unit", "pipeline_health"],
        facets: "Data Storage & Management; Infrastructure & Operations; Core Application & Runtime; External Systems",
      },
{
        id: "obs-13",
        title: "Firm health Sequence and Engagement materializations",
        implementationSource: "Firm health reads windowed sequence and engagement rates from stream-backed materializations with correlated attempt drill-down.",
        start: "`firm_tenancy` (existing)",
        pathHops: ["When a firm-scoped sequence emits enqueued, sent, delivered, replied, classified-stop, booked, or suppressed-block events to `messaging_event_stream`, every event carries `firm_tenancy` and correlation keys.", "When `metric_definition` defines sequence windows and step rates, rollup consumers update `metrics_materialization` per firm, sequence, channel, and event class. [tenant-scoped; windowed]", "When a suppressed-block event comes from C1 send decisions, `send_gate_decision` and `suppression_list` feed the same event path as delivered or replied outcomes.", "When Sequence detail opens a drop-off cell, `correlation_envelope` and `engagement_record` reconstruct the affected attempt timelines from stream ids."],
        nodesTouched: ["firm_tenancy", "messaging_event_stream", "metric_definition", "metrics_materialization", "send_gate_decision", "suppression_list", "correlation_envelope", "engagement_record"],
        facets: "Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime; Infrastructure & Operations",
      },
{
        id: "obs-14",
        title: "Activation Progress as readiness SLIs",
        implementationSource: "Activation Progress shows readiness signals such as producer coverage, delivery health, book-audit pass, and freshness rather than vanity engagement counters.",
        start: "`metric_definition` (existing)",
        pathHops: ["When readiness SLIs are registered at `metric_definition`, each one binds to operational inputs: producer coverage, delivery health, book-audit pass rate, and stream freshness.", "When C1 readiness state changes at `sending_domain` or `warmup_schedule`, related delivery-health events reach `messaging_event_stream`.", "When book ingestion or audit events arrive from `import_batch`, readiness materializations update without treating marketing engagement as send readiness.", "When `pipeline_health` records producer coverage or freshness state, `metrics_materialization` exposes that readiness state to Activation Progress."],
        nodesTouched: ["metric_definition", "sending_domain", "warmup_schedule", "import_batch", "messaging_event_stream", "pipeline_health", "metrics_materialization"],
        facets: "Core Application & Runtime; Data Storage & Management; Infrastructure & Operations; External Systems",
      },
{
        id: "obs-15",
        title: "Approach instrumentation joins first-party steps to proxies",
        implementationSource: "First-party capture and activation steps carry campaign and creative keys so Approach instrumentation can join them to platform proxy aggregates.",
        start: "`campaign_proxy_aggregate` (new)",
        pathHops: ["When ad-platform proxy data is imported at `campaign_proxy_aggregate`, campaign and creative keys become joinable aggregate state rather than a standalone vanity source.", "When Capture strip and activation steps occur at `prepared_workspace` or `engagement_attempt`, the owning `producer_contract` emits first-party funnel events with the same campaign and creative keys to `messaging_event_stream`.", "When `funnel_definition` identifies proxy-pair and first-party steps, rollup consumers join `campaign_proxy_aggregate` to stream-backed steps inside `metrics_materialization`. [first-party join required; proxy is not truth alone]", "When a campaign or creative lacks required keys, `pipeline_health` records producer/key coverage failure and the variant is not scoreable."],
        nodesTouched: ["campaign_proxy_aggregate", "prepared_workspace", "engagement_attempt", "producer_contract", "messaging_event_stream", "funnel_definition", "metrics_materialization", "pipeline_health"],
        facets: "External Systems; Data Storage & Management; Core Application & Runtime; Infrastructure & Operations",
      },
{
        id: "obs-16",
        title: "Acquisition reading rules reject vanity CTR",
        implementationSource: "Approach and Acquisition metrics promote form-open, intro-complete, qualified capture, first-text answered, and workspace open ahead of CTR/open diagnostics.",
        start: "`metric_definition` (existing)",
        pathHops: ["When Acquisition or Approach shell metrics are registered at `metric_definition`, primary metrics are marked as form-open, intro-complete, qualified capture, first-text answered, and prepared-workspace open.", "When CTR or open events enter `messaging_event_stream`, `metric_definition` marks them diagnostic-only so they cannot satisfy primary success or kill/continue criteria.", "When `metrics_materialization` rolls campaign and approach series, it orders primary outcome series ahead of diagnostic click/open series.", "When PM-facing reading rules change, `audit_trail` records the metric definition version so older decisions remain interpretable."],
        nodesTouched: ["metric_definition", "messaging_event_stream", "metrics_materialization", "audit_trail"],
        facets: "Core Application & Runtime; Data Storage & Management; Identity / Security / Compliance",
      },
{
        id: "obs-17",
        title: "Versioned funnel definitions as operable objects",
        implementationSource: "Shell drop-offs are computed from versioned funnel definitions rather than ad-hoc per-surface step lists.",
        start: "`funnel_definition` (new)",
        pathHops: ["When a funnel is published at `funnel_definition`, ordered taxonomy steps, conversion window, subject key, and primary/diagnostic roles become versioned state.", "When matching stream events arrive at `messaging_event_stream`, the active `funnel_definition` selects eligible steps by event time and subject key.", "When completions and drop-offs are computed, `metrics_materialization` stores values tagged to the funnel definition version.", "When a funnel definition changes, `audit_trail` records the new version and existing historical values keep their prior version binding."],
        nodesTouched: ["funnel_definition", "event_taxonomy", "messaging_event_stream", "metrics_materialization", "audit_trail"],
        facets: "Data Storage & Management; Core Application & Runtime; Identity / Security / Compliance; Infrastructure & Operations",
      },
{
        id: "obs-18",
        title: "Freshness SLO for shell fill",
        implementationSource: "Stream-to-shell lag has a declared SLO, and stale Oversight or Firm health projections surface as telemetry health, not current truth.",
        start: "`pipeline_health` (existing)",
        pathHops: ["When freshness budgets are declared at `metric_definition`, delivery and funnel event paths receive ingest-to-materialization lag targets.", "When events land at `messaging_event_stream`, `pipeline_health` measures producer time, provider receipt time, stream append time, and materialization time. [freshness SLO; p95 lag budget]", "When lag exceeds budget, `pipeline_health` marks stale state for the affected firm, stream class, or shell scope.", "When `metrics_materialization` serves shell values, it includes freshness state so operators do not treat stale rates as current."],
        nodesTouched: ["metric_definition", "messaging_event_stream", "pipeline_health", "metrics_materialization", "firm_tenancy"],
        facets: "Infrastructure & Operations; Data Storage & Management; Core Application & Runtime; Identity / Security / Compliance",
      },
{
        id: "obs-19",
        title: "Late, duplicate, and out-of-order event handling",
        implementationSource: "Aggregations use event time, watermarks, and idempotent upserts so late or reordered provider and reply events correct shell rates within policy.",
        start: "`messaging_event_stream` (existing)",
        pathHops: ["When provider or runtime events append to `messaging_event_stream`, each event stores occurred time separately from processing time.", "When duplicate retries land through `provider_webhook_receipt`, stream ids and receipt idempotency keys prevent double application before aggregation.", "When out-of-order events arrive within a watermark, `metrics_materialization` recomputes affected windows and funnel completions by event time. [event-time truth; late-arrival watermark]", "When late events arrive outside the policy window, `pipeline_health` records the late-drop or late-audit state and `audit_trail` preserves the correction decision.", "When Sequence detail reads timelines, `correlation_envelope` orders attempt history by event time while showing late corrections where applied."],
        nodesTouched: ["messaging_event_stream", "provider_webhook_receipt", "metrics_materialization", "pipeline_health", "audit_trail", "correlation_envelope"],
        facets: "Data Storage & Management; Infrastructure & Operations; External Systems; Identity / Security / Compliance",
      },
{
        id: "obs-20",
        title: "Multi-tenant isolation in the metrics store",
        implementationSource: "Every stream event and metric series is firm-scoped, with fleet rollups allowed only through deliberate Oversight scope.",
        start: "`firm_tenancy` (existing)",
        pathHops: ["When an event is emitted or received, `firm_tenancy` supplies the required tenant key before the event can append to `messaging_event_stream`. [tenant key required]", "When `metrics_materialization` creates a series, the series is keyed by firm and, for deliverability, reputation unit where applicable.", "When Firm health queries materialized rates or stream drills, `firm_tenancy` denies peer-firm series and event ids.", "When Oversight reads Fleet health, `firm_tenancy` allows deliberate fleet rollup while preserving scoped descent into Firm row and Firm health.", "When an event or metric lacks tenant scope, `pipeline_health` records isolation failure and `audit_trail` preserves the rejected access or ingest outcome."],
        nodesTouched: ["firm_tenancy", "messaging_event_stream", "metrics_materialization", "reputation_unit", "pipeline_health", "audit_trail"],
        facets: "Identity / Security / Compliance; Data Storage & Management; Core Application & Runtime; Infrastructure & Operations",
      },
{
        id: "obs-21",
        title: "Metric definition audit for every shell tile",
        implementationSource: "Every shell number opens to a versioned metric definition and definition changes are auditable.",
        start: "`metric_definition` (existing)",
        pathHops: ["When a shell tile is made operable, `metric_definition` stores its taxonomy inputs, filters, window, unit, owner, and shell binding.", "When rollup consumers compute that tile, `metrics_materialization` stores values with the metric definition version and source stream ids.", "When a user opens the definition behind a shell number, `metric_definition` returns the current formula and `audit_trail` returns the change history.", "When a metric formula changes, a new definition version is written and historical materializations keep their prior version reference."],
        nodesTouched: ["metric_definition", "metrics_materialization", "messaging_event_stream", "audit_trail"],
        facets: "Data Storage & Management; Identity / Security / Compliance; Core Application & Runtime",
      },
{
        id: "obs-22",
        title: "Pipeline SLIs make ingest health first-class",
        implementationSource: "Oversight sees ingest accept, verify fail, dead-letter, consumer lag, and materialization success so empty shells become incidents.",
        start: "`pipeline_health` (existing)",
        pathHops: ["When provider receipts, stream appends, consumer reads, and rollup writes occur, each producer emits pipeline SLI events to `messaging_event_stream` or updates `pipeline_health` directly at the same event/state altitude.", "When webhook verification fails at `provider_webhook_receipt`, normalize fails before stream append, or a consumer dead-letters an event, `pipeline_health` records the failed class and affected producer.", "When consumers lag or materialization writes fail, `pipeline_health` records lag, dead-letter depth, and materialization success state against the freshness SLO.", "When Oversight Fleet health reads shell-fill state, `metrics_materialization` surfaces Pipeline health beside fleet deliverability SLIs so zeroes are not mistaken for healthy silence.", "When pipeline SLI thresholds burn, `audit_trail` records the incident-relevant state transitions and source event ids."],
        nodesTouched: ["pipeline_health", "provider_webhook_receipt", "messaging_event_stream", "metrics_materialization", "audit_trail"],
        facets: "Infrastructure & Operations; Data Storage & Management; External Systems; Core Application & Runtime",
      }
    ],
  }
];

export function getWiringSeatTraces(seatId: string): WiringSeatTraces | undefined {
  return WIRING_FUNCTION_TRACES.find((seat) => seat.id === seatId);
}

export function getWiringTraceItem(seatId: string, traceId: string) {
  return getWiringSeatTraces(seatId)?.items.find((item) => item.id === traceId);
}
